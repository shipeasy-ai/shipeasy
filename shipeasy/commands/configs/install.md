---
description: "[moved] Use /shipeasy:flags:install instead — one install now covers gates + configs + kill switches + experiments + events"
---

This command moved to **/shipeasy:flags:install**. If it's missing or errors, update first: `npm i -g @shipeasy/cli@latest`, then `/plugin marketplace update shipeasy` → `/plugin install shipeasy@shipeasy`. Then run **/shipeasy:flags:install**.
