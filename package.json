{
  "name": "@shipeasy/repo",
  "private": true,
  "version": "0.0.0",
  "description": "shipeasy-ai/shipeasy root: the plugin marketplace (shipeasy/) plus the TS tooling workspace (@shipeasy/openapi, @shipeasy/mcp, @shipeasy/cli).",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "scripts": {
    "build": "turbo build",
    "test": "turbo test",
    "type-check": "turbo type-check",
    "lint": "turbo lint",
    "docs": "turbo docs"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.7.4"
  }
}
