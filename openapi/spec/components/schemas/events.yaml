ListEventsResponse:
  type: array
  items:
    type: object
    properties:
      id:
        type: string
        description: Stable opaque event id.
      name:
        type: string
        description: Event name — the handle metric queries reference.
      folder:
        anyOf:
          - description: "Optional folder name used to group items in the dashboard. Part of the SDK lookup key: an item in folder `checkout` named `new-cart` is referenced as `checkout/new-cart` from the SDK."
            anyOf:
              - type: string
                maxLength: 256
                pattern: ^[a-zA-Z0-9_-]+$
              - type: "null"
          - type: "null"
        description: Folder the event is filed under, or `null` if at the root.
      description:
        anyOf:
          - type: string
          - type: "null"
        description: Human-readable description, or `null` if none set.
      properties:
        type: array
        items:
          type: object
          properties:
            name:
              type: string
              minLength: 1
              maxLength: 64
              pattern: ^[a-zA-Z_][a-zA-Z_0-9]*$
              description: Property key, e.g. `amount` or `plan`. Must be a bare identifier — it is emitted as a SQL alias when the property is used as a `by()` dimension, and there is no way to quote one in the Analytics Engine dialect.
            type:
              type: string
              enum:
                - string
                - number
                - boolean
              description: Value type of the property as recorded on the event.
            required:
              default: false
              description: Whether the property must be present on every emitted event.
              type: boolean
            description:
              default: ""
              description: Human-readable description of the property.
              type: string
          required:
            - name
            - type
          additionalProperties: false
          description: One typed property declared on a catalogued event.
        description: Typed properties declared on the event.
      pending:
        type: integer
        minimum: -9007199254740991
        maximum: 9007199254740991
        description: "`1` if this is an auto-discovered name awaiting approval (metrics on it fail until approved), `0` if approved/usable."
      createdAt:
        type: string
        description: ISO-8601 timestamp of creation.
    required:
      - id
      - name
      - description
      - properties
      - pending
      - createdAt
    additionalProperties: false
    description: A catalogued event.
  description: Every catalogued event in the project (including pending auto-discovered names).
GetEventResponse:
  type: object
  properties:
    id:
      type: string
      description: Stable opaque event id.
    name:
      type: string
      description: Event name — the handle metric queries reference.
    folder:
      anyOf:
        - description: "Optional folder name used to group items in the dashboard. Part of the SDK lookup key: an item in folder `checkout` named `new-cart` is referenced as `checkout/new-cart` from the SDK."
          anyOf:
            - type: string
              maxLength: 256
              pattern: ^[a-zA-Z0-9_-]+$
            - type: "null"
        - type: "null"
      description: Folder the event is filed under, or `null` if at the root.
    description:
      anyOf:
        - type: string
        - type: "null"
      description: Human-readable description, or `null` if none set.
    properties:
      type: array
      items:
        type: object
        properties:
          name:
            type: string
            minLength: 1
            maxLength: 64
            pattern: ^[a-zA-Z_][a-zA-Z_0-9]*$
            description: Property key, e.g. `amount` or `plan`. Must be a bare identifier — it is emitted as a SQL alias when the property is used as a `by()` dimension, and there is no way to quote one in the Analytics Engine dialect.
          type:
            type: string
            enum:
              - string
              - number
              - boolean
            description: Value type of the property as recorded on the event.
          required:
            default: false
            description: Whether the property must be present on every emitted event.
            type: boolean
          description:
            default: ""
            description: Human-readable description of the property.
            type: string
        required:
          - name
          - type
        additionalProperties: false
        description: One typed property declared on a catalogued event.
      description: Typed properties declared on the event.
    pending:
      type: integer
      minimum: -9007199254740991
      maximum: 9007199254740991
      description: "`1` if this is an auto-discovered name awaiting approval (metrics on it fail until approved), `0` if approved/usable."
    createdAt:
      type: string
      description: ISO-8601 timestamp of creation.
  required:
    - id
    - name
    - description
    - properties
    - pending
    - createdAt
  additionalProperties: false
  description: A catalogued event.
CreateEventRequest:
  type: object
  properties:
    name:
      type: string
      pattern: ^[a-zA-Z0-9_][a-zA-Z0-9_\-.]{0,49}$
      description: Event name. Starts with a letter, digit, or `_`; letters, digits, `_`, `-`, `.`; max 50 characters. The cap is the Analytics Engine index budget — 96 bytes total, less a 36-byte project UUID and a separator — and the API has always enforced 50 while this spec advertised 128. The charset is ASCII, so characters and bytes are the same count. Immutable after create — this is the handle metric queries reference.
    folder:
      $ref: ./common.yaml#/Folder
    description:
      description: Optional human-readable description of the event.
      type: string
    properties:
      default: []
      description: Typed properties declared on the event. Defaults to an empty list.
      type: array
      items:
        type: object
        properties:
          name:
            type: string
            minLength: 1
            maxLength: 64
            pattern: ^[a-zA-Z_][a-zA-Z_0-9]*$
            description: Property key, e.g. `amount` or `plan`. Must be a bare identifier — it is emitted as a SQL alias when the property is used as a `by()` dimension, and there is no way to quote one in the Analytics Engine dialect.
          type:
            type: string
            enum:
              - string
              - number
              - boolean
            description: Value type of the property as recorded on the event.
          required:
            default: false
            description: Whether the property must be present on every emitted event.
            type: boolean
          description:
            default: ""
            description: Human-readable description of the property.
            type: string
        required:
          - name
          - type
        additionalProperties: false
        description: One typed property declared on a catalogued event.
  required:
    - name
  additionalProperties: false
  description: Body for `POST /api/admin/events`. Only `name` is required.
CreateEventResponse:
  type: object
  properties:
    id:
      type: string
      description: Newly assigned event id.
    name:
      type: string
      description: The event name that was registered.
  required:
    - id
    - name
  additionalProperties: false
  description: Result of creating (or approving an existing pending) event.
UpdateEventRequest:
  type: object
  properties:
    folder:
      $ref: ./common.yaml#/Folder
    description:
      description: New description for the event.
      type: string
    properties:
      description: Replaces the full property set (no merge). Omit to leave properties unchanged.
      type: array
      items:
        type: object
        properties:
          name:
            type: string
            minLength: 1
            maxLength: 64
            pattern: ^[a-zA-Z_][a-zA-Z_0-9]*$
            description: Property key, e.g. `amount` or `plan`. Must be a bare identifier — it is emitted as a SQL alias when the property is used as a `by()` dimension, and there is no way to quote one in the Analytics Engine dialect.
          type:
            type: string
            enum:
              - string
              - number
              - boolean
            description: Value type of the property as recorded on the event.
          required:
            default: false
            description: Whether the property must be present on every emitted event.
            type: boolean
          description:
            default: ""
            description: Human-readable description of the property.
            type: string
        required:
          - name
          - type
        additionalProperties: false
        description: One typed property declared on a catalogued event.
  additionalProperties: false
  description: Body for `PATCH /api/admin/events/{id}` and `POST /api/admin/events/{id}/approve`. All fields optional; `name` is immutable after create.
UpdateEventResponse:
  type: object
  properties:
    id:
      type: string
      description: Event id that was updated.
  required:
    - id
  additionalProperties: false
  description: Result of updating or approving an event.
ApproveEventRequest:
  type: object
  properties:
    folder:
      $ref: ./common.yaml#/Folder
    description:
      description: New description for the event.
      type: string
    properties:
      description: Replaces the full property set (no merge). Omit to leave properties unchanged.
      type: array
      items:
        type: object
        properties:
          name:
            type: string
            minLength: 1
            maxLength: 64
            pattern: ^[a-zA-Z_][a-zA-Z_0-9]*$
            description: Property key, e.g. `amount` or `plan`. Must be a bare identifier — it is emitted as a SQL alias when the property is used as a `by()` dimension, and there is no way to quote one in the Analytics Engine dialect.
          type:
            type: string
            enum:
              - string
              - number
              - boolean
            description: Value type of the property as recorded on the event.
          required:
            default: false
            description: Whether the property must be present on every emitted event.
            type: boolean
          description:
            default: ""
            description: Human-readable description of the property.
            type: string
        required:
          - name
          - type
        additionalProperties: false
        description: One typed property declared on a catalogued event.
  additionalProperties: false
  description: Body for `PATCH /api/admin/events/{id}` and `POST /api/admin/events/{id}/approve`. All fields optional; `name` is immutable after create.
ApproveEventResponse:
  type: object
  properties:
    id:
      type: string
      description: Event id that was updated.
  required:
    - id
  additionalProperties: false
  description: Result of updating or approving an event.
DeleteEventResponse:
  type: object
  properties:
    ok:
      type: boolean
      const: true
  required:
    - ok
  additionalProperties: false
  description: Confirmation that the event was archived.
