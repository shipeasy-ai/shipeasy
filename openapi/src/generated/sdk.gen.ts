// This file is auto-generated by @hey-api/openapi-ts

import type { Client, ClientMeta, Options as Options2, RequestResult, TDataShape } from './client';
import { client } from './client.gen';
import type { ApproveEventData, ApproveEventErrors, ApproveEventResponses, CreateAlertRuleData, CreateAlertRuleErrors, CreateAlertRuleResponses, CreateBugData, CreateBugErrors, CreateBugResponses, CreateConfigData, CreateConfigErrors, CreateConfigResponses, CreateEventData, CreateEventErrors, CreateEventResponses, CreateExperimentData, CreateExperimentErrors, CreateExperimentResponses, CreateFeatureRequestData, CreateFeatureRequestErrors, CreateFeatureRequestResponses, CreateGateData, CreateGateErrors, CreateGateResponses, CreateI18nProfileData, CreateI18nProfileErrors, CreateI18nProfileResponses, CreateKillswitchData, CreateKillswitchErrors, CreateKillswitchResponses, CreateMetricData, CreateMetricErrors, CreateMetricResponses, CreateUniverseData, CreateUniverseErrors, CreateUniverseResponses, DeleteAlertRuleData, DeleteAlertRuleErrors, DeleteAlertRuleResponses, DeleteConfigData, DeleteConfigErrors, DeleteConfigResponses, DeleteEventData, DeleteEventErrors, DeleteEventResponses, DeleteExperimentData, DeleteExperimentErrors, DeleteExperimentResponses, DeleteGateData, DeleteGateErrors, DeleteGateResponses, DeleteKillswitchData, DeleteKillswitchErrors, DeleteKillswitchResponses, DeleteMetricData, DeleteMetricErrors, DeleteMetricResponses, DeleteUniverseData, DeleteUniverseErrors, DeleteUniverseResponses, DisableGateData, DisableGateErrors, DisableGateResponses, DiscardConfigDraftData, DiscardConfigDraftErrors, DiscardConfigDraftResponses, EnableGateData, EnableGateErrors, EnableGateResponses, GetConfigData, GetConfigErrors, GetConfigResponses, GetCurrentProjectData, GetCurrentProjectErrors, GetCurrentProjectResponses, GetEventData, GetEventErrors, GetEventResponses, GetExperimentData, GetExperimentErrors, GetExperimentResponses, GetExperimentResultsData, GetExperimentResultsErrors, GetExperimentResultsResponses, GetExperimentTimeseriesData, GetExperimentTimeseriesErrors, GetExperimentTimeseriesResponses, GetKillswitchData, GetKillswitchErrors, GetKillswitchResponses, GetMetricData, GetMetricErrors, GetMetricResponses, GetOpsItemData, GetOpsItemErrors, GetOpsItemResponses, LinkPrToOpsItemData, LinkPrToOpsItemErrors, LinkPrToOpsItemResponses, ListAlertRulesData, ListAlertRulesErrors, ListAlertRulesResponses, ListAttributesData, ListAttributesErrors, ListAttributesResponses, ListConfigActivityData, ListConfigActivityErrors, ListConfigActivityResponses, ListConfigsData, ListConfigsErrors, ListConfigsResponses, ListEventsData, ListEventsErrors, ListEventsResponses, ListExperimentsData, ListExperimentsErrors, ListExperimentsResponses, ListGatesData, ListGatesErrors, ListGatesResponses, ListI18nDraftsData, ListI18nDraftsErrors, ListI18nDraftsResponses, ListI18nKeysData, ListI18nKeysErrors, ListI18nKeysResponses, ListI18nProfilesData, ListI18nProfilesErrors, ListI18nProfilesResponses, ListKillswitchesData, ListKillswitchesErrors, ListKillswitchesResponses, ListMetricsData, ListMetricsErrors, ListMetricsResponses, ListOpsItemsData, ListOpsItemsErrors, ListOpsItemsResponses, ListSlackChannelsData, ListSlackChannelsErrors, ListSlackChannelsResponses, ListUniversesData, ListUniversesErrors, ListUniversesResponses, NotifyOpsData, NotifyOpsErrors, NotifyOpsResponses, PublishConfigDraftData, PublishConfigDraftErrors, PublishConfigDraftResponses, PublishI18nProfileData, PublishI18nProfileErrors, PublishI18nProfileResponses, PushI18nKeysData, PushI18nKeysErrors, PushI18nKeysResponses, ReanalyzeExperimentData, ReanalyzeExperimentErrors, ReanalyzeExperimentResponses, SaveConfigDraftData, SaveConfigDraftErrors, SaveConfigDraftResponses, SetExperimentMetricsData, SetExperimentMetricsErrors, SetExperimentMetricsResponses, SetExperimentStatusData, SetExperimentStatusErrors, SetExperimentStatusResponses, SetKillswitchSwitchData, SetKillswitchSwitchErrors, SetKillswitchSwitchResponses, UnsetKillswitchSwitchData, UnsetKillswitchSwitchErrors, UnsetKillswitchSwitchResponses, UpdateAlertRuleData, UpdateAlertRuleErrors, UpdateAlertRuleResponses, UpdateConfigData, UpdateConfigErrors, UpdateConfigResponses, UpdateEventData, UpdateEventErrors, UpdateEventResponses, UpdateExperimentData, UpdateExperimentErrors, UpdateExperimentResponses, UpdateGateData, UpdateGateErrors, UpdateGateResponses, UpdateI18nKeyData, UpdateI18nKeyErrors, UpdateI18nKeyResponses, UpdateKillswitchData, UpdateKillswitchErrors, UpdateKillswitchResponses, UpdateOpsItemData, UpdateOpsItemErrors, UpdateOpsItemResponses, UpdateUniverseData, UpdateUniverseErrors, UpdateUniverseResponses, UpsertProjectData, UpsertProjectErrors, UpsertProjectResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: keyof ClientMeta extends never ? Record<string, unknown> : ClientMeta;
};

/**
 * List feature gates
 *
 * Returns a single page of gates ordered by `updated_at desc, id desc`. Use the `cursor` query parameter to paginate.
 *
 * **Use case:** Snapshot every gate in the project — for example to render an admin overview or to drive a CI check that asserts no gate is left at 100% in staging.
 */
export const listGates = <ThrowOnError extends boolean = false>(options?: Options<ListGatesData, ThrowOnError>): RequestResult<ListGatesResponses, ListGatesErrors, ThrowOnError> => (options?.client ?? client).get<ListGatesResponses, ListGatesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/gates',
    ...options
});

/**
 * Create a feature gate
 *
 * Creates a new gate. Default `enabled: true` at the supplied `rollout_pct` (`0` = fully dark).
 *
 * Only `name` is required. Request fields use snake_case (`owner_email`); the GET response returns camelCase (`ownerEmail`, `groupName`).
 *
 * Returns `409` if `name` already exists in the project (case-sensitive).
 *
 * **Use cases**
 *
 * - **Dark create + ramp later** — `{ "name": "checkout_v2" }` at 0% rollout. Ramp via PATCH after deploy validation.
 * - **Targeted rollout** — supply `rules` to gate the caller (e.g. only `plan = pro` users) plus a `rollout_pct` to bucket within that audience.
 * - **Gatekeeper stack** — supply `stack` instead of `rules`/`rollout_pct` for `internal ∪ beta ∪ public` fall-through. Stack entries evaluated top-to-bottom; first match wins.
 * - **Dashboard metadata** — populate `title`, `description`, `folder`, `group`, `owner_email` so the admin UI is self-documenting from day one.
 * - **Disabled on create** — pre-provision with `enabled: false` for a future launch; flip on with `POST /{id}/enable` at go-live.
 */
export const createGate = <ThrowOnError extends boolean = false>(options: Options<CreateGateData, ThrowOnError>): RequestResult<CreateGateResponses, CreateGateErrors, ThrowOnError> => (options.client ?? client).post<CreateGateResponses, CreateGateErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/gates',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a feature gate
 *
 * Soft-deletes the gate. Returns 409 if the gate is still referenced by a running experiment as a targeting gate — stop the experiment first.
 *
 * **Use case:** Tear down a gate after a feature has fully shipped and the rollout flag is no longer needed.
 */
export const deleteGate = <ThrowOnError extends boolean = false>(options: Options<DeleteGateData, ThrowOnError>): RequestResult<DeleteGateResponses, DeleteGateErrors, ThrowOnError> => (options.client ?? client).delete<DeleteGateResponses, DeleteGateErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/gates/{id}',
    ...options
});

/**
 * Update a feature gate
 *
 * Partial update — only supplied fields change. Array fields (`rules`, `stack`) **replace** wholesale; there is no merge or append.
 *
 * `name` and the gate id are immutable. The response carries only `{ id }` — re-fetch via `GET /api/admin/gates` for the new row.
 *
 * **Use cases**
 *
 * - **Ramp rollout** — `{ "rollout_pct": 5000 }` for 50%. Basis points (0–10000); `100` = 1%.
 * - **Kill switch** — `{ "enabled": false }`. Forces evaluation to `false` for every caller regardless of rules/rollout. Re-enable with `POST /{id}/enable` or `{ "enabled": true }`.
 * - **Modify a rule's `in` set** — send the full new `rules` array. To add `'GB'` to `['US','CA']`: `{ "rules": [{ "attr": "country", "op": "in", "value": ["US","CA","GB"] }] }`. No per-rule patch endpoint.
 * - **Add targeting from scratch** — `{ "rules": [{ "attr": "email", "op": "regex", "value": "@acme\\.com$" }] }`.
 * - **Switch to gatekeeper stack** — send a non-null `stack`. To revert to flat eval, send `{ "stack": null }`.
 * - **Update metadata** — any subset of `title`, `description`, `folder`, `group`, `owner_email`.
 */
export const updateGate = <ThrowOnError extends boolean = false>(options: Options<UpdateGateData, ThrowOnError>): RequestResult<UpdateGateResponses, UpdateGateErrors, ThrowOnError> => (options.client ?? client).patch<UpdateGateResponses, UpdateGateErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/gates/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Enable a gate
 *
 * Sets `enabled: true`. The current `rollout_pct` is preserved.
 *
 * **Use case:** Re-enable a previously disabled gate without re-issuing a full update.
 */
export const enableGate = <ThrowOnError extends boolean = false>(options: Options<EnableGateData, ThrowOnError>): RequestResult<EnableGateResponses, EnableGateErrors, ThrowOnError> => (options.client ?? client).post<EnableGateResponses, EnableGateErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/gates/{id}/enable',
    ...options
});

/**
 * Disable a gate
 *
 * Sets `enabled: false` so the gate evaluates to `false` for every caller, regardless of `rollout_pct` or `rules`. Use as a quick kill switch.
 *
 * **Use case:** Flip a gate off in production without redeploying — the canonical kill-switch flow.
 */
export const disableGate = <ThrowOnError extends boolean = false>(options: Options<DisableGateData, ThrowOnError>): RequestResult<DisableGateResponses, DisableGateErrors, ThrowOnError> => (options.client ?? client).post<DisableGateResponses, DisableGateErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/gates/{id}/disable',
    ...options
});

/**
 * List experiments
 *
 * Returns a single page of non-archived experiments ordered by `updated_at desc, id desc`. Use the `cursor` query parameter to paginate.
 *
 * **Use case:** Snapshot every active experiment in the project — e.g. render an overview dashboard or drive a CI check that no experiment has been running past its `min_runtime_days`.
 */
export const listExperiments = <ThrowOnError extends boolean = false>(options?: Options<ListExperimentsData, ThrowOnError>): RequestResult<ListExperimentsResponses, ListExperimentsErrors, ThrowOnError> => (options?.client ?? client).get<ListExperimentsResponses, ListExperimentsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/experiments',
    ...options
});

/**
 * Create an experiment
 *
 * Creates a new experiment in `draft` status. `name`, `universe`, and `groups` are required; everything else has sensible defaults.
 *
 * Returns `409` if `name` already exists, `422` if the named universe doesn't exist, `403` if a plan-gated option is set (`sequential_testing`, custom `significance_threshold`) on a plan that doesn't include it.
 *
 * **Use cases**
 *
 * - **Minimal 50/50** — `name` + `universe` + two equal-weight groups.
 * - **Targeted rollout** — supply `targeting_gate` to restrict the eligible audience and `allocation_pct` to enrol a slice of it.
 * - **Multivariant** — three or more groups with weights summing to 10000.
 * - **Sequential testing** — `sequential_testing: true` for Premium plans.
 */
export const createExperiment = <ThrowOnError extends boolean = false>(options: Options<CreateExperimentData, ThrowOnError>): RequestResult<CreateExperimentResponses, CreateExperimentErrors, ThrowOnError> => (options.client ?? client).post<CreateExperimentResponses, CreateExperimentErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/experiments',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete an experiment
 *
 * Archives the experiment (soft-delete via status transition). Returns `409` if the experiment is still `running` — stop it first.
 *
 * **Use case:** Tear down an experiment after the analysis is signed off.
 */
export const deleteExperiment = <ThrowOnError extends boolean = false>(options: Options<DeleteExperimentData, ThrowOnError>): RequestResult<DeleteExperimentResponses, DeleteExperimentErrors, ThrowOnError> => (options.client ?? client).delete<DeleteExperimentResponses, DeleteExperimentErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/experiments/{id}',
    ...options
});

/**
 * Get one experiment
 *
 * Returns the full experiment row including groups, params, allocation, and lifecycle timestamps.
 *
 * **Use case:** Fetch one experiment to render the detail page or to inspect its current allocation and group weights.
 */
export const getExperiment = <ThrowOnError extends boolean = false>(options: Options<GetExperimentData, ThrowOnError>): RequestResult<GetExperimentResponses, GetExperimentErrors, ThrowOnError> => (options.client ?? client).get<GetExperimentResponses, GetExperimentErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/experiments/{id}',
    ...options
});

/**
 * Update an experiment
 *
 * Partial update. `allocation_pct`, `groups`, `salt`, `universe`, `params` are **immutable while running** — returns `409` if you try. Stop the experiment first.
 *
 * Editing `groups` while in `draft` is fine; weights must still sum to 10000.
 *
 * **Use cases**
 *
 * - **Update metadata** — `description`, `tag`, `targeting_gate` editable any time.
 * - **Ramp before launch** — set `allocation_pct` while still in `draft`.
 * - **Tighten significance** — `significance_threshold` (Pro+).
 * - **Rewire groups** — replace `groups` wholesale while in `draft`; immutable once running.
 */
export const updateExperiment = <ThrowOnError extends boolean = false>(options: Options<UpdateExperimentData, ThrowOnError>): RequestResult<UpdateExperimentResponses, UpdateExperimentErrors, ThrowOnError> => (options.client ?? client).patch<UpdateExperimentResponses, UpdateExperimentErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/experiments/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Transition experiment status
 *
 * Drives the experiment lifecycle. Allowed transitions:
 *
 * - `draft → running` — starts allocation. Bumps the `startedAt` timestamp.
 * - `running → stopped` — halts allocation. Existing exposures stay in the dataset.
 * - `stopped → archived` — soft-delete.
 * - `draft → archived` — discard an unstarted experiment.
 * - `archived → draft` — restore a soft-deleted experiment so it can be re-completed and started. Allowed only if it never started; one that already ran must be cloned instead.
 *
 * Restarting an `archived` experiment directly is not allowed — restore it to `draft` first. Returns `409` on illegal transitions and `429` if the plan's `experiments_running` limit is exceeded on `→ running`.
 *
 * **Use cases**
 *
 * - **Start** — `{ "status": "running" }` after wiring up the SDK and verifying targeting on staging.
 * - **Stop** — `{ "status": "stopped" }` once the experiment hits its `min_runtime_days` and conclusive results land.
 * - **Archive** — `{ "status": "archived" }` to soft-delete after sign-off.
 */
export const setExperimentStatus = <ThrowOnError extends boolean = false>(options: Options<SetExperimentStatusData, ThrowOnError>): RequestResult<SetExperimentStatusResponses, SetExperimentStatusErrors, ThrowOnError> => (options.client ?? client).post<SetExperimentStatusResponses, SetExperimentStatusErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/experiments/{id}/status',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Attach metrics
 *
 * Replaces the experiment's metric attachments wholesale. Each entry pairs an existing `metric_id` with a `role` (`goal` / `guardrail` / `secondary`).
 *
 * Returns `422` if any `metric_id` doesn't exist in the project. Pass `{ metrics: [] }` to detach everything.
 *
 * **Use cases**
 *
 * - **Standard setup** — one `goal`, one or two `guardrail`, optional `secondary` metrics for diagnostics.
 * - **Detach all** — send `{ "metrics": [] }` before archiving.
 */
export const setExperimentMetrics = <ThrowOnError extends boolean = false>(options: Options<SetExperimentMetricsData, ThrowOnError>): RequestResult<SetExperimentMetricsResponses, SetExperimentMetricsErrors, ThrowOnError> => (options.client ?? client).post<SetExperimentMetricsResponses, SetExperimentMetricsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/experiments/{id}/metrics',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get analysis results
 *
 * Returns the latest analysis output for the experiment — one row per metric/group/day, including sample size, mean, % delta vs. control, p-value, and a sample-ratio mismatch flag.
 *
 * **Use case:** Render the results table on the experiment detail page or drive an automated decision once a `goal` metric reaches significance.
 */
export const getExperimentResults = <ThrowOnError extends boolean = false>(options: Options<GetExperimentResultsData, ThrowOnError>): RequestResult<GetExperimentResultsResponses, GetExperimentResultsErrors, ThrowOnError> => (options.client ?? client).get<GetExperimentResultsResponses, GetExperimentResultsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/experiments/{id}/results',
    ...options
});

/**
 * Get analysis timeseries
 *
 * Same row shape as `/results`, but returns every daily slice rather than the latest. Filter to a single metric with the `metric` query parameter.
 *
 * **Use case:** Drive a chart of metric movement over the experiment runtime, or sanity-check the lift is monotonic before deciding.
 */
export const getExperimentTimeseries = <ThrowOnError extends boolean = false>(options: Options<GetExperimentTimeseriesData, ThrowOnError>): RequestResult<GetExperimentTimeseriesResponses, GetExperimentTimeseriesErrors, ThrowOnError> => (options.client ?? client).get<GetExperimentTimeseriesResponses, GetExperimentTimeseriesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/experiments/{id}/timeseries',
    ...options
});

/**
 * Re-queue analysis
 *
 * Requeues the daily analysis pass for this experiment outside the normal cron cadence. Useful after attaching a new metric or correcting an event taxonomy. The job runs asynchronously.
 *
 * **Use case:** Force-refresh results after wiring up a new metric without waiting for the next nightly cron tick.
 */
export const reanalyzeExperiment = <ThrowOnError extends boolean = false>(options: Options<ReanalyzeExperimentData, ThrowOnError>): RequestResult<ReanalyzeExperimentResponses, ReanalyzeExperimentErrors, ThrowOnError> => (options.client ?? client).post<ReanalyzeExperimentResponses, ReanalyzeExperimentErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/experiments/{id}/reanalyze',
    ...options
});

/**
 * List dynamic configs
 *
 * Returns a single page of configs ordered by `updated_at desc, id desc`. Each row includes the latest published `version` per env and any active drafts.
 *
 * **Use case:** Snapshot every config in the project — e.g. CI check that asserts no env is stuck on a stale default or that every config has a published value on prod.
 */
export const listConfigs = <ThrowOnError extends boolean = false>(options?: Options<ListConfigsData, ThrowOnError>): RequestResult<ListConfigsResponses, ListConfigsErrors, ThrowOnError> => (options?.client ?? client).get<ListConfigsResponses, ListConfigsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/configs',
    ...options
});

/**
 * Create a dynamic config
 *
 * Creates a new config with the given `schema`. The initial `value` (or an empty object) is published as version 1 on **every** env.
 *
 * Returns `409` if `name` already exists in the project, `400` if `value` doesn't validate against `schema`.
 *
 * **Use cases**
 *
 * - **Minimal create** — `name` + `schema`. Initial value defaults to `{}`.
 * - **Seeded create** — supply a flat `value` to publish the same object on every env.
 * - **Per-env seed** — supply a `{ env: value }` map for different per-env starting values.
 */
export const createConfig = <ThrowOnError extends boolean = false>(options: Options<CreateConfigData, ThrowOnError>): RequestResult<CreateConfigResponses, CreateConfigErrors, ThrowOnError> => (options.client ?? client).post<CreateConfigResponses, CreateConfigErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/configs',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a dynamic config
 *
 * Soft-deletes the config and rebuilds the project's flags KV blob.
 *
 * **Use case:** Tear down a config after its consumers have stopped reading it.
 */
export const deleteConfig = <ThrowOnError extends boolean = false>(options: Options<DeleteConfigData, ThrowOnError>): RequestResult<DeleteConfigResponses, DeleteConfigErrors, ThrowOnError> => (options.client ?? client).delete<DeleteConfigResponses, DeleteConfigErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/configs/{id}',
    ...options
});

/**
 * Get one config
 *
 * Returns config metadata plus the latest published values per env and any active draft values. Use this to fetch the JSON the editor renders.
 *
 * **Use case:** Fetch one config's current published values and any in-flight drafts.
 */
export const getConfig = <ThrowOnError extends boolean = false>(options: Options<GetConfigData, ThrowOnError>): RequestResult<GetConfigResponses, GetConfigErrors, ThrowOnError> => (options.client ?? client).get<GetConfigResponses, GetConfigErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/configs/{id}',
    ...options
});

/**
 * Update a dynamic config
 *
 * Partial update. When `value` is supplied it is **republished on every env** (new version per env). When `schema` is supplied it replaces the current schema; every existing value is re-validated.
 *
 * For env-scoped edits, use the draft/publish flow (`PUT /{id}/drafts` then `POST /{id}/publish`) instead.
 *
 * **Use cases**
 *
 * - **Republish flat value** — `{ "value": {…} }` sets the same value on every env.
 * - **Schema migration** — `{ "schema": {…} }` replaces the schema; existing values are re-validated.
 * - **Env-scoped edits** — use `PUT /{id}/drafts` + `POST /{id}/publish` instead of PATCH.
 */
export const updateConfig = <ThrowOnError extends boolean = false>(options: Options<UpdateConfigData, ThrowOnError>): RequestResult<UpdateConfigResponses, UpdateConfigErrors, ThrowOnError> => (options.client ?? client).patch<UpdateConfigResponses, UpdateConfigErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/configs/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Discard a draft
 *
 * Drops the in-flight draft on one env. Published values are unaffected.
 *
 * **Use case:** Abandon an in-progress draft after deciding not to ship it.
 */
export const discardConfigDraft = <ThrowOnError extends boolean = false>(options: Options<DiscardConfigDraftData, ThrowOnError>): RequestResult<DiscardConfigDraftResponses, DiscardConfigDraftErrors, ThrowOnError> => (options.client ?? client).delete<DiscardConfigDraftResponses, DiscardConfigDraftErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/configs/{id}/drafts',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Save a draft value
 *
 * Stages a value for one env without publishing. The draft is validated against the config's current schema and stored alongside the `baseVersion` it was forked from.
 *
 * Saving over an existing draft overwrites it. Use `POST /{id}/publish` to promote it to a new published version.
 *
 * **Use case:** Iterate on a config value on dev without affecting prod — preview in staging, then publish.
 */
export const saveConfigDraft = <ThrowOnError extends boolean = false>(options: Options<SaveConfigDraftData, ThrowOnError>): RequestResult<SaveConfigDraftResponses, SaveConfigDraftErrors, ThrowOnError> => (options.client ?? client).put<SaveConfigDraftResponses, SaveConfigDraftErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/configs/{id}/drafts',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Publish a draft
 *
 * Promotes the staged draft on one env to a new published version. The draft must still validate against the current schema.
 *
 * Returns `404` if there is no draft for the given env.
 *
 * **Use case:** Ship a staged change once you've validated it on a lower env.
 */
export const publishConfigDraft = <ThrowOnError extends boolean = false>(options: Options<PublishConfigDraftData, ThrowOnError>): RequestResult<PublishConfigDraftResponses, PublishConfigDraftErrors, ThrowOnError> => (options.client ?? client).post<PublishConfigDraftResponses, PublishConfigDraftErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/configs/{id}/publish',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List config activity
 *
 * Returns recent audit rows for one config (create, update, draft.save, publish, delete) ordered newest first. Use the `limit` query parameter to cap the result (1–100, default 20).
 *
 * **Use case:** Render the activity panel in the config editor or drive a slack notification on publish events.
 */
export const listConfigActivity = <ThrowOnError extends boolean = false>(options: Options<ListConfigActivityData, ThrowOnError>): RequestResult<ListConfigActivityResponses, ListConfigActivityErrors, ThrowOnError> => (options.client ?? client).get<ListConfigActivityResponses, ListConfigActivityErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/configs/{id}/activity',
    ...options
});

/**
 * List killswitches
 *
 * Returns a single page of killswitches ordered by `updated_at desc, id desc`. Each row includes the latest published `value`/`switches`/`version` per env.
 *
 * **Use case:** Snapshot every killswitch in the project — e.g. to render an incident-response runbook listing every kill and its current trip state.
 */
export const listKillswitches = <ThrowOnError extends boolean = false>(options?: Options<ListKillswitchesData, ThrowOnError>): RequestResult<ListKillswitchesResponses, ListKillswitchesErrors, ThrowOnError> => (options?.client ?? client).get<ListKillswitchesResponses, ListKillswitchesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/killswitches',
    ...options
});

/**
 * Create a killswitch
 *
 * Creates a new killswitch with `value` (default `false`) applied to **every** env at version 1.
 *
 * Returns `409` if `name` already exists in the project.
 *
 * **Use cases**
 *
 * - **Untripped create** — `{ "name": "payments.checkout" }`. Provision the kill ahead of an incident.
 * - **Pre-tripped** — `{ "value": true }` to ship the killswitch already engaged.
 * - **With switches** — seed `switches` to carve out per-region/per-tenant kills from day one.
 */
export const createKillswitch = <ThrowOnError extends boolean = false>(options: Options<CreateKillswitchData, ThrowOnError>): RequestResult<CreateKillswitchResponses, CreateKillswitchErrors, ThrowOnError> => (options.client ?? client).post<CreateKillswitchResponses, CreateKillswitchErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/killswitches',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a killswitch
 *
 * Soft-deletes the killswitch and rebuilds the project's flags KV blob so SDKs stop seeing it.
 *
 * **Use case:** Tear down a killswitch after the feature it protected has been removed.
 */
export const deleteKillswitch = <ThrowOnError extends boolean = false>(options: Options<DeleteKillswitchData, ThrowOnError>): RequestResult<DeleteKillswitchResponses, DeleteKillswitchErrors, ThrowOnError> => (options.client ?? client).delete<DeleteKillswitchResponses, DeleteKillswitchErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/killswitches/{id}',
    ...options
});

/**
 * Get one killswitch
 *
 * Returns the killswitch metadata plus the latest published `value`/`switches`/`version` per env.
 *
 * **Use case:** Fetch the current state of one killswitch — e.g. to verify a trip propagated before declaring an incident mitigated.
 */
export const getKillswitch = <ThrowOnError extends boolean = false>(options: Options<GetKillswitchData, ThrowOnError>): RequestResult<GetKillswitchResponses, GetKillswitchErrors, ThrowOnError> => (options.client ?? client).get<GetKillswitchResponses, GetKillswitchErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/killswitches/{id}',
    ...options
});

/**
 * Update a killswitch
 *
 * Partial update applied to **every** env. Setting `value`/`switches` publishes a new version per env. Description-only patches don't bump versions.
 *
 * To change a single switch on a single env, use `PUT /{id}/switch` instead.
 *
 * **Use cases**
 *
 * - **Trip everywhere** — `{ "value": true }`. Kills the feature across dev/stage/prod in one call.
 * - **Untrip everywhere** — `{ "value": false }`.
 * - **Replace switches** — send the full new map; per-key edits use `PUT /{id}/switch`.
 * - **Update description** — metadata-only patches don't bump versions.
 */
export const updateKillswitch = <ThrowOnError extends boolean = false>(options: Options<UpdateKillswitchData, ThrowOnError>): RequestResult<UpdateKillswitchResponses, UpdateKillswitchErrors, ThrowOnError> => (options.client ?? client).patch<UpdateKillswitchResponses, UpdateKillswitchErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/killswitches/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Remove one switch entry
 *
 * Removes a single `switchKey` from the `switches` map on a single `env`. Publishes a new version on that env.
 *
 * Returns `{ removed: false }` if the entry didn't exist (idempotent no-op).
 *
 * **Use case:** Clean up a per-region override after the incident is resolved so the flat `value` governs again.
 */
export const unsetKillswitchSwitch = <ThrowOnError extends boolean = false>(options: Options<UnsetKillswitchSwitchData, ThrowOnError>): RequestResult<UnsetKillswitchSwitchResponses, UnsetKillswitchSwitchErrors, ThrowOnError> => (options.client ?? client).delete<UnsetKillswitchSwitchResponses, UnsetKillswitchSwitchErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/killswitches/{id}/switch',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Set one switch entry
 *
 * Sets or updates a single `switchKey` on a single `env`. Publishes one new version on that env only — other envs untouched.
 *
 * Use this for surgical per-env, per-key flips during incident response (e.g. trip `eu_region` on prod without touching the flat `value` or other envs).
 *
 * **Use cases**
 *
 * - **Trip a region** — `{ "env": "prod", "switchKey": "eu_region", "value": true }`.
 * - **Untrip without removing** — same payload with `value: false`. To remove the entry entirely use `DELETE /{id}/switch`.
 */
export const setKillswitchSwitch = <ThrowOnError extends boolean = false>(options: Options<SetKillswitchSwitchData, ThrowOnError>): RequestResult<SetKillswitchSwitchResponses, SetKillswitchSwitchErrors, ThrowOnError> => (options.client ?? client).put<SetKillswitchSwitchResponses, SetKillswitchSwitchErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/killswitches/{id}/switch',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List universes
 *
 * Returns a single page of universes ordered by `created_at desc, id desc`. The universes table has no `updated_at`, so this list is keyed on creation time.
 *
 * **Use case:** Snapshot every universe in the project — for example to audit which `unit_type` and `holdout_range` are in use before launching a new experiment.
 */
export const listUniverses = <ThrowOnError extends boolean = false>(options?: Options<ListUniversesData, ThrowOnError>): RequestResult<ListUniversesResponses, ListUniversesErrors, ThrowOnError> => (options?.client ?? client).get<ListUniversesResponses, ListUniversesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/universes',
    ...options
});

/**
 * Create a universe
 *
 * Creates a new universe. Only `name` is required — `unit_type` defaults to `user_id` and `holdout_range` defaults to `null` (no holdout).
 *
 * Returns `409` if `name` already exists in the project. Returns `403` if you supply `holdout_range` on a plan below Pro.
 *
 * **Use cases**
 *
 * - **Default universe** — `{ "name": "primary_users" }`. Per-user randomisation, no holdout.
 * - **Reserved holdout** — supply `holdout_range` to carve out a measurement slice excluded from all experiments.
 * - **Account-level** — `unit_type: 'account_id'` so multi-seat accounts see one consistent variant.
 */
export const createUniverse = <ThrowOnError extends boolean = false>(options: Options<CreateUniverseData, ThrowOnError>): RequestResult<CreateUniverseResponses, CreateUniverseErrors, ThrowOnError> => (options.client ?? client).post<CreateUniverseResponses, CreateUniverseErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/universes',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a universe
 *
 * Soft-deletes the universe. Returns `409` if any non-archived experiment still references it — archive those experiments first.
 *
 * **Use case:** Tear down a universe after every experiment that used it has been archived.
 */
export const deleteUniverse = <ThrowOnError extends boolean = false>(options: Options<DeleteUniverseData, ThrowOnError>): RequestResult<DeleteUniverseResponses, DeleteUniverseErrors, ThrowOnError> => (options.client ?? client).delete<DeleteUniverseResponses, DeleteUniverseErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/universes/{id}',
    ...options
});

/**
 * Update a universe
 *
 * Partial update. Only `holdout_range` is mutable — `name` and `unit_type` are immutable after create.
 *
 * Pass `"holdout_range": null` to remove an existing holdout.
 *
 * **Use cases**
 *
 * - **Adjust holdout** — change the reserved measurement slice without recreating experiments.
 * - **Remove holdout** — `{ "holdout_range": null }`.
 */
export const updateUniverse = <ThrowOnError extends boolean = false>(options: Options<UpdateUniverseData, ThrowOnError>): RequestResult<UpdateUniverseResponses, UpdateUniverseErrors, ThrowOnError> => (options.client ?? client).patch<UpdateUniverseResponses, UpdateUniverseErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/universes/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List targeting attributes
 *
 * Returns every auto-inferred targeting attribute in the project — the `name` and (when known) the value `type` — for building gate/experiment targeting rules.
 *
 * **Use case:** Discover which user-context keys are available before authoring a targeting rule, instead of guessing attribute names.
 */
export const listAttributes = <ThrowOnError extends boolean = false>(options?: Options<ListAttributesData, ThrowOnError>): RequestResult<ListAttributesResponses, ListAttributesErrors, ThrowOnError> => (options?.client ?? client).get<ListAttributesResponses, ListAttributesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/attributes',
    ...options
});

/**
 * List metrics
 *
 * Returns every metric in the project (not paginated) — name, folder, source event, aggregation, and the rendered query.
 *
 * **Use case:** Audit every metric defined in the project — for example to find the metric id to attach as an experiment's success metric.
 */
export const listMetrics = <ThrowOnError extends boolean = false>(options?: Options<ListMetricsData, ThrowOnError>): RequestResult<ListMetricsResponses, ListMetricsErrors, ThrowOnError> => (options?.client ?? client).get<ListMetricsResponses, ListMetricsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/metrics',
    ...options
});

/**
 * Create a metric
 *
 * Creates an event-backed metric. Pass the query as the DSL string (`query`) **or**
 * the typed IR (`query_ir`) — exactly one. `event_name` must equal the event the
 * query references.
 *
 * Returns `409` if a metric with the same `name` already exists, and `422` if the
 * query is invalid or references an unregistered event / label.
 *
 * **Use cases**
 *
 * - **Track an event** — `count_users(<event>)` for unique-user counts.
 * - **Sum a value** — `sum(<event>, <label>)` for revenue / quantity metrics.
 * - **Experiment success metric** — create the metric, then attach its id to an experiment.
 */
export const createMetric = <ThrowOnError extends boolean = false>(options: Options<CreateMetricData, ThrowOnError>): RequestResult<CreateMetricResponses, CreateMetricErrors, ThrowOnError> => (options.client ?? client).post<CreateMetricResponses, CreateMetricErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/metrics',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Archive a metric
 *
 * Soft-deletes (archives) the metric. Returns `409` if it is attached to a running experiment — stop those experiments first.
 *
 * **Use case:** Retire a metric once no running experiment depends on it (the user-facing verb is `archive`).
 */
export const deleteMetric = <ThrowOnError extends boolean = false>(options: Options<DeleteMetricData, ThrowOnError>): RequestResult<DeleteMetricResponses, DeleteMetricErrors, ThrowOnError> => (options.client ?? client).delete<DeleteMetricResponses, DeleteMetricErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/metrics/{id}',
    ...options
});

/**
 * Get a metric
 *
 * Fetch one metric by its id or name, including the rendered DSL query and the typed IR.
 *
 * **Use case:** Inspect a single metric's full definition before reusing it in an experiment or alert rule.
 */
export const getMetric = <ThrowOnError extends boolean = false>(options: Options<GetMetricData, ThrowOnError>): RequestResult<GetMetricResponses, GetMetricErrors, ThrowOnError> => (options.client ?? client).get<GetMetricResponses, GetMetricErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/metrics/{id}',
    ...options
});

/**
 * List events
 *
 * Returns every catalogued event in the project, including pending auto-discovered names. Pass `?pending=true` to return only the unapproved queue.
 *
 * **Use case:** Snapshot the event catalog — for example to review the `pending` auto-discovery queue (`?pending=true`) before approving names, or to confirm which events your metrics can reference.
 */
export const listEvents = <ThrowOnError extends boolean = false>(options?: Options<ListEventsData, ThrowOnError>): RequestResult<ListEventsResponses, ListEventsErrors, ThrowOnError> => (options?.client ?? client).get<ListEventsResponses, ListEventsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/events',
    ...options
});

/**
 * Register an event
 *
 * Registers a new event name and (optionally) its typed properties. Only `name` is required.
 *
 * If the name matches an existing **pending** (auto-discovered) row, this approves that row instead of returning a conflict. Otherwise an already-registered name returns `409`.
 *
 * **Use cases**
 *
 * - **Register a known event** — `{ "name": "checkout_completed" }` so metrics can reference it.
 * - **Declare typed properties** — supply `properties` to document the event's payload shape.
 */
export const createEvent = <ThrowOnError extends boolean = false>(options: Options<CreateEventData, ThrowOnError>): RequestResult<CreateEventResponses, CreateEventErrors, ThrowOnError> => (options.client ?? client).post<CreateEventResponses, CreateEventErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/events',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Archive an event
 *
 * Soft-deletes (archives) the event. Returns `409` if any metric still references it — delete those metrics first.
 *
 * **Use case:** Retire an event from the catalog once no metric depends on it.
 */
export const deleteEvent = <ThrowOnError extends boolean = false>(options: Options<DeleteEventData, ThrowOnError>): RequestResult<DeleteEventResponses, DeleteEventErrors, ThrowOnError> => (options.client ?? client).delete<DeleteEventResponses, DeleteEventErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/events/{id}',
    ...options
});

/**
 * Get an event
 *
 * Returns one event's full detail. Resolves by exact id, unique id-prefix, or exact (unique) name.
 *
 * **Use case:** Inspect one event's declared properties and pending state by id or name.
 */
export const getEvent = <ThrowOnError extends boolean = false>(options: Options<GetEventData, ThrowOnError>): RequestResult<GetEventResponses, GetEventErrors, ThrowOnError> => (options.client ?? client).get<GetEventResponses, GetEventErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/events/{id}',
    ...options
});

/**
 * Update an event
 *
 * Partial update of an event's folder, description, or properties. `name` is immutable.
 *
 * `properties` replaces the full set (no merge) — omit it to leave properties unchanged.
 *
 * **Use case:** Refile an event, update its description, or redeclare its typed properties.
 */
export const updateEvent = <ThrowOnError extends boolean = false>(options: Options<UpdateEventData, ThrowOnError>): RequestResult<UpdateEventResponses, UpdateEventErrors, ThrowOnError> => (options.client ?? client).patch<UpdateEventResponses, UpdateEventErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/events/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Approve a pending event
 *
 * Promotes a pending (auto-discovered) event to usable so metrics can query it (`pending` → `0`).
 *
 * You may optionally declare the event's folder, description, or properties in the same call — the body is the same shape as update, and may be empty.
 *
 * **Use case:** Clear an auto-discovered event out of the pending queue so metrics defined on it start resolving.
 */
export const approveEvent = <ThrowOnError extends boolean = false>(options: Options<ApproveEventData, ThrowOnError>): RequestResult<ApproveEventResponses, ApproveEventErrors, ThrowOnError> => (options.client ?? client).post<ApproveEventResponses, ApproveEventErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/events/{id}/approve',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List the operational queue
 *
 * Returns the unified feedback queue (bugs, feature requests, errors, alerts), newest first. Filter by `type` and/or `status`, and cap with `limit`.
 *
 * **Use case:** Pull the open queue to triage — e.g. every `bug` still `open` — before working items down one by one.
 */
export const listOpsItems = <ThrowOnError extends boolean = false>(options?: Options<ListOpsItemsData, ThrowOnError>): RequestResult<ListOpsItemsResponses, ListOpsItemsErrors, ThrowOnError> => (options?.client ?? client).get<ListOpsItemsResponses, ListOpsItemsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/feedback',
    ...options
});

/**
 * Get one queue item
 *
 * Fetch a single queue item by its per-project `number` or full id.
 *
 * **Use case:** Inspect one item's full detail before updating its status or linking a PR.
 */
export const getOpsItem = <ThrowOnError extends boolean = false>(options: Options<GetOpsItemData, ThrowOnError>): RequestResult<GetOpsItemResponses, GetOpsItemErrors, ThrowOnError> => (options.client ?? client).get<GetOpsItemResponses, GetOpsItemErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/feedback/{handle}',
    ...options
});

/**
 * Update a queue item
 *
 * Update a queue item's `status` and/or `priority`. Other fields are immutable.
 *
 * **Use case:** Move an item through its lifecycle (triage → in_progress → resolved) as you work it.
 */
export const updateOpsItem = <ThrowOnError extends boolean = false>(options: Options<UpdateOpsItemData, ThrowOnError>): RequestResult<UpdateOpsItemResponses, UpdateOpsItemErrors, ThrowOnError> => (options.client ?? client).patch<UpdateOpsItemResponses, UpdateOpsItemErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/feedback/{handle}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * File a bug report
 *
 * Files a bug into the queue and fires the project's connectors (GitHub issue / Slack). Returns the new id and per-project number.
 *
 * **Use case:** Report a defect programmatically so it lands in the same queue the dashboard shows.
 */
export const createBug = <ThrowOnError extends boolean = false>(options: Options<CreateBugData, ThrowOnError>): RequestResult<CreateBugResponses, CreateBugErrors, ThrowOnError> => (options.client ?? client).post<CreateBugResponses, CreateBugErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/bugs',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * File a feature request
 *
 * Files a feature request into the queue and fires the project's connectors. Returns the new id and per-project number.
 *
 * **Use case:** Capture a feature ask from an integration or a user-facing widget.
 */
export const createFeatureRequest = <ThrowOnError extends boolean = false>(options: Options<CreateFeatureRequestData, ThrowOnError>): RequestResult<CreateFeatureRequestResponses, CreateFeatureRequestErrors, ThrowOnError> => (options.client ?? client).post<CreateFeatureRequestResponses, CreateFeatureRequestErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/feature-requests',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Link a fixing PR
 *
 * Record the pull request that fixes a queue item (and clears the link with `prNumber: null`).
 *
 * **Use case:** Tie the fixing PR to the item so closing the PR can flip it to ready_for_qa.
 */
export const linkPrToOpsItem = <ThrowOnError extends boolean = false>(options: Options<LinkPrToOpsItemData, ThrowOnError>): RequestResult<LinkPrToOpsItemResponses, LinkPrToOpsItemErrors, ThrowOnError> => (options.client ?? client).post<LinkPrToOpsItemResponses, LinkPrToOpsItemErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/feedback/{handle}/link-pr',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Raise an attention notification
 *
 * Raise a 'needs your attention' bell notification. Create-only and idempotent on `dedupeKey`.
 *
 * **Use case:** Escalate something that needs a human, deduped so repeats don't spam the bell.
 */
export const notifyOps = <ThrowOnError extends boolean = false>(options: Options<NotifyOpsData, ThrowOnError>): RequestResult<NotifyOpsResponses, NotifyOpsErrors, ThrowOnError> => (options.client ?? client).post<NotifyOpsResponses, NotifyOpsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/notifications',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Slack channels
 *
 * List the project's connected Slack channels — used to resolve an alert rule's notification target.
 *
 * **Use case:** Populate a channel picker, or validate an alert rule's `--slack-channel` before saving.
 */
export const listSlackChannels = <ThrowOnError extends boolean = false>(options?: Options<ListSlackChannelsData, ThrowOnError>): RequestResult<ListSlackChannelsResponses, ListSlackChannelsErrors, ThrowOnError> => (options?.client ?? client).get<ListSlackChannelsResponses, ListSlackChannelsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/slack/channels',
    ...options
});

/**
 * List alert rules
 *
 * Returns every alert rule in the project (not paginated). Each rule carries its bound `metricId`, the denormalised `metricName` (or `null` if the metric was removed), the comparator/threshold/window, severity, enabled flag, and delivery target.
 *
 * **Use case:** Audit which metrics have alerting configured — for example to confirm an on-call threshold is set before a launch.
 */
export const listAlertRules = <ThrowOnError extends boolean = false>(options?: Options<ListAlertRulesData, ThrowOnError>): RequestResult<ListAlertRulesResponses, ListAlertRulesErrors, ThrowOnError> => (options?.client ?? client).get<ListAlertRulesResponses, ListAlertRulesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/alert-rules',
    ...options
});

/**
 * Create an alert rule
 *
 * Creates a metric-threshold alert rule. `name`, `metricId`, `comparator`, and `threshold` are required; `windowHours` defaults to `24`, `severity` to `warn`, and `enabled` to `true`.
 *
 * Returns `404` if `metricId` does not resolve, and `400` for a metric with no scalar form over a window (e.g. retention metrics) — the cron can't evaluate those.
 *
 * **Use cases**
 *
 * - **Threshold alert** — warn when an error/latency metric crosses a value over a rolling window.
 * - **Routed alert** — set `notify` to page a specific Slack channel or on-call email instead of the project default.
 */
export const createAlertRule = <ThrowOnError extends boolean = false>(options: Options<CreateAlertRuleData, ThrowOnError>): RequestResult<CreateAlertRuleResponses, CreateAlertRuleErrors, ThrowOnError> => (options.client ?? client).post<CreateAlertRuleResponses, CreateAlertRuleErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/alert-rules',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete an alert rule
 *
 * Deletes the alert rule. The cron stops evaluating it immediately. Use this (then create a new rule) to repoint alerting at a different metric, since `metricId` is immutable.
 *
 * **Use case:** Remove an alert rule that is no longer needed, or as the first half of repointing a rule at a different metric.
 */
export const deleteAlertRule = <ThrowOnError extends boolean = false>(options: Options<DeleteAlertRuleData, ThrowOnError>): RequestResult<DeleteAlertRuleResponses, DeleteAlertRuleErrors, ThrowOnError> => (options.client ?? client).delete<DeleteAlertRuleResponses, DeleteAlertRuleErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/alert-rules/{id}',
    ...options
});

/**
 * Update an alert rule
 *
 * Partial update of a rule's tunable knobs. `metricId` is immutable — it is rejected by the schema (the metric also pins the aggregation), so delete + recreate to repoint a rule.
 *
 * Pass `"notify": null` to revert the rule's delivery target back to the project default.
 *
 * **Use cases**
 *
 * - **Tune sensitivity** — change `threshold`/`comparator`/`windowHours` as the metric's baseline shifts.
 * - **Pause without losing config** — `{ "enabled": false }` instead of deleting the rule.
 */
export const updateAlertRule = <ThrowOnError extends boolean = false>(options: Options<UpdateAlertRuleData, ThrowOnError>): RequestResult<UpdateAlertRuleResponses, UpdateAlertRuleErrors, ThrowOnError> => (options.client ?? client).patch<UpdateAlertRuleResponses, UpdateAlertRuleErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/alert-rules/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Show the current project
 *
 * Returns the project the caller's auth header resolves to — plan, status, billing, and which modules are enabled. The server reads the project from the credential, so there is no id parameter. Powers `whoami`.
 *
 * **Use case:** Resolve who you are — the project, plan, and enabled modules tied to the current credential — without passing an id. Backs a registry-driven `whoami`.
 */
export const getCurrentProject = <ThrowOnError extends boolean = false>(options?: Options<GetCurrentProjectData, ThrowOnError>): RequestResult<GetCurrentProjectResponses, GetCurrentProjectErrors, ThrowOnError> => (options?.client ?? client).get<GetCurrentProjectResponses, GetCurrentProjectErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/projects/current',
    ...options
});

/**
 * Find-or-create a project by domain
 *
 * Find-or-creates a project keyed by `(owner_email, domain)` under the session's owner, and returns it. Idempotent: a second call with the same domain returns the existing project with `created: false`.
 *
 * Only `domain` is required — `name` defaults to the domain on first create. Recording the result in a local `.shipeasy` binding is a consumer side-effect; this endpoint never performs it.
 *
 * **Use cases**
 *
 * - **Install flow** — provision a per-app project without a trip to the dashboard. Run it on every install; the idempotent key means a re-run returns the existing project rather than duplicating it.
 * - **Name explicitly** — pass `name` to label the project distinctly from its `domain`.
 */
export const upsertProject = <ThrowOnError extends boolean = false>(options: Options<UpsertProjectData, ThrowOnError>): RequestResult<UpsertProjectResponses, UpsertProjectErrors, ThrowOnError> => (options.client ?? client).post<UpsertProjectResponses, UpsertProjectErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/projects/upsert',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List i18n profiles
 *
 * Returns every locale profile in the project (e.g. `en:prod`, `fr:prod`).
 *
 * **Use case:** Discover which locale profiles exist before pushing keys or publishing a chunk.
 */
export const listI18nProfiles = <ThrowOnError extends boolean = false>(options?: Options<ListI18nProfilesData, ThrowOnError>): RequestResult<ListI18nProfilesResponses, ListI18nProfilesErrors, ThrowOnError> => (options?.client ?? client).get<ListI18nProfilesResponses, ListI18nProfilesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/i18n/profiles',
    ...options
});

/**
 * Create an i18n profile
 *
 * Create a locale profile. `name` is the stable handle (e.g. `fr:prod`).
 *
 * **Use case:** Stand up a new locale before seeding its keys.
 */
export const createI18nProfile = <ThrowOnError extends boolean = false>(options: Options<CreateI18nProfileData, ThrowOnError>): RequestResult<CreateI18nProfileResponses, CreateI18nProfileErrors, ThrowOnError> => (options.client ?? client).post<CreateI18nProfileResponses, CreateI18nProfileErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/i18n/profiles',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List i18n keys
 *
 * List keys for a profile, optionally filtered to a name `prefix`.
 *
 * **Use case:** Read the current keys (and values) for a profile — e.g. to diff before a push.
 */
export const listI18nKeys = <ThrowOnError extends boolean = false>(options?: Options<ListI18nKeysData, ThrowOnError>): RequestResult<ListI18nKeysResponses, ListI18nKeysErrors, ThrowOnError> => (options?.client ?? client).get<ListI18nKeysResponses, ListI18nKeysErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/i18n/keys',
    ...options
});

/**
 * Push new i18n keys (insert-only)
 *
 * Add NEW keys to a profile. Insert-only — existing keys are left untouched (overwrite one with `updateI18nKey`).
 *
 * **Use case:** Seed newly-extracted keys without clobbering translations already in the profile.
 */
export const pushI18nKeys = <ThrowOnError extends boolean = false>(options: Options<PushI18nKeysData, ThrowOnError>): RequestResult<PushI18nKeysResponses, PushI18nKeysErrors, ThrowOnError> => (options.client ?? client).post<PushI18nKeysResponses, PushI18nKeysErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/i18n/keys',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Update one i18n key
 *
 * Overwrite a single existing key's value — the only overwrite path.
 *
 * **Use case:** Correct or re-translate a single string in place.
 */
export const updateI18nKey = <ThrowOnError extends boolean = false>(options: Options<UpdateI18nKeyData, ThrowOnError>): RequestResult<UpdateI18nKeyResponses, UpdateI18nKeyErrors, ThrowOnError> => (options.client ?? client).put<UpdateI18nKeyResponses, UpdateI18nKeyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/i18n/keys/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List translation drafts
 *
 * List staged translation drafts awaiting review/publish.
 *
 * **Use case:** Review machine-translation drafts before publishing them to a locale.
 */
export const listI18nDrafts = <ThrowOnError extends boolean = false>(options?: Options<ListI18nDraftsData, ThrowOnError>): RequestResult<ListI18nDraftsResponses, ListI18nDraftsErrors, ThrowOnError> => (options?.client ?? client).get<ListI18nDraftsResponses, ListI18nDraftsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/i18n/drafts',
    ...options
});

/**
 * Publish a profile chunk
 *
 * Publish a profile's chunk to the CDN (rebuild KV manifest + purge). Defaults to the `default` chunk.
 *
 * **Use case:** Ship the latest translations live after pushing/updating keys.
 */
export const publishI18nProfile = <ThrowOnError extends boolean = false>(options: Options<PublishI18nProfileData, ThrowOnError>): RequestResult<PublishI18nProfileResponses, PublishI18nProfileErrors, ThrowOnError> => (options.client ?? client).post<PublishI18nProfileResponses, PublishI18nProfileErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/admin/i18n/profiles/{profileId}/publish',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});
