// This file is auto-generated by @hey-api/openapi-ts

export type ClientOptions = {
    baseUrl: 'https://shipeasy.ai' | 'http://localhost:3000' | (string & {});
};

export type ListGatesResponse = {
    data: Array<{
        /**
         * Stable opaque gate id (`gat_…`).
         */
        id: string;
        /**
         * Stable gate key used by SDKs (`Shipeasy.checkGate(user, '<name>')`). Single segment or `folder.name`. Lowercase letters, digits, `_` or `-`; max 128 chars. Immutable after create — rename = delete + recreate.
         */
        name: string;
        /**
         * Master switch. Returned as `1`/`0` from D1; treat truthy as enabled.
         */
        enabled: boolean | number;
        /**
         * Current rollout in basis points (0–10000).
         */
        rolloutPct: number;
        rules?: Array<{
            /**
             * Attribute key on the evaluation context (e.g. `country`, `plan`, `email`). Matched case-sensitively against `Shipeasy.checkGate(user, name)` input.
             */
            attr: string;
            /**
             * Comparison operator. Equality: `eq`/`neq`. Set membership: `in`/`not_in` (value is an array). Numeric order: `gt`/`gte`/`lt`/`lte`. Semver order: `semver_gt`/`semver_gte`/`semver_lt`/`semver_lte` (value is a dotted version like `4.2.0`). String: `contains` (substring), `regex` (JS-flavour pattern, value is the pattern string). Cross-resource references (evaluated only on the worker hot path, where both KV blobs load): `gate_pass` (`value` is another gate's name — true when that gate passes for the same caller); `exp_in` (`attr` is a running experiment's name, `value` is a group name, `"$holdout"`, or `"$any"` — true when the caller is in that variant / the universe holdout / any variant).
             */
            op: 'eq' | 'neq' | 'in' | 'not_in' | 'gt' | 'gte' | 'lt' | 'lte' | 'contains' | 'regex' | 'semver_gt' | 'semver_gte' | 'semver_lt' | 'semver_lte' | 'gate_pass' | 'exp_in';
            /**
             * Operand. Shape depends on `op`: scalar for `eq/neq/gt/gte/lt/lte/contains/semver_*`; array of scalars for `in/not_in`; string pattern for `regex`; referenced gate name for `gate_pass`; group name / `"$holdout"` / `"$any"` for `exp_in`.
             */
            value: unknown;
        }>;
        /**
         * Hash salt used for percentage bucketing.
         */
        salt?: string;
        title?: string | null;
        description?: string | null;
        folder?: string | null;
        /**
         * Group label (note: `groupName` in response, `group` in request).
         */
        groupName?: string | null;
        /**
         * Owner email (note: `ownerEmail` in response, `owner_email` in request).
         */
        ownerEmail?: string | null;
        stack?: Array<{
            /**
             * Client-supplied stable id for the stack entry (used for React keys, audit diffs, and as the default per-condition rollout salt).
             */
            id: string;
            /**
             * Discriminator. `condition` = rule-based predicate sub-gate.
             */
            type: 'condition';
            /**
             * Human label shown in the dashboard.
             */
            name?: string;
            /**
             * Id of the project template this entry was seeded from, or `null` if hand-authored.
             */
            fromTemplate?: string | null;
            /**
             * Combinator across `rules`. `all` = AND (default), `any` = OR.
             */
            pass?: 'all' | 'any';
            /**
             * Predicates evaluated under `pass`. Empty array = match nothing.
             */
            rules?: Array<{
                /**
                 * Attribute key on the evaluation context (e.g. `country`, `plan`, `email`). Matched case-sensitively against `Shipeasy.checkGate(user, name)` input.
                 */
                attr: string;
                /**
                 * Comparison operator. Equality: `eq`/`neq`. Set membership: `in`/`not_in` (value is an array). Numeric order: `gt`/`gte`/`lt`/`lte`. Semver order: `semver_gt`/`semver_gte`/`semver_lt`/`semver_lte` (value is a dotted version like `4.2.0`). String: `contains` (substring), `regex` (JS-flavour pattern, value is the pattern string). Cross-resource references (evaluated only on the worker hot path, where both KV blobs load): `gate_pass` (`value` is another gate's name — true when that gate passes for the same caller); `exp_in` (`attr` is a running experiment's name, `value` is a group name, `"$holdout"`, or `"$any"` — true when the caller is in that variant / the universe holdout / any variant).
                 */
                op: 'eq' | 'neq' | 'in' | 'not_in' | 'gt' | 'gte' | 'lt' | 'lte' | 'contains' | 'regex' | 'semver_gt' | 'semver_gte' | 'semver_lt' | 'semver_lte' | 'gate_pass' | 'exp_in';
                /**
                 * Operand. Shape depends on `op`: scalar for `eq/neq/gt/gte/lt/lte/contains/semver_*`; array of scalars for `in/not_in`; string pattern for `regex`; referenced gate name for `gate_pass`; group name / `"$holdout"` / `"$any"` for `exp_in`.
                 */
                value: unknown;
            }>;
            /**
             * Per-condition rollout in basis points. After the rules match, the caller is bucketed at this %; in-bucket → gate `true`, otherwise fall through to the next entry. Absent ⇒ 10000 (100%, every matching caller passes).
             */
            rolloutPct?: number;
            /**
             * Attribute used to hash the caller into the per-condition bucket. Default `userID`.
             */
            bucketBy?: string;
            /**
             * Per-condition hash salt. Defaults to the entry `id`, then the gate salt.
             */
            salt?: string;
            /**
             * Linear rollout ramp. The effective rollout % interpolates `from`→`to` over `[startAt, startAt+durationMs]`, clamped outside that window, evaluated against the wall-clock at eval time. Overrides the entry's static `rolloutPct` while present.
             */
            ramp?: {
                /**
                 * Start of the ramp in basis points (0–10000).
                 */
                from: number;
                /**
                 * End of the ramp in basis points (0–10000).
                 */
                to: number;
                /**
                 * Ramp start, epoch milliseconds (UTC).
                 */
                startAt: number;
                /**
                 * Ramp length in milliseconds.
                 */
                durationMs: number;
            };
            /**
             * If `true`, dashboard hides edit controls (e.g. the trailing public-floor entry).
             */
            locked?: boolean;
        } | {
            /**
             * Client-supplied stable id for the stack entry.
             */
            id: string;
            /**
             * Discriminator. `rollout` = percentage-bucket sub-gate.
             */
            type: 'rollout';
            /**
             * Human label shown in the dashboard.
             */
            name?: string;
            /**
             * Source template id, or `null`.
             */
            fromTemplate?: string | null;
            /**
             * Bucket size in basis points (0–10000 = 0%–100%). `5000` = 50%, `100` = 1%, allows sub-1% precision.
             */
            rolloutPct: number;
            /**
             * Attribute used to hash the caller into a bucket. Defaults to `user.userID`. Use e.g. `accountID` to bucket whole accounts together.
             */
            bucketBy?: string;
            /**
             * Per-entry hash salt. Defaults to the gate's salt. Set explicitly to keep two rollouts independent or correlated across gates.
             */
            salt?: string;
            /**
             * Linear rollout ramp. The effective rollout % interpolates `from`→`to` over `[startAt, startAt+durationMs]`, clamped outside that window, evaluated against the wall-clock at eval time. Overrides the entry's static `rolloutPct` while present.
             */
            ramp?: {
                /**
                 * Start of the ramp in basis points (0–10000).
                 */
                from: number;
                /**
                 * End of the ramp in basis points (0–10000).
                 */
                to: number;
                /**
                 * Ramp start, epoch milliseconds (UTC).
                 */
                startAt: number;
                /**
                 * Ramp length in milliseconds.
                 */
                durationMs: number;
            };
            /**
             * If `true`, dashboard hides edit controls.
             */
            locked?: boolean;
        }> | null;
        /**
         * ISO-8601 timestamp of last mutation.
         */
        updatedAt: string;
    }>;
    next_cursor: string | null;
};

/**
 * Stable, machine-readable error code. Mirrors the `ErrorCode` catalogue in `@shipeasy/core`. Each operation lists the subset it can return under `x-error-codes`.
 */
export type ErrorCode = 'BAD_REQUEST' | 'UNAUTHORIZED' | 'FORBIDDEN' | 'PLAN_REQUIRED' | 'NOT_FOUND' | 'ALREADY_EXISTS' | 'INVALID_TRANSITION' | 'IMMUTABLE_FIELD' | 'REFERENCED_IN_USE' | 'VALIDATION' | 'REFERENCED_NOT_FOUND' | 'GROUPS_WEIGHT_SUM' | 'EVENT_PENDING' | 'INTERNAL';

/**
 * Uniform error envelope returned by every admin endpoint on a non-2xx status. `error` is the human-readable message; `code` is the stable machine code (present whenever the failure maps to a catalogued `ErrorCode`); `detail` carries extra context (validation paths, conflicting field); `instructions` is optional actionable guidance for resolving the error.
 */
export type Error = {
    /**
     * Human-readable error message.
     */
    error: string;
    code?: ErrorCode;
    /**
     * Extra context (validation details, conflicting field, referenced row).
     */
    detail?: string;
    /**
     * Actionable guidance for resolving this error — what to check or change next.
     */
    instructions?: string;
};

/**
 * Optional folder name grouping items in the dashboard. Alphanumeric, `_` or `-` (no `/`). Part of the SDK lookup key (`<folder>/<name>`).
 */
export type Folder = string | null;

/**
 * Body for `POST /api/admin/gates`. At minimum supply `name`; everything else has sensible defaults.
 */
export type CreateGateRequest = {
    /**
     * Stable gate key used by SDKs (`Shipeasy.checkGate(user, '<name>')`). Single segment or `folder.name`. Lowercase letters, digits, `_` or `-`; max 128 chars. Immutable after create — rename = delete + recreate.
     */
    name: string;
    /**
     * Master switch. Defaults to `true`. Set `false` to create the gate disabled (evaluates to `false` regardless of rules/rollout); flip on via `POST /{id}/enable` or PATCH.
     */
    enabled?: boolean;
    /**
     * Initial rollout in **basis points** (0–10000 = 0%–100%) — `100` here means **1%**, not 100%. Use `rollout_percent` (0–100) below if you'd rather think in percent. Use `0` to create the gate dark and ramp via PATCH after deploy validation.
     */
    rollout_pct?: number;
    /**
     * Initial rollout as a **percentage** (0–100, fractional ok). Friendlier alias for `rollout_pct`; converted internally to basis points (e.g. `100` here = 10000 bp = 100%). If both `rollout_pct` and `rollout_percent` are set, `rollout_percent` wins.
     */
    rollout_percent?: number;
    /**
     * Targeting predicates. AND-combined. If non-empty, the gate returns `true` only for callers that satisfy every rule **and** fall under `rollout_pct`.
     */
    rules?: Array<{
        /**
         * Attribute key on the evaluation context (e.g. `country`, `plan`, `email`). Matched case-sensitively against `Shipeasy.checkGate(user, name)` input.
         */
        attr: string;
        /**
         * Comparison operator. Equality: `eq`/`neq`. Set membership: `in`/`not_in` (value is an array). Numeric order: `gt`/`gte`/`lt`/`lte`. Semver order: `semver_gt`/`semver_gte`/`semver_lt`/`semver_lte` (value is a dotted version like `4.2.0`). String: `contains` (substring), `regex` (JS-flavour pattern, value is the pattern string). Cross-resource references (evaluated only on the worker hot path, where both KV blobs load): `gate_pass` (`value` is another gate's name — true when that gate passes for the same caller); `exp_in` (`attr` is a running experiment's name, `value` is a group name, `"$holdout"`, or `"$any"` — true when the caller is in that variant / the universe holdout / any variant).
         */
        op: 'eq' | 'neq' | 'in' | 'not_in' | 'gt' | 'gte' | 'lt' | 'lte' | 'contains' | 'regex' | 'semver_gt' | 'semver_gte' | 'semver_lt' | 'semver_lte' | 'gate_pass' | 'exp_in';
        /**
         * Operand. Shape depends on `op`: scalar for `eq/neq/gt/gte/lt/lte/contains/semver_*`; array of scalars for `in/not_in`; string pattern for `regex`; referenced gate name for `gate_pass`; group name / `"$holdout"` / `"$any"` for `exp_in`.
         */
        value: unknown;
    }>;
    /**
     * Hash salt for percentage bucketing. Auto-generated if omitted. Provide explicitly to keep a gate's buckets stable across delete/recreate. **Immutable after create** — there is no PATCH for `salt` because changing it would re-bucket every caller.
     */
    salt?: string;
    /**
     * Optional gatekeeper stack. When provided, takes precedence over `rules` + `rollout_pct` at evaluation time. Omit (or pass `null`) for a flat gate.
     */
    stack?: Array<{
        /**
         * Client-supplied stable id for the stack entry (used for React keys, audit diffs, and as the default per-condition rollout salt).
         */
        id: string;
        /**
         * Discriminator. `condition` = rule-based predicate sub-gate.
         */
        type: 'condition';
        /**
         * Human label shown in the dashboard.
         */
        name?: string;
        /**
         * Id of the project template this entry was seeded from, or `null` if hand-authored.
         */
        fromTemplate?: string | null;
        /**
         * Combinator across `rules`. `all` = AND (default), `any` = OR.
         */
        pass?: 'all' | 'any';
        /**
         * Predicates evaluated under `pass`. Empty array = match nothing.
         */
        rules?: Array<{
            /**
             * Attribute key on the evaluation context (e.g. `country`, `plan`, `email`). Matched case-sensitively against `Shipeasy.checkGate(user, name)` input.
             */
            attr: string;
            /**
             * Comparison operator. Equality: `eq`/`neq`. Set membership: `in`/`not_in` (value is an array). Numeric order: `gt`/`gte`/`lt`/`lte`. Semver order: `semver_gt`/`semver_gte`/`semver_lt`/`semver_lte` (value is a dotted version like `4.2.0`). String: `contains` (substring), `regex` (JS-flavour pattern, value is the pattern string). Cross-resource references (evaluated only on the worker hot path, where both KV blobs load): `gate_pass` (`value` is another gate's name — true when that gate passes for the same caller); `exp_in` (`attr` is a running experiment's name, `value` is a group name, `"$holdout"`, or `"$any"` — true when the caller is in that variant / the universe holdout / any variant).
             */
            op: 'eq' | 'neq' | 'in' | 'not_in' | 'gt' | 'gte' | 'lt' | 'lte' | 'contains' | 'regex' | 'semver_gt' | 'semver_gte' | 'semver_lt' | 'semver_lte' | 'gate_pass' | 'exp_in';
            /**
             * Operand. Shape depends on `op`: scalar for `eq/neq/gt/gte/lt/lte/contains/semver_*`; array of scalars for `in/not_in`; string pattern for `regex`; referenced gate name for `gate_pass`; group name / `"$holdout"` / `"$any"` for `exp_in`.
             */
            value: unknown;
        }>;
        /**
         * Per-condition rollout in basis points. After the rules match, the caller is bucketed at this %; in-bucket → gate `true`, otherwise fall through to the next entry. Absent ⇒ 10000 (100%, every matching caller passes).
         */
        rolloutPct?: number;
        /**
         * Attribute used to hash the caller into the per-condition bucket. Default `userID`.
         */
        bucketBy?: string;
        /**
         * Per-condition hash salt. Defaults to the entry `id`, then the gate salt.
         */
        salt?: string;
        /**
         * Linear rollout ramp. The effective rollout % interpolates `from`→`to` over `[startAt, startAt+durationMs]`, clamped outside that window, evaluated against the wall-clock at eval time. Overrides the entry's static `rolloutPct` while present.
         */
        ramp?: {
            /**
             * Start of the ramp in basis points (0–10000).
             */
            from: number;
            /**
             * End of the ramp in basis points (0–10000).
             */
            to: number;
            /**
             * Ramp start, epoch milliseconds (UTC).
             */
            startAt: number;
            /**
             * Ramp length in milliseconds.
             */
            durationMs: number;
        };
        /**
         * If `true`, dashboard hides edit controls (e.g. the trailing public-floor entry).
         */
        locked?: boolean;
    } | {
        /**
         * Client-supplied stable id for the stack entry.
         */
        id: string;
        /**
         * Discriminator. `rollout` = percentage-bucket sub-gate.
         */
        type: 'rollout';
        /**
         * Human label shown in the dashboard.
         */
        name?: string;
        /**
         * Source template id, or `null`.
         */
        fromTemplate?: string | null;
        /**
         * Bucket size in basis points (0–10000 = 0%–100%). `5000` = 50%, `100` = 1%, allows sub-1% precision.
         */
        rolloutPct: number;
        /**
         * Attribute used to hash the caller into a bucket. Defaults to `user.userID`. Use e.g. `accountID` to bucket whole accounts together.
         */
        bucketBy?: string;
        /**
         * Per-entry hash salt. Defaults to the gate's salt. Set explicitly to keep two rollouts independent or correlated across gates.
         */
        salt?: string;
        /**
         * Linear rollout ramp. The effective rollout % interpolates `from`→`to` over `[startAt, startAt+durationMs]`, clamped outside that window, evaluated against the wall-clock at eval time. Overrides the entry's static `rolloutPct` while present.
         */
        ramp?: {
            /**
             * Start of the ramp in basis points (0–10000).
             */
            from: number;
            /**
             * End of the ramp in basis points (0–10000).
             */
            to: number;
            /**
             * Ramp start, epoch milliseconds (UTC).
             */
            startAt: number;
            /**
             * Ramp length in milliseconds.
             */
            durationMs: number;
        };
        /**
         * If `true`, dashboard hides edit controls.
         */
        locked?: boolean;
    }> | null;
    /**
     * Human-readable title shown in the dashboard. Free-form, no key format constraint.
     */
    title?: string;
    /**
     * Long-form description / runbook. Markdown is rendered in the dashboard.
     */
    description?: string;
    folder?: Folder;
    /**
     * Group label for dashboard organisation (e.g. team or product area).
     */
    group?: string;
    /**
     * Owner contact. Displayed verbatim; not used for auth.
     */
    owner_email?: string;
};

export type CreateGateResponse = {
    /**
     * Newly assigned gate id (`gat_…`).
     */
    id: string;
    /**
     * Stable gate key used by SDKs (`Shipeasy.checkGate(user, '<name>')`). Single segment or `folder.name`. Lowercase letters, digits, `_` or `-`; max 128 chars. Immutable after create — rename = delete + recreate.
     */
    name: string;
};

export type DeleteGateResponse = {
    ok: true;
};

/**
 * Body for `PATCH /api/admin/gates/{id}`. Partial — only supplied fields change. Array fields (`rules`, `stack`) replace, not merge.
 */
export type UpdateGateRequest = {
    /**
     * New rollout in **basis points** (0–10000 = 0%–100%) — `100` here means **1%**. Use `rollout_percent` (0–100) below for percent. Omit both to leave unchanged.
     */
    rollout_pct?: number;
    /**
     * New rollout as a **percentage** (0–100). Friendlier alias for `rollout_pct`; converted internally. Wins over `rollout_pct` if both are supplied. Omit both to leave unchanged.
     */
    rollout_percent?: number;
    /**
     * Replaces the rule list wholesale. To add a value to an `in` rule, send the full new `rules` array with the augmented `value` (e.g. previous `['US','CA']` → `['US','CA','GB']`).
     */
    rules?: Array<{
        /**
         * Attribute key on the evaluation context (e.g. `country`, `plan`, `email`). Matched case-sensitively against `Shipeasy.checkGate(user, name)` input.
         */
        attr: string;
        /**
         * Comparison operator. Equality: `eq`/`neq`. Set membership: `in`/`not_in` (value is an array). Numeric order: `gt`/`gte`/`lt`/`lte`. Semver order: `semver_gt`/`semver_gte`/`semver_lt`/`semver_lte` (value is a dotted version like `4.2.0`). String: `contains` (substring), `regex` (JS-flavour pattern, value is the pattern string). Cross-resource references (evaluated only on the worker hot path, where both KV blobs load): `gate_pass` (`value` is another gate's name — true when that gate passes for the same caller); `exp_in` (`attr` is a running experiment's name, `value` is a group name, `"$holdout"`, or `"$any"` — true when the caller is in that variant / the universe holdout / any variant).
         */
        op: 'eq' | 'neq' | 'in' | 'not_in' | 'gt' | 'gte' | 'lt' | 'lte' | 'contains' | 'regex' | 'semver_gt' | 'semver_gte' | 'semver_lt' | 'semver_lte' | 'gate_pass' | 'exp_in';
        /**
         * Operand. Shape depends on `op`: scalar for `eq/neq/gt/gte/lt/lte/contains/semver_*`; array of scalars for `in/not_in`; string pattern for `regex`; referenced gate name for `gate_pass`; group name / `"$holdout"` / `"$any"` for `exp_in`.
         */
        value: unknown;
    }>;
    /**
     * Master switch. `false` makes the gate evaluate to `false` for every caller regardless of `rollout_pct`, `rules`, or `stack` — use as kill switch.
     */
    enabled?: boolean;
    /**
     * Replaces the gatekeeper stack wholesale. Send `null` to revert to flat `rules` + `rollout_pct` evaluation.
     */
    stack?: Array<{
        /**
         * Client-supplied stable id for the stack entry (used for React keys, audit diffs, and as the default per-condition rollout salt).
         */
        id: string;
        /**
         * Discriminator. `condition` = rule-based predicate sub-gate.
         */
        type: 'condition';
        /**
         * Human label shown in the dashboard.
         */
        name?: string;
        /**
         * Id of the project template this entry was seeded from, or `null` if hand-authored.
         */
        fromTemplate?: string | null;
        /**
         * Combinator across `rules`. `all` = AND (default), `any` = OR.
         */
        pass?: 'all' | 'any';
        /**
         * Predicates evaluated under `pass`. Empty array = match nothing.
         */
        rules?: Array<{
            /**
             * Attribute key on the evaluation context (e.g. `country`, `plan`, `email`). Matched case-sensitively against `Shipeasy.checkGate(user, name)` input.
             */
            attr: string;
            /**
             * Comparison operator. Equality: `eq`/`neq`. Set membership: `in`/`not_in` (value is an array). Numeric order: `gt`/`gte`/`lt`/`lte`. Semver order: `semver_gt`/`semver_gte`/`semver_lt`/`semver_lte` (value is a dotted version like `4.2.0`). String: `contains` (substring), `regex` (JS-flavour pattern, value is the pattern string). Cross-resource references (evaluated only on the worker hot path, where both KV blobs load): `gate_pass` (`value` is another gate's name — true when that gate passes for the same caller); `exp_in` (`attr` is a running experiment's name, `value` is a group name, `"$holdout"`, or `"$any"` — true when the caller is in that variant / the universe holdout / any variant).
             */
            op: 'eq' | 'neq' | 'in' | 'not_in' | 'gt' | 'gte' | 'lt' | 'lte' | 'contains' | 'regex' | 'semver_gt' | 'semver_gte' | 'semver_lt' | 'semver_lte' | 'gate_pass' | 'exp_in';
            /**
             * Operand. Shape depends on `op`: scalar for `eq/neq/gt/gte/lt/lte/contains/semver_*`; array of scalars for `in/not_in`; string pattern for `regex`; referenced gate name for `gate_pass`; group name / `"$holdout"` / `"$any"` for `exp_in`.
             */
            value: unknown;
        }>;
        /**
         * Per-condition rollout in basis points. After the rules match, the caller is bucketed at this %; in-bucket → gate `true`, otherwise fall through to the next entry. Absent ⇒ 10000 (100%, every matching caller passes).
         */
        rolloutPct?: number;
        /**
         * Attribute used to hash the caller into the per-condition bucket. Default `userID`.
         */
        bucketBy?: string;
        /**
         * Per-condition hash salt. Defaults to the entry `id`, then the gate salt.
         */
        salt?: string;
        /**
         * Linear rollout ramp. The effective rollout % interpolates `from`→`to` over `[startAt, startAt+durationMs]`, clamped outside that window, evaluated against the wall-clock at eval time. Overrides the entry's static `rolloutPct` while present.
         */
        ramp?: {
            /**
             * Start of the ramp in basis points (0–10000).
             */
            from: number;
            /**
             * End of the ramp in basis points (0–10000).
             */
            to: number;
            /**
             * Ramp start, epoch milliseconds (UTC).
             */
            startAt: number;
            /**
             * Ramp length in milliseconds.
             */
            durationMs: number;
        };
        /**
         * If `true`, dashboard hides edit controls (e.g. the trailing public-floor entry).
         */
        locked?: boolean;
    } | {
        /**
         * Client-supplied stable id for the stack entry.
         */
        id: string;
        /**
         * Discriminator. `rollout` = percentage-bucket sub-gate.
         */
        type: 'rollout';
        /**
         * Human label shown in the dashboard.
         */
        name?: string;
        /**
         * Source template id, or `null`.
         */
        fromTemplate?: string | null;
        /**
         * Bucket size in basis points (0–10000 = 0%–100%). `5000` = 50%, `100` = 1%, allows sub-1% precision.
         */
        rolloutPct: number;
        /**
         * Attribute used to hash the caller into a bucket. Defaults to `user.userID`. Use e.g. `accountID` to bucket whole accounts together.
         */
        bucketBy?: string;
        /**
         * Per-entry hash salt. Defaults to the gate's salt. Set explicitly to keep two rollouts independent or correlated across gates.
         */
        salt?: string;
        /**
         * Linear rollout ramp. The effective rollout % interpolates `from`→`to` over `[startAt, startAt+durationMs]`, clamped outside that window, evaluated against the wall-clock at eval time. Overrides the entry's static `rolloutPct` while present.
         */
        ramp?: {
            /**
             * Start of the ramp in basis points (0–10000).
             */
            from: number;
            /**
             * End of the ramp in basis points (0–10000).
             */
            to: number;
            /**
             * Ramp start, epoch milliseconds (UTC).
             */
            startAt: number;
            /**
             * Ramp length in milliseconds.
             */
            durationMs: number;
        };
        /**
         * If `true`, dashboard hides edit controls.
         */
        locked?: boolean;
    }> | null;
    /**
     * Human-readable title shown in the dashboard. Free-form, no key format constraint.
     */
    title?: string;
    /**
     * Long-form description / runbook. Markdown is rendered in the dashboard.
     */
    description?: string;
    folder?: Folder;
    /**
     * Group label for dashboard organisation (e.g. team or product area).
     */
    group?: string;
    /**
     * Owner contact. Displayed verbatim; not used for auth.
     */
    owner_email?: string;
};

/**
 * Update returns only the id — re-fetch via `GET /api/admin/gates` for the full row.
 */
export type UpdateGateResponse = {
    /**
     * Gate id that was updated.
     */
    id: string;
};

export type EnableGateResponse = {
    id: string;
    enabled: boolean;
};

export type DisableGateResponse = {
    id: string;
    enabled: boolean;
};

export type ListExperimentsResponse = {
    data: Array<{
        /**
         * Stable opaque experiment id (`exp_…`).
         */
        id: string;
        /**
         * Stable experiment key. Single segment or `folder.name` (a-z, 0-9, `_`/`-`; max 128 chars). Used by SDKs as `Shipeasy.getExperiment(user, '<name>')`. Immutable after create.
         */
        name: string;
        description: string | null;
        hypothesis: string | null;
        tag: string | null;
        ownerEmail: string | null;
        audience: string | null;
        bucketBy: string | null;
        folder: string | null;
        status: 'draft' | 'running' | 'stopped' | 'archived';
        /**
         * Universe name this experiment draws from.
         */
        universe: string;
        targetingGate: string | null;
        /**
         * Allocation in basis points (0–10000).
         */
        allocationPct: number;
        salt: string;
        params: {
            [key: string]: 'string' | 'bool' | 'number';
        };
        groups: Array<{
            /**
             * Group label (e.g. `control`, `treatment_a`). Max 64 chars.
             */
            name: string;
            /**
             * Allocation weight in basis points (0–10000). The sum across all groups must equal exactly **10000** (100%).
             */
            weight: number;
            /**
             * Per-group parameter values delivered to the SDK when a caller is hashed into this group. Keys must match the experiment's `params` schema.
             */
            params?: {
                [key: string]: unknown;
            };
        }>;
        significanceThreshold: number;
        minRuntimeDays: number;
        minSampleSize: number;
        sequentialTesting: boolean;
        /**
         * ISO-8601 timestamp the experiment last transitioned to `running`, or `null`.
         */
        startedAt: string | null;
        stoppedAt?: string | null;
        updatedAt: string;
    }>;
    next_cursor: string | null;
};

/**
 * Stable metric key. Single segment or `folder.name`; lowercase letters, digits, `_`/`-`; max 128 chars.
 */
export type MetricName = string;

/**
 * Inline metric — a DSL `query`, or an `event` (+ `aggregation`/`value`) the server compiles into one.
 */
export type ExperimentInlineMetric = {
    name?: MetricName;
    /**
     * Metric DSL string. Provide this OR `event`.
     */
    query?: string;
    /**
     * Event name to build the metric from server-side. Auto-created if missing.
     */
    event?: string;
    /**
     * Reducer for the `event` form. Defaults to `count_users`.
     */
    aggregation?: 'count_users' | 'count_events' | 'retention_7d' | 'retention_30d' | 'sum' | 'avg';
    /**
     * Numeric event property for `sum`/`avg` (with `event`).
     */
    value?: string;
};

/**
 * Body for `POST /api/admin/experiments`. `name`, `universe`, and `groups` (≥2, weights sum to 10000) required.
 */
export type CreateExperimentRequest = {
    /**
     * Stable experiment key. Single segment or `folder.name` (a-z, 0-9, `_`/`-`; max 128 chars). Used by SDKs as `Shipeasy.getExperiment(user, '<name>')`. Immutable after create.
     */
    name: string;
    /**
     * Free-form description. Max 2000 chars, markdown rendered in the dashboard.
     */
    description?: string | null;
    /**
     * Hypothesis statement shown in the editor. Display-only.
     */
    hypothesis?: string | null;
    /**
     * Short tag chip rendered next to the name. Display-only.
     */
    tag?: string | null;
    /**
     * Owner email. Display-only.
     */
    owner_email?: string | null;
    /**
     * Audience label shown in the editor. Display-only.
     */
    audience?: string | null;
    bucket_by?: string | null;
    folder?: Folder;
    /**
     * Name of an existing universe in the project. Returns `422` if the universe doesn't exist.
     */
    universe: string;
    /**
     * Optional gate name. Only callers that pass the gate are enrolled in the experiment.
     */
    targeting_gate?: string | null;
    /**
     * Share of the (gated) audience allocated to the experiment, in basis points (0–10000 = 0%–100%). `0` = unallocated. Use `allocation_percent` (0–100) below to think in percent. Immutable while the experiment is running.
     */
    allocation_pct?: number;
    /**
     * Allocation as a **percentage** (0–100, fractional ok). Friendlier alias for `allocation_pct`; converted to basis points server-side (e.g. `50` = 5000 bp). If both are set, `allocation_percent` wins.
     */
    allocation_percent?: number;
    /**
     * Hash salt for bucketing. Auto-generated if omitted. Immutable while running.
     */
    salt?: string;
    /**
     * Map of param-name → scalar type. Defines the shape of `groups[].params`. Example: `{ headline: 'string', show_cta: 'bool' }`.
     */
    params?: {
        [key: string]: 'string' | 'bool' | 'number';
    };
    /**
     * Two or more variants. Weights must sum to exactly 10000 (100%). Immutable while running.
     */
    groups: Array<{
        /**
         * Group label (e.g. `control`, `treatment_a`). Max 64 chars.
         */
        name: string;
        /**
         * Allocation weight in basis points (0–10000). The sum across all groups must equal exactly **10000** (100%).
         */
        weight: number;
        /**
         * Per-group parameter values delivered to the SDK when a caller is hashed into this group. Keys must match the experiment's `params` schema.
         */
        params?: {
            [key: string]: unknown;
        };
    }>;
    /**
     * p-value cutoff used by the analysis pass. Defaults to `0.05`. Values other than 0.05 require Pro plan or higher.
     */
    significance_threshold?: number;
    /**
     * Minimum days the experiment must run before results are considered conclusive.
     */
    min_runtime_days?: number;
    /**
     * Minimum exposures per group before results are considered conclusive.
     */
    min_sample_size?: number;
    /**
     * Enable sequential testing (always-valid p-values). Requires Premium plan or higher.
     */
    sequential_testing?: boolean;
    /**
     * Single goal metric defined inline — either a DSL `query` or an `event` (+`aggregation`/`value`) the server compiles. Attaching one is required before the experiment can be started. The underlying event is auto-created if missing.
     */
    goal_metric?: ExperimentInlineMetric;
    /**
     * Up to 10 guardrail metrics defined inline. Each is upserted (event + metric) and attached with role=guardrail.
     */
    guardrail_metrics?: Array<ExperimentInlineMetric>;
};

export type CreateExperimentResponse = {
    /**
     * Newly assigned experiment id.
     */
    id: string;
    /**
     * Stable experiment key. Single segment or `folder.name` (a-z, 0-9, `_`/`-`; max 128 chars). Used by SDKs as `Shipeasy.getExperiment(user, '<name>')`. Immutable after create.
     */
    name: string;
};

export type GetExperimentResponse = {
    /**
     * Stable opaque experiment id (`exp_…`).
     */
    id: string;
    /**
     * Stable experiment key. Single segment or `folder.name` (a-z, 0-9, `_`/`-`; max 128 chars). Used by SDKs as `Shipeasy.getExperiment(user, '<name>')`. Immutable after create.
     */
    name: string;
    description: string | null;
    hypothesis: string | null;
    tag: string | null;
    ownerEmail: string | null;
    audience: string | null;
    bucketBy: string | null;
    folder: string | null;
    status: 'draft' | 'running' | 'stopped' | 'archived';
    /**
     * Universe name this experiment draws from.
     */
    universe: string;
    targetingGate: string | null;
    /**
     * Allocation in basis points (0–10000).
     */
    allocationPct: number;
    salt: string;
    params: {
        [key: string]: 'string' | 'bool' | 'number';
    };
    groups: Array<{
        /**
         * Group label (e.g. `control`, `treatment_a`). Max 64 chars.
         */
        name: string;
        /**
         * Allocation weight in basis points (0–10000). The sum across all groups must equal exactly **10000** (100%).
         */
        weight: number;
        /**
         * Per-group parameter values delivered to the SDK when a caller is hashed into this group. Keys must match the experiment's `params` schema.
         */
        params?: {
            [key: string]: unknown;
        };
    }>;
    significanceThreshold: number;
    minRuntimeDays: number;
    minSampleSize: number;
    sequentialTesting: boolean;
    /**
     * ISO-8601 timestamp the experiment last transitioned to `running`, or `null`.
     */
    startedAt: string | null;
    stoppedAt?: string | null;
    updatedAt: string;
};

export type DeleteExperimentResponse = {
    ok: true;
};

/**
 * Body for `PATCH /api/admin/experiments/{id}`. Partial — only supplied fields change. `allocation_pct`, `groups`, `salt`, `universe`, `params` are immutable while the experiment is running (stop first).
 */
export type UpdateExperimentRequest = {
    /**
     * Stable experiment key. Single segment or `folder.name` (a-z, 0-9, `_`/`-`; max 128 chars). Used by SDKs as `Shipeasy.getExperiment(user, '<name>')`. Immutable after create.
     */
    name?: string;
    description?: string | null;
    hypothesis?: string | null;
    tag?: string | null;
    owner_email?: string | null;
    audience?: string | null;
    bucket_by?: string | null;
    folder?: Folder;
    targeting_gate?: string | null;
    /**
     * Basis-points allocation (0–10000). Use `allocation_percent` (0–100) for percent. Immutable while the experiment is running.
     */
    allocation_pct?: number;
    /**
     * Allocation as a **percentage** (0–100). Friendlier alias for `allocation_pct`; converted to basis points server-side. Wins over `allocation_pct` if both are supplied. Immutable while running.
     */
    allocation_percent?: number;
    /**
     * Hash salt. Immutable while running.
     */
    salt?: string;
    /**
     * New universe name. Immutable while running. Returns `422` if the universe doesn't exist.
     */
    universe?: string;
    /**
     * Map of param-name → scalar type. Defines the shape of `groups[].params`. Example: `{ headline: 'string', show_cta: 'bool' }`.
     */
    params?: {
        [key: string]: 'string' | 'bool' | 'number';
    };
    /**
     * Replacement groups. Weights must sum to 10000. Immutable while running.
     */
    groups?: Array<{
        /**
         * Group label (e.g. `control`, `treatment_a`). Max 64 chars.
         */
        name: string;
        /**
         * Allocation weight in basis points (0–10000). The sum across all groups must equal exactly **10000** (100%).
         */
        weight: number;
        /**
         * Per-group parameter values delivered to the SDK when a caller is hashed into this group. Keys must match the experiment's `params` schema.
         */
        params?: {
            [key: string]: unknown;
        };
    }>;
    significance_threshold?: number;
    min_runtime_days?: number;
    min_sample_size?: number;
    sequential_testing?: boolean;
    /**
     * Replaces the goal metric — DSL `query` or `event` (+`aggregation`/`value`) the server compiles (event auto-upserted).
     */
    goal_metric?: ExperimentInlineMetric;
    /**
     * Replaces the guardrail set wholesale (event auto-upserted per entry).
     */
    guardrail_metrics?: Array<ExperimentInlineMetric>;
};

export type UpdateExperimentResponse = {
    /**
     * Experiment id that was updated.
     */
    id: string;
};

/**
 * Body for `POST /api/admin/experiments/{id}/status`.
 */
export type SetExperimentStatusRequest = {
    /**
     * Target status. Allowed transitions: `draft → running`, `running → stopped`, `stopped → archived`, `draft → archived`, and `archived → draft` (restore — only if the experiment never started). Restarting an archived experiment directly is not allowed; restore it to draft first, then start.
     */
    status: 'draft' | 'running' | 'stopped' | 'archived';
};

export type SetExperimentStatusResponse = {
    id: string;
    status: 'draft' | 'running' | 'stopped' | 'archived';
};

/**
 * Body for `POST /api/admin/experiments/{id}/metrics`. Replaces the experiment's metric attachments wholesale. Returns `422` if any `metric_id` is unknown.
 */
export type SetExperimentMetricsRequest = {
    /**
     * Replacement metrics list — replaces the current attachments wholesale.
     */
    metrics: Array<{
        /**
         * Existing metric id in the project.
         */
        metric_id: string;
        /**
         * Metric role. `goal` drives the decision, `guardrail` blocks ship if degraded, `secondary` is informational.
         */
        role: 'goal' | 'guardrail' | 'secondary';
    }>;
};

export type SetExperimentMetricsResponse = {
    id: string;
    metrics: Array<{
        metric_id: string;
        role: 'goal' | 'guardrail' | 'secondary';
    }>;
};

export type GetExperimentResultsResponse = {
    experiment: {
        id: string;
        /**
         * Stable experiment key. Single segment or `folder.name` (a-z, 0-9, `_`/`-`; max 128 chars). Used by SDKs as `Shipeasy.getExperiment(user, '<name>')`. Immutable after create.
         */
        name: string;
        status: 'draft' | 'running' | 'stopped' | 'archived';
    };
    results: Array<{
        metric: string;
        group_name: string;
        /**
         * Date slice (`YYYY-MM-DD`).
         */
        ds: string;
        n: number | null;
        mean: number | null;
        delta_pct: number | null;
        p_value: number | null;
        /**
         * `1` if sample-ratio mismatch detected, else `0`.
         */
        srm_detected: number | null;
    }>;
    /**
     * Server-computed decision from the goal metric + guardrails + SRM vs. the significance threshold and min runtime: `ship` (goal significant + guardrails pass), `hold` (a guardrail regressed), `wait` (inconclusive / under-powered), `invalid` (sample-ratio mismatch), `draft` (never started).
     */
    verdict: 'ship' | 'hold' | 'wait' | 'invalid' | 'draft';
};

export type GetExperimentTimeseriesResponse = {
    experiment: {
        id: string;
        /**
         * Stable experiment key. Single segment or `folder.name` (a-z, 0-9, `_`/`-`; max 128 chars). Used by SDKs as `Shipeasy.getExperiment(user, '<name>')`. Immutable after create.
         */
        name: string;
        status: 'draft' | 'running' | 'stopped' | 'archived';
    };
    series: Array<{
        metric: string;
        group_name: string;
        /**
         * Date slice (`YYYY-MM-DD`).
         */
        ds: string;
        n: number | null;
        mean: number | null;
        delta_pct: number | null;
        p_value: number | null;
        /**
         * `1` if sample-ratio mismatch detected, else `0`.
         */
        srm_detected: number | null;
    }>;
};

export type ReanalyzeExperimentResponse = {
    id: string;
    queued: true;
};

export type ListConfigsResponse = {
    data: Array<{
        /**
         * Stable opaque config id (`cfg_…`).
         */
        id: string;
        /**
         * Stable config key in `folder.name` form (two lowercase segments separated by a dot, e.g. `pricing.tiers`). Used by SDKs as `Shipeasy.getConfig('<name>')`. Immutable after create.
         */
        name: string;
        description: string | null;
        /**
         * JSON Schema (draft 2020-12) describing the shape of the config value. Top-level `type` must be `'object'`; every published value is validated against this schema.
         */
        schema: {
            [key: string]: unknown;
        };
        /**
         * ISO-8601 timestamp of last mutation.
         */
        updatedAt: string;
        /**
         * Per-env latest published version metadata.
         */
        envs: {
            [key: string]: {
                /**
                 * Monotonically increasing version per env.
                 */
                version: number;
                /**
                 * ISO-8601 publish timestamp.
                 */
                publishedAt: string;
                /**
                 * Email/id of the actor who published this version.
                 */
                publishedBy: string;
            };
        };
        /**
         * Per-env active drafts (if any).
         */
        drafts: {
            [key: string]: {
                /**
                 * ISO-8601 last-edit timestamp on the draft.
                 */
                updatedAt: string;
                /**
                 * Email/id of the actor who last edited the draft.
                 */
                authorEmail: string;
                /**
                 * Published version the draft was forked from.
                 */
                baseVersion: number;
            };
        };
        /**
         * Per-env latest published values (only returned by `GET /{id}`, not list).
         */
        values?: {
            [key: string]: unknown;
        };
        /**
         * Per-env draft values (only returned by `GET /{id}`).
         */
        draftValues?: {
            [key: string]: unknown;
        };
    }>;
    next_cursor: string | null;
};

/**
 * Stable config/killswitch key in `folder.name` form (two lowercase segments separated by a dot, e.g. `pricing.tiers`). Immutable after create.
 */
export type ConfigName = string;

/**
 * Body for `POST /api/admin/configs`. `name` + `schema` required.
 */
export type CreateConfigRequest = {
    name: ConfigName;
    /**
     * Optional free-form description shown in the dashboard. Max 512 chars.
     */
    description?: string;
    folder?: Folder;
    /**
     * JSON Schema (draft 2020-12) describing the shape of the config value. Top-level `type` must be `'object'`; every published value is validated against this schema.
     */
    schema: {
        [key: string]: unknown;
    };
    /**
     * Initial config value. Either a single JSON object applied to every env, or a `{ env: value }` map seeding per-env values. Must match `schema`. Defaults to `{}` on every env when omitted.
     */
    value?: unknown;
};

export type CreateConfigResponse = {
    /**
     * Newly assigned config id.
     */
    id: string;
    /**
     * Stable config key in `folder.name` form (two lowercase segments separated by a dot, e.g. `pricing.tiers`). Used by SDKs as `Shipeasy.getConfig('<name>')`. Immutable after create.
     */
    name: string;
};

export type GetConfigResponse = {
    /**
     * Stable opaque config id (`cfg_…`).
     */
    id: string;
    /**
     * Stable config key in `folder.name` form (two lowercase segments separated by a dot, e.g. `pricing.tiers`). Used by SDKs as `Shipeasy.getConfig('<name>')`. Immutable after create.
     */
    name: string;
    description: string | null;
    /**
     * JSON Schema (draft 2020-12) describing the shape of the config value. Top-level `type` must be `'object'`; every published value is validated against this schema.
     */
    schema: {
        [key: string]: unknown;
    };
    /**
     * ISO-8601 timestamp of last mutation.
     */
    updatedAt: string;
    /**
     * Per-env latest published version metadata.
     */
    envs: {
        [key: string]: {
            /**
             * Monotonically increasing version per env.
             */
            version: number;
            /**
             * ISO-8601 publish timestamp.
             */
            publishedAt: string;
            /**
             * Email/id of the actor who published this version.
             */
            publishedBy: string;
        };
    };
    /**
     * Per-env active drafts (if any).
     */
    drafts: {
        [key: string]: {
            /**
             * ISO-8601 last-edit timestamp on the draft.
             */
            updatedAt: string;
            /**
             * Email/id of the actor who last edited the draft.
             */
            authorEmail: string;
            /**
             * Published version the draft was forked from.
             */
            baseVersion: number;
        };
    };
    /**
     * Per-env latest published values (only returned by `GET /{id}`, not list).
     */
    values?: {
        [key: string]: unknown;
    };
    /**
     * Per-env draft values (only returned by `GET /{id}`).
     */
    draftValues?: {
        [key: string]: unknown;
    };
};

export type DeleteConfigResponse = {
    ok: true;
};

/**
 * Body for `PATCH /api/admin/configs/{id}`. Partial — only supplied fields change. `value` republishes on every env.
 */
export type UpdateConfigRequest = {
    /**
     * Replacement schema. When supplied, the new schema is validated against every published value before it lands.
     */
    schema?: {
        [key: string]: unknown;
    };
    /**
     * Flat value applied to **every** env. Publishes a new version per env. To target one env, use `PUT /{id}/drafts` then `POST /{id}/publish`.
     */
    value?: unknown;
    folder?: Folder;
};

export type UpdateConfigResponse = {
    /**
     * Config id that was updated.
     */
    id: string;
};

/**
 * Target environment. One of the project's configured envs (`dev`, `staging`, `prod`).
 */
export type Env = 'dev' | 'staging' | 'prod';

/**
 * Body for `PUT /api/admin/configs/{id}/drafts`. Stages a value for a single env without publishing.
 */
export type SaveConfigDraftRequest = {
    env: Env;
    /**
     * Draft value to stage on `env`. Validated against the config's current schema.
     */
    value: unknown;
};

export type SaveConfigDraftResponse = {
    id: string;
    env: Env;
    /**
     * Published version the draft is based on.
     */
    baseVersion: number;
    updatedAt: string;
};

/**
 * Body for `POST /api/admin/configs/{id}/publish` and `DELETE /api/admin/configs/{id}/drafts`. Names the target env.
 */
export type DiscardConfigDraftRequest = {
    env: Env;
};

export type DiscardConfigDraftResponse = {
    ok: true;
};

/**
 * Body for `POST /api/admin/configs/{id}/publish` and `DELETE /api/admin/configs/{id}/drafts`. Names the target env.
 */
export type PublishConfigDraftRequest = {
    env: Env;
};

export type PublishConfigDraftResponse = {
    id: string;
    env: Env;
    /**
     * Newly published version on `env`.
     */
    version: number;
};

/**
 * Recent audit rows for one config, newest first.
 */
export type ListConfigActivityResponse = Array<{
    id: string;
    /**
     * Audit action (e.g. `config.create`, `config.publish`).
     */
    action: string;
    actorEmail: string;
    actorType: 'user' | 'cli' | 'system';
    payload: unknown;
    /**
     * ISO-8601 timestamp.
     */
    createdAt: string;
}>;

/**
 * Body for `PATCH /api/admin/configs/{id}/schema`. Schema-only update — replaces the config's schema and re-validates existing values.
 */
export type UpdateConfigSchemaRequest = {
    /**
     * Replacement JSON Schema (draft 2020-12). Validated against every published value before it lands.
     */
    schema: {
        [key: string]: unknown;
    };
};

export type UpdateConfigSchemaResponse = {
    /**
     * Config id whose schema was updated.
     */
    id: string;
};

export type ListKillswitchesResponse = {
    data: Array<{
        /**
         * Stable opaque killswitch id.
         */
        id: string;
        /**
         * Stable config key in `folder.name` form (two lowercase segments separated by a dot, e.g. `pricing.tiers`). Used by SDKs as `Shipeasy.getConfig('<name>')`. Immutable after create.
         */
        name: string;
        /**
         * Free-form description or `null`.
         */
        description: string | null;
        /**
         * ISO-8601 timestamp of last mutation.
         */
        updatedAt: string;
        /**
         * Per-env latest value, switches, version, and publish timestamp.
         */
        envs: {
            [key: string]: {
                /**
                 * Flat boolean published for this env.
                 */
                value: boolean;
                switches?: {
                    [key: string]: boolean;
                };
                /**
                 * Monotonically increasing version per env.
                 */
                version: number;
                /**
                 * ISO-8601 publish timestamp for this version.
                 */
                publishedAt: string;
                /**
                 * Email/id of the actor who published this version.
                 */
                publishedBy?: string | null;
            };
        };
    }>;
    next_cursor: string | null;
};

/**
 * Body for `POST /api/admin/killswitches`. Only `name` is required.
 */
export type CreateKillswitchRequest = {
    name: ConfigName;
    /**
     * Optional free-form description shown in the dashboard. Max 512 chars.
     */
    description?: string;
    folder?: Folder;
    /**
     * Default value applied to every env at creation. Defaults to `false`. Use `true` to ship the killswitch pre-tripped.
     */
    value?: boolean;
    /**
     * Initial per-switch overrides applied to every env. Empty/omitted leaves the killswitch with only the flat `value`.
     */
    switches?: {
        [key: string]: boolean;
    };
};

export type CreateKillswitchResponse = {
    /**
     * Newly assigned killswitch id.
     */
    id: string;
    /**
     * Stable config key in `folder.name` form (two lowercase segments separated by a dot, e.g. `pricing.tiers`). Used by SDKs as `Shipeasy.getConfig('<name>')`. Immutable after create.
     */
    name: string;
};

export type GetKillswitchResponse = {
    /**
     * Stable opaque killswitch id.
     */
    id: string;
    /**
     * Stable config key in `folder.name` form (two lowercase segments separated by a dot, e.g. `pricing.tiers`). Used by SDKs as `Shipeasy.getConfig('<name>')`. Immutable after create.
     */
    name: string;
    /**
     * Free-form description or `null`.
     */
    description: string | null;
    /**
     * ISO-8601 timestamp of last mutation.
     */
    updatedAt: string;
    /**
     * Per-env latest value, switches, version, and publish timestamp.
     */
    envs: {
        [key: string]: {
            /**
             * Flat boolean published for this env.
             */
            value: boolean;
            switches?: {
                [key: string]: boolean;
            };
            /**
             * Monotonically increasing version per env.
             */
            version: number;
            /**
             * ISO-8601 publish timestamp for this version.
             */
            publishedAt: string;
            /**
             * Email/id of the actor who published this version.
             */
            publishedBy?: string | null;
        };
    };
};

export type DeleteKillswitchResponse = {
    ok: true;
};

/**
 * Body for `PATCH /api/admin/killswitches/{id}`. Partial — only supplied fields change. `switches` replaces wholesale (no merge).
 */
export type UpdateKillswitchRequest = {
    /**
     * New description, or `null` to clear it. Max 512 chars.
     */
    description?: string | null;
    folder?: Folder;
    /**
     * Flat value applied to every env. Publishes a new version per env when set. Omit to leave values unchanged.
     */
    value?: boolean;
    /**
     * Replace the switches map wholesale on every env. To edit a single entry on a single env use `PUT /{id}/switch` instead.
     */
    switches?: {
        [key: string]: boolean;
    };
};

export type UpdateKillswitchResponse = {
    /**
     * Killswitch id that was updated.
     */
    id: string;
};

/**
 * Body for `PUT /api/admin/killswitches/{id}/switch`. Sets or updates one switch entry on one env.
 */
export type SetKillswitchSwitchRequest = {
    env: Env;
    /**
     * Switch key to set.
     */
    switchKey: string;
    /**
     * New boolean value for this `switchKey` on this `env`.
     */
    value: boolean;
};

export type SetKillswitchSwitchResponse = {
    id: string;
    env: Env;
    /**
     * Single-segment switch key (lowercase letters, digits, `_`/`-`; no dots). Used as the nested switch entry inside a killswitch's `switches` map.
     */
    switchKey: string;
    value: boolean;
};

/**
 * Body for `DELETE /api/admin/killswitches/{id}/switch`. Removes one switch entry from one env.
 */
export type UnsetKillswitchSwitchRequest = {
    env: Env;
    /**
     * Switch key to remove.
     */
    switchKey: string;
};

export type UnsetKillswitchSwitchResponse = {
    id: string;
    env: Env;
    /**
     * Single-segment switch key (lowercase letters, digits, `_`/`-`; no dots). Used as the nested switch entry inside a killswitch's `switches` map.
     */
    switchKey: string;
    /**
     * `true` if the entry existed and was removed, `false` if no-op.
     */
    removed: boolean;
};

/**
 * Body for `PUT /api/admin/killswitches/{id}/value`. Sets the flat `value` on one env without touching `switches` or other envs.
 */
export type SetKillswitchValueRequest = {
    env: Env;
    /**
     * Flat boolean to publish on `env`. Publishes a new version on that env only.
     */
    value: boolean;
};

/**
 * Runtime killswitch payload returned to SDKs.
 */
export type KillswitchValue = {
    /**
     * Default boolean delivered when no `switches` entry matches.
     */
    value: boolean;
    /**
     * Per-switch overrides; a matching key takes precedence over `value`.
     */
    switches?: {
        [key: string]: boolean;
    };
};

export type SetKillswitchValueResponse = {
    id: string;
    env: Env;
    /**
     * Newly published version on `env`.
     */
    version: number;
    published: KillswitchValue;
};

export type ListUniversesResponse = {
    data: Array<{
        /**
         * Stable opaque universe id.
         */
        id: string;
        /**
         * Stable universe key. Single segment or `folder.name`. Lowercase letters, digits, `_` or `-`; max 128 chars. Immutable after create.
         */
        name: string;
        /**
         * Unit of randomisation. Snake-case in request (`unit_type`), camelCase in response.
         */
        unitType: string;
        /**
         * Reserved holdout bucket range, or `null` if none.
         */
        holdoutRange: [
            number,
            number
        ] | null;
        /**
         * ISO-8601 timestamp of creation.
         */
        createdAt: string;
    }>;
    next_cursor: string | null;
};

/**
 * Body for `POST /api/admin/universes`. Only `name` is required.
 */
export type CreateUniverseRequest = {
    /**
     * Stable universe key. Single segment or `folder.name`. Lowercase letters, digits, `_` or `-`; max 128 chars. Immutable after create.
     */
    name: string;
    folder?: Folder;
    /**
     * Unit of randomisation. Typically `user_id`. Use `account_id` to keep whole accounts in the same group across an experiment.
     */
    unit_type?: string;
    /**
     * Inclusive `[lo, hi]` bucket range (0–9999) reserved as the **holdout** — callers hashed into this slice are excluded from every experiment in the universe. `null` disables the holdout. Pro plan or higher required.
     */
    holdout_range?: [
        number,
        number
    ] | null;
};

export type CreateUniverseResponse = {
    /**
     * Newly assigned universe id.
     */
    id: string;
    /**
     * Stable universe key. Single segment or `folder.name`. Lowercase letters, digits, `_` or `-`; max 128 chars. Immutable after create.
     */
    name: string;
};

export type DeleteUniverseResponse = {
    ok: true;
};

/**
 * Body for `PATCH /api/admin/universes/{id}`. Only `holdout_range` is mutable — name and unit_type are immutable after create.
 */
export type UpdateUniverseRequest = {
    folder?: Folder;
    /**
     * Inclusive `[lo, hi]` bucket range (0–9999) reserved as the **holdout** — callers hashed into this slice are excluded from every experiment in the universe. `null` disables the holdout. Pro plan or higher required.
     */
    holdout_range?: [
        number,
        number
    ] | null;
};

export type UpdateUniverseResponse = {
    /**
     * Universe id that was updated.
     */
    id: string;
};

/**
 * Every auto-inferred targeting attribute in the project.
 */
export type ListAttributesResponse = Array<{
    /**
     * Attribute key as seen in evaluation context (e.g. `plan`, `country`).
     */
    name: string;
    /**
     * Inferred value type (`string`, `number`, `boolean`, …) when known.
     */
    type?: string;
    [key: string]: unknown;
}>;

/**
 * Declared value type of a targeting attribute.
 */
export type AttributeType = 'string' | 'number' | 'boolean' | 'enum' | 'date';

/**
 * Body for `POST /api/admin/attributes`. Declares a targeting attribute.
 */
export type CreateAttributeRequest = {
    /**
     * Attribute key (lowercase alphanumeric start, then letters/digits/`_`/`-`; max 64 chars). Immutable after create.
     */
    name: string;
    type: AttributeType;
    /**
     * Allowed values when `type` is `enum` (required in that case — 422 otherwise); `null` for non-enum types.
     */
    enum_values?: Array<string> | null;
    /**
     * Whether the attribute must be present on the evaluation context.
     */
    required?: boolean;
    /**
     * Optional human note shown in the dashboard.
     */
    description?: string;
    /**
     * Optional dotted path the SDK reads the value from.
     */
    sdk_path?: string;
};

/**
 * Response for `POST /api/admin/attributes`.
 */
export type CreateAttributeResponse = {
    /**
     * Newly assigned attribute id.
     */
    id: string;
    /**
     * The attribute key that was created.
     */
    name: string;
};

/**
 * One targeting attribute row.
 */
export type GetAttributeResponse = {
    /**
     * Stable opaque attribute id.
     */
    id: string;
    /**
     * Attribute key.
     */
    name: string;
    type: AttributeType;
    /**
     * Allowed values for `enum` attributes, else `null`.
     */
    enumValues?: Array<string> | null;
    /**
     * Whether the attribute is required (D1 stores the flag as `0`/`1`).
     */
    required?: 0 | 1;
    /**
     * Human note, or `null`.
     */
    description?: string | null;
    /**
     * Dotted SDK path, or `null`.
     */
    sdkPath?: string | null;
    /**
     * ISO-8601 creation timestamp.
     */
    createdAt?: string;
    [key: string]: unknown;
};

/**
 * Response for `DELETE /api/admin/attributes/{id}`.
 */
export type DeleteAttributeResponse = {
    /**
     * True when the attribute was archived.
     */
    ok: boolean;
};

/**
 * Body for `PATCH /api/admin/attributes/{id}`. Every field is optional; `name` is immutable.
 */
export type UpdateAttributeRequest = {
    type?: AttributeType;
    /**
     * Replacement allowed values (for `enum`), or `null` to clear.
     */
    enum_values?: Array<string> | null;
    /**
     * Whether the attribute must be present on the evaluation context.
     */
    required?: boolean;
    /**
     * Optional human note shown in the dashboard.
     */
    description?: string;
    /**
     * Optional dotted path the SDK reads the value from.
     */
    sdk_path?: string;
};

/**
 * Response for `PATCH /api/admin/attributes/{id}`.
 */
export type UpdateAttributeResponse = {
    /**
     * Id of the attribute that was updated.
     */
    id: string;
};

/**
 * Every metric in the project (the list endpoint is not paginated).
 */
export type ListMetricsResponse = Array<{
    /**
     * Stable opaque metric id.
     */
    id: string;
    /**
     * Metric key.
     */
    name: string;
    /**
     * Folder grouping the metric, or `null`.
     */
    folder: string | null;
    /**
     * Source event name (camelCase in response).
     */
    eventName: string;
    /**
     * Legacy aggregation enum derived from the IR (`count_users`, `sum`, `ratio`, …).
     */
    aggregation: string;
    /**
     * Numeric value label for sum/avg metrics, or `null`.
     */
    valuePath: string | null;
    /**
     * Rendered DSL text form of the query, or `null` if it could not be rendered.
     */
    query?: string | null;
    /**
     * Typed query IR stored for the metric.
     */
    queryIr?: {
        /**
         * Aggregation function applied to the source event.
         */
        agg: {
            kind: 'count_users';
        } | {
            kind: 'count_events';
        } | {
            kind: 'sum';
        } | {
            kind: 'avg';
        } | {
            kind: 'min';
        } | {
            kind: 'max';
        } | {
            kind: 'unique';
        } | {
            kind: 'quantile';
            /**
             * Quantile fraction (0.5 = p50 … 0.999 = p999).
             */
            p: 0.5 | 0.75 | 0.9 | 0.95 | 0.99 | 0.999;
        } | {
            kind: 'retention_Nd';
            /**
             * Retention window in days (1–90).
             */
            n: number;
        } | {
            kind: 'ratio';
            /**
             * Numerator or denominator arm of a `ratio` aggregation.
             */
            numerator: {
                /**
                 * Counting mode for this ratio arm.
                 */
                agg: 'count_users' | 'count_events';
                /**
                 * Source event name for this ratio arm.
                 */
                metric: string;
                /**
                 * Optional label filters.
                 */
                filters?: Array<{
                    /**
                     * Event property / label identifier.
                     */
                    label: string;
                    /**
                     * Match operator (`=~`/`!~` are regex).
                     */
                    op: '=' | '!=' | '=~' | '!~';
                    /**
                     * Quoted filter value (coerced for numeric labels).
                     */
                    value: string;
                }>;
            };
            /**
             * Numerator or denominator arm of a `ratio` aggregation.
             */
            denominator: {
                /**
                 * Counting mode for this ratio arm.
                 */
                agg: 'count_users' | 'count_events';
                /**
                 * Source event name for this ratio arm.
                 */
                metric: string;
                /**
                 * Optional label filters.
                 */
                filters?: Array<{
                    /**
                     * Event property / label identifier.
                     */
                    label: string;
                    /**
                     * Match operator (`=~`/`!~` are regex).
                     */
                    op: '=' | '!=' | '=~' | '!~';
                    /**
                     * Quoted filter value (coerced for numeric labels).
                     */
                    value: string;
                }>;
            };
        };
        /**
         * Source event name (must equal `event_name`).
         */
        metric: string;
        /**
         * Numeric property summed/averaged for `sum`/`avg`/quantile aggregations.
         */
        valueLabel?: string;
        /**
         * Label filters on the event.
         */
        filters?: Array<{
            /**
             * Event property / label identifier.
             */
            label: string;
            /**
             * Match operator (`=~`/`!~` are regex).
             */
            op: '=' | '!=' | '=~' | '!~';
            /**
             * Quoted filter value (coerced for numeric labels).
             */
            value: string;
        }>;
        /**
         * Optional group-by clause (ignored for experiment analysis).
         */
        groupBy?: {
            /**
             * `by` keeps the listed labels; `without` drops them.
             */
            op: 'by' | 'without';
            /**
             * Labels to group by (max 5).
             */
            labels: Array<string>;
        };
    };
    /**
     * Desired direction of movement. `higher_better` (default), `lower_better`, or `neutral` (guardrail).
     */
    direction?: 'higher_better' | 'lower_better' | 'neutral';
    /**
     * Winsorise percentile applied to the metric.
     */
    winsorizePct?: number;
    /**
     * Configured MDE, or `null`.
     */
    minDetectableEffect?: number | null;
    /**
     * ISO-8601 creation timestamp.
     */
    createdAt?: string;
    /**
     * ISO-8601 last-update timestamp.
     */
    updatedAt?: string;
    [key: string]: unknown;
}>;

/**
 * Body for `POST /api/admin/metrics`. Requires `name`, `event_name`, and exactly one of `query` / `query_ir`.
 */
export type CreateMetricRequest = {
    name: MetricName;
    folder?: Folder;
    /**
     * Source event the query reads from.
     */
    event_name: string;
    /**
     * Metric query DSL string, e.g. `sum(purchase, amount)`. Provide this OR `query_ir`.
     */
    query?: string;
    /**
     * Typed query IR — the structured alternative to `query`. Provide this OR `query`.
     */
    query_ir?: {
        /**
         * Aggregation function applied to the source event.
         */
        agg: {
            kind: 'count_users';
        } | {
            kind: 'count_events';
        } | {
            kind: 'sum';
        } | {
            kind: 'avg';
        } | {
            kind: 'min';
        } | {
            kind: 'max';
        } | {
            kind: 'unique';
        } | {
            kind: 'quantile';
            /**
             * Quantile fraction (0.5 = p50 … 0.999 = p999).
             */
            p: 0.5 | 0.75 | 0.9 | 0.95 | 0.99 | 0.999;
        } | {
            kind: 'retention_Nd';
            /**
             * Retention window in days (1–90).
             */
            n: number;
        } | {
            kind: 'ratio';
            /**
             * Numerator or denominator arm of a `ratio` aggregation.
             */
            numerator: {
                /**
                 * Counting mode for this ratio arm.
                 */
                agg: 'count_users' | 'count_events';
                /**
                 * Source event name for this ratio arm.
                 */
                metric: string;
                /**
                 * Optional label filters.
                 */
                filters?: Array<{
                    /**
                     * Event property / label identifier.
                     */
                    label: string;
                    /**
                     * Match operator (`=~`/`!~` are regex).
                     */
                    op: '=' | '!=' | '=~' | '!~';
                    /**
                     * Quoted filter value (coerced for numeric labels).
                     */
                    value: string;
                }>;
            };
            /**
             * Numerator or denominator arm of a `ratio` aggregation.
             */
            denominator: {
                /**
                 * Counting mode for this ratio arm.
                 */
                agg: 'count_users' | 'count_events';
                /**
                 * Source event name for this ratio arm.
                 */
                metric: string;
                /**
                 * Optional label filters.
                 */
                filters?: Array<{
                    /**
                     * Event property / label identifier.
                     */
                    label: string;
                    /**
                     * Match operator (`=~`/`!~` are regex).
                     */
                    op: '=' | '!=' | '=~' | '!~';
                    /**
                     * Quoted filter value (coerced for numeric labels).
                     */
                    value: string;
                }>;
            };
        };
        /**
         * Source event name (must equal `event_name`).
         */
        metric: string;
        /**
         * Numeric property summed/averaged for `sum`/`avg`/quantile aggregations.
         */
        valueLabel?: string;
        /**
         * Label filters on the event.
         */
        filters?: Array<{
            /**
             * Event property / label identifier.
             */
            label: string;
            /**
             * Match operator (`=~`/`!~` are regex).
             */
            op: '=' | '!=' | '=~' | '!~';
            /**
             * Quoted filter value (coerced for numeric labels).
             */
            value: string;
        }>;
        /**
         * Optional group-by clause (ignored for experiment analysis).
         */
        groupBy?: {
            /**
             * `by` keeps the listed labels; `without` drops them.
             */
            op: 'by' | 'without';
            /**
             * Labels to group by (max 5).
             */
            labels: Array<string>;
        };
    };
    /**
     * Winsorise percentile (1–99) to clamp outliers. Defaults to 99.
     */
    winsorize_pct?: number;
    /**
     * Minimum detectable effect (relative, 0–1) for power planning. `null` to omit.
     */
    min_detectable_effect?: number | null;
    /**
     * Desired direction of movement. `higher_better` (default), `lower_better`, or `neutral` (guardrail).
     */
    direction?: 'higher_better' | 'lower_better' | 'neutral';
};

/**
 * Result of a successful metric create.
 */
export type CreateMetricResponse = {
    /**
     * Newly assigned metric id.
     */
    id: string;
    /**
     * Metric name that was created.
     */
    name: string;
};

/**
 * A metric definition.
 */
export type GetMetricResponse = {
    /**
     * Stable opaque metric id.
     */
    id: string;
    /**
     * Metric key.
     */
    name: string;
    /**
     * Folder grouping the metric, or `null`.
     */
    folder: string | null;
    /**
     * Source event name (camelCase in response).
     */
    eventName: string;
    /**
     * Legacy aggregation enum derived from the IR (`count_users`, `sum`, `ratio`, …).
     */
    aggregation: string;
    /**
     * Numeric value label for sum/avg metrics, or `null`.
     */
    valuePath: string | null;
    /**
     * Rendered DSL text form of the query, or `null` if it could not be rendered.
     */
    query?: string | null;
    /**
     * Typed query IR stored for the metric.
     */
    queryIr?: {
        /**
         * Aggregation function applied to the source event.
         */
        agg: {
            kind: 'count_users';
        } | {
            kind: 'count_events';
        } | {
            kind: 'sum';
        } | {
            kind: 'avg';
        } | {
            kind: 'min';
        } | {
            kind: 'max';
        } | {
            kind: 'unique';
        } | {
            kind: 'quantile';
            /**
             * Quantile fraction (0.5 = p50 … 0.999 = p999).
             */
            p: 0.5 | 0.75 | 0.9 | 0.95 | 0.99 | 0.999;
        } | {
            kind: 'retention_Nd';
            /**
             * Retention window in days (1–90).
             */
            n: number;
        } | {
            kind: 'ratio';
            /**
             * Numerator or denominator arm of a `ratio` aggregation.
             */
            numerator: {
                /**
                 * Counting mode for this ratio arm.
                 */
                agg: 'count_users' | 'count_events';
                /**
                 * Source event name for this ratio arm.
                 */
                metric: string;
                /**
                 * Optional label filters.
                 */
                filters?: Array<{
                    /**
                     * Event property / label identifier.
                     */
                    label: string;
                    /**
                     * Match operator (`=~`/`!~` are regex).
                     */
                    op: '=' | '!=' | '=~' | '!~';
                    /**
                     * Quoted filter value (coerced for numeric labels).
                     */
                    value: string;
                }>;
            };
            /**
             * Numerator or denominator arm of a `ratio` aggregation.
             */
            denominator: {
                /**
                 * Counting mode for this ratio arm.
                 */
                agg: 'count_users' | 'count_events';
                /**
                 * Source event name for this ratio arm.
                 */
                metric: string;
                /**
                 * Optional label filters.
                 */
                filters?: Array<{
                    /**
                     * Event property / label identifier.
                     */
                    label: string;
                    /**
                     * Match operator (`=~`/`!~` are regex).
                     */
                    op: '=' | '!=' | '=~' | '!~';
                    /**
                     * Quoted filter value (coerced for numeric labels).
                     */
                    value: string;
                }>;
            };
        };
        /**
         * Source event name (must equal `event_name`).
         */
        metric: string;
        /**
         * Numeric property summed/averaged for `sum`/`avg`/quantile aggregations.
         */
        valueLabel?: string;
        /**
         * Label filters on the event.
         */
        filters?: Array<{
            /**
             * Event property / label identifier.
             */
            label: string;
            /**
             * Match operator (`=~`/`!~` are regex).
             */
            op: '=' | '!=' | '=~' | '!~';
            /**
             * Quoted filter value (coerced for numeric labels).
             */
            value: string;
        }>;
        /**
         * Optional group-by clause (ignored for experiment analysis).
         */
        groupBy?: {
            /**
             * `by` keeps the listed labels; `without` drops them.
             */
            op: 'by' | 'without';
            /**
             * Labels to group by (max 5).
             */
            labels: Array<string>;
        };
    };
    /**
     * Desired direction of movement. `higher_better` (default), `lower_better`, or `neutral` (guardrail).
     */
    direction?: 'higher_better' | 'lower_better' | 'neutral';
    /**
     * Winsorise percentile applied to the metric.
     */
    winsorizePct?: number;
    /**
     * Configured MDE, or `null`.
     */
    minDetectableEffect?: number | null;
    /**
     * ISO-8601 creation timestamp.
     */
    createdAt?: string;
    /**
     * ISO-8601 last-update timestamp.
     */
    updatedAt?: string;
    [key: string]: unknown;
};

/**
 * Soft-delete (archive) acknowledgement.
 */
export type DeleteMetricResponse = {
    ok: true;
};

/**
 * Metric query DSL string, e.g. `sum(purchase, amount)`. Provide this OR `query_ir`.
 */
export type Query = string;

/**
 * Typed query IR — the structured alternative to `query`. Provide this OR `query`.
 */
export type QueryIr = {
    /**
     * Aggregation function applied to the source event.
     */
    agg: {
        kind: 'count_users';
    } | {
        kind: 'count_events';
    } | {
        kind: 'sum';
    } | {
        kind: 'avg';
    } | {
        kind: 'min';
    } | {
        kind: 'max';
    } | {
        kind: 'unique';
    } | {
        kind: 'quantile';
        /**
         * Quantile fraction (0.5 = p50 … 0.999 = p999).
         */
        p: 0.5 | 0.75 | 0.9 | 0.95 | 0.99 | 0.999;
    } | {
        kind: 'retention_Nd';
        /**
         * Retention window in days (1–90).
         */
        n: number;
    } | {
        kind: 'ratio';
        /**
         * Numerator or denominator arm of a `ratio` aggregation.
         */
        numerator: {
            /**
             * Counting mode for this ratio arm.
             */
            agg: 'count_users' | 'count_events';
            /**
             * Source event name for this ratio arm.
             */
            metric: string;
            /**
             * Optional label filters.
             */
            filters?: Array<{
                /**
                 * Event property / label identifier.
                 */
                label: string;
                /**
                 * Match operator (`=~`/`!~` are regex).
                 */
                op: '=' | '!=' | '=~' | '!~';
                /**
                 * Quoted filter value (coerced for numeric labels).
                 */
                value: string;
            }>;
        };
        /**
         * Numerator or denominator arm of a `ratio` aggregation.
         */
        denominator: {
            /**
             * Counting mode for this ratio arm.
             */
            agg: 'count_users' | 'count_events';
            /**
             * Source event name for this ratio arm.
             */
            metric: string;
            /**
             * Optional label filters.
             */
            filters?: Array<{
                /**
                 * Event property / label identifier.
                 */
                label: string;
                /**
                 * Match operator (`=~`/`!~` are regex).
                 */
                op: '=' | '!=' | '=~' | '!~';
                /**
                 * Quoted filter value (coerced for numeric labels).
                 */
                value: string;
            }>;
        };
    };
    /**
     * Source event name (must equal `event_name`).
     */
    metric: string;
    /**
     * Numeric property summed/averaged for `sum`/`avg`/quantile aggregations.
     */
    valueLabel?: string;
    /**
     * Label filters on the event.
     */
    filters?: Array<{
        /**
         * Event property / label identifier.
         */
        label: string;
        /**
         * Match operator (`=~`/`!~` are regex).
         */
        op: '=' | '!=' | '=~' | '!~';
        /**
         * Quoted filter value (coerced for numeric labels).
         */
        value: string;
    }>;
    /**
     * Optional group-by clause (ignored for experiment analysis).
     */
    groupBy?: {
        /**
         * `by` keeps the listed labels; `without` drops them.
         */
        op: 'by' | 'without';
        /**
         * Labels to group by (max 5).
         */
        labels: Array<string>;
    };
};

/**
 * Winsorise percentile (1–99) to clamp outliers. Defaults to 99.
 */
export type WinsorizePct = number;

/**
 * Minimum detectable effect (relative, 0–1) for power planning. `null` to omit.
 */
export type MinDetectableEffect = number | null;

/**
 * Desired direction of movement. `higher_better` (default), `lower_better`, or `neutral` (guardrail).
 */
export type Direction = 'higher_better' | 'lower_better' | 'neutral';

/**
 * Body for `PATCH /api/admin/metrics/{id}`. Every field is optional (`name` is immutable); provide at most one of `query` / `query_ir`.
 */
export type UpdateMetricRequest = {
    folder?: Folder;
    /**
     * Source event the query reads from.
     */
    event_name?: string;
    query?: Query;
    query_ir?: QueryIr;
    winsorize_pct?: WinsorizePct;
    min_detectable_effect?: MinDetectableEffect;
    direction?: Direction;
};

/**
 * Every catalogued event in the project (including pending auto-discovered names).
 */
export type ListEventsResponse = Array<{
    /**
     * Stable opaque event id.
     */
    id: string;
    /**
     * Event name — the handle metric queries reference.
     */
    name: string;
    /**
     * Folder the event is filed under, or `null` if at the root.
     */
    folder?: string | null | null;
    /**
     * Human-readable description, or `null` if none set.
     */
    description: string | null;
    /**
     * Typed properties declared on the event.
     */
    properties: Array<{
        /**
         * Property key, e.g. `amount` or `plan`.
         */
        name: string;
        /**
         * Value type of the property as recorded on the event.
         */
        type: 'string' | 'number' | 'boolean';
        /**
         * Whether the property must be present on every emitted event.
         */
        required?: boolean;
        /**
         * Human-readable description of the property.
         */
        description?: string;
    }>;
    /**
     * `1` if this is an auto-discovered name awaiting approval (metrics on it fail until approved), `0` if approved/usable.
     */
    pending: number;
    /**
     * ISO-8601 timestamp of creation.
     */
    createdAt: string;
}>;

/**
 * Body for `POST /api/admin/events`. Only `name` is required.
 */
export type CreateEventRequest = {
    /**
     * Event name. Starts with a letter, digit, or `_`; letters, digits, `_`, `-`, `.`; max 128 chars. Immutable after create — this is the handle metric queries reference.
     */
    name: string;
    folder?: Folder;
    /**
     * Optional human-readable description of the event.
     */
    description?: string;
    /**
     * Typed properties declared on the event. Defaults to an empty list.
     */
    properties?: Array<{
        /**
         * Property key, e.g. `amount` or `plan`.
         */
        name: string;
        /**
         * Value type of the property as recorded on the event.
         */
        type: 'string' | 'number' | 'boolean';
        /**
         * Whether the property must be present on every emitted event.
         */
        required?: boolean;
        /**
         * Human-readable description of the property.
         */
        description?: string;
    }>;
};

/**
 * Result of creating (or approving an existing pending) event.
 */
export type CreateEventResponse = {
    /**
     * Newly assigned event id.
     */
    id: string;
    /**
     * The event name that was registered.
     */
    name: string;
};

/**
 * A catalogued event.
 */
export type GetEventResponse = {
    /**
     * Stable opaque event id.
     */
    id: string;
    /**
     * Event name — the handle metric queries reference.
     */
    name: string;
    /**
     * Folder the event is filed under, or `null` if at the root.
     */
    folder?: string | null | null;
    /**
     * Human-readable description, or `null` if none set.
     */
    description: string | null;
    /**
     * Typed properties declared on the event.
     */
    properties: Array<{
        /**
         * Property key, e.g. `amount` or `plan`.
         */
        name: string;
        /**
         * Value type of the property as recorded on the event.
         */
        type: 'string' | 'number' | 'boolean';
        /**
         * Whether the property must be present on every emitted event.
         */
        required?: boolean;
        /**
         * Human-readable description of the property.
         */
        description?: string;
    }>;
    /**
     * `1` if this is an auto-discovered name awaiting approval (metrics on it fail until approved), `0` if approved/usable.
     */
    pending: number;
    /**
     * ISO-8601 timestamp of creation.
     */
    createdAt: string;
};

/**
 * Confirmation that the event was archived.
 */
export type DeleteEventResponse = {
    ok: true;
};

/**
 * Body for `PATCH /api/admin/events/{id}` and `POST /api/admin/events/{id}/approve`. All fields optional; `name` is immutable after create.
 */
export type UpdateEventRequest = {
    folder?: Folder;
    /**
     * New description for the event.
     */
    description?: string;
    /**
     * Replaces the full property set (no merge). Omit to leave properties unchanged.
     */
    properties?: Array<{
        /**
         * Property key, e.g. `amount` or `plan`.
         */
        name: string;
        /**
         * Value type of the property as recorded on the event.
         */
        type: 'string' | 'number' | 'boolean';
        /**
         * Whether the property must be present on every emitted event.
         */
        required?: boolean;
        /**
         * Human-readable description of the property.
         */
        description?: string;
    }>;
};

/**
 * Result of updating or approving an event.
 */
export type UpdateEventResponse = {
    /**
     * Event id that was updated.
     */
    id: string;
};

/**
 * Body for `PATCH /api/admin/events/{id}` and `POST /api/admin/events/{id}/approve`. All fields optional; `name` is immutable after create.
 */
export type ApproveEventRequest = {
    folder?: Folder;
    /**
     * New description for the event.
     */
    description?: string;
    /**
     * Replaces the full property set (no merge). Omit to leave properties unchanged.
     */
    properties?: Array<{
        /**
         * Property key, e.g. `amount` or `plan`.
         */
        name: string;
        /**
         * Value type of the property as recorded on the event.
         */
        type: 'string' | 'number' | 'boolean';
        /**
         * Whether the property must be present on every emitted event.
         */
        required?: boolean;
        /**
         * Human-readable description of the property.
         */
        description?: string;
    }>;
};

/**
 * Result of updating or approving an event.
 */
export type ApproveEventResponse = {
    /**
     * Event id that was updated.
     */
    id: string;
};

/**
 * A page of queue items, newest first.
 */
export type ListOpsItemsResponse = Array<{
    /**
     * Stable opaque item id.
     */
    id: string;
    /**
     * Per-project item number (the `#7` handle), or `null` if unnumbered.
     */
    number: number | null;
    /**
     * Queue item type. `bug` and `feature_request` are user-fileable; `error`, `alert`, and `measure_plan` tickets are auto-filed by the platform (a tracked production error, a metric-threshold alert, and an assistant-proposed measurement plan respectively).
     */
    type: 'bug' | 'feature_request' | 'error' | 'alert' | 'measure_plan';
    /**
     * One-line item title.
     */
    title: string;
    /**
     * Lifecycle status of a queue item.
     */
    status: 'open' | 'triaged' | 'in_progress' | 'ready_for_qa' | 'resolved' | 'wont_fix';
    /**
     * Triage priority, or `null` if not yet set.
     */
    priority: 'nice_to_have' | 'medium' | 'high' | 'critical' | null;
    /**
     * Source reference for auto-filed tickets (e.g. an error fingerprint).
     */
    sourceRef?: string | null;
    /**
     * ISO-8601 creation timestamp.
     */
    createdAt: string;
    [key: string]: unknown;
}>;

/**
 * Delivery target for a notification; `null` = use the project default.
 */
export type NotificationTarget = {
    slackChannel?: {
        id: string;
        name: string;
    } | null;
    email?: string | null;
} | null;

/**
 * Bug-kind fields for `POST /api/admin/ops` (sent with `type: bug`).
 */
export type CreateBugRequest = {
    /**
     * Discriminator — files a bug.
     */
    type: 'bug';
    /**
     * One-line bug title.
     */
    title: string;
    /**
     * How to reproduce the bug.
     */
    stepsToReproduce?: string;
    /**
     * What actually happened.
     */
    actualResult?: string;
    /**
     * What was expected instead.
     */
    expectedResult?: string;
    /**
     * Initial triage priority, or `null`.
     */
    priority?: 'nice_to_have' | 'medium' | 'high' | 'critical' | null;
    /**
     * Email of the reporter, or `null`.
     */
    reporterEmail?: string | null;
    /**
     * URL of the page the bug relates to, or `null`.
     */
    pageUrl?: string | null;
    /**
     * Reporter's user-agent string, or `null`.
     */
    userAgent?: string | null;
    /**
     * Reporter's viewport (e.g. `1280x720`), or `null`.
     */
    viewport?: string | null;
    /**
     * Arbitrary capture context, or `null`.
     */
    context?: {
        [key: string]: unknown;
    } | null;
    /**
     * Where this bug's completion notification lands.
     */
    notify?: NotificationTarget | null;
};

/**
 * Feature-request-kind fields for `POST /api/admin/ops` (sent with `type: feature_request`).
 */
export type CreateFeatureRequestRequest = {
    /**
     * Discriminator — files a feature request.
     */
    type: 'feature_request';
    /**
     * One-line feature-request title.
     */
    title: string;
    /**
     * What the feature is.
     */
    description?: string;
    /**
     * Why it's needed / the use case.
     */
    useCase?: string;
    /**
     * Initial triage priority, or `null`.
     */
    priority?: 'nice_to_have' | 'medium' | 'high' | 'critical' | null;
    /**
     * Email of the reporter, or `null`.
     */
    reporterEmail?: string | null;
    /**
     * URL of the page the request relates to, or `null`.
     */
    pageUrl?: string | null;
    /**
     * Reporter's user-agent string, or `null`.
     */
    userAgent?: string | null;
    /**
     * Arbitrary capture context, or `null`.
     */
    context?: {
        [key: string]: unknown;
    } | null;
    /**
     * Where this request's completion notification lands.
     */
    notify?: NotificationTarget | null;
};

/**
 * Body for `POST /api/admin/ops`. A discriminated union on `type`: `bug` carries the bug fields, `feature_request` the feature fields. Only these two user-fileable types are accepted — `error`, `alert`, and `measure_plan` tickets are auto-filed by the platform and cannot be created over the API.
 */
export type CreateOpsItemRequest = ({
    type: 'bug';
} & CreateBugRequest) | ({
    type: 'feature_request';
} & CreateFeatureRequestRequest);

/**
 * Response for `POST /api/admin/ops`.
 */
export type CreateOpsItemResponse = {
    /**
     * Newly created item id.
     */
    id: string;
    /**
     * Per-project item number assigned to the new item.
     */
    number?: number | null;
};

/**
 * One queue item, any of the five types. Shared fields apply to all; `stepsToReproduce`/`actualResult`/`expectedResult`/`viewport` are bug-specific, `description`/`useCase` feature-specific, and `context` carries the per-type payload for auto-filed `error`/`alert`/`measure_plan` tickets.
 */
export type GetOpsItemResponse = {
    /**
     * Stable opaque item id.
     */
    id: string;
    /**
     * Per-project item number (the `#7` handle), or `null` if unnumbered.
     */
    number: number | null;
    /**
     * Queue item type. `bug` and `feature_request` are user-fileable; `error`, `alert`, and `measure_plan` tickets are auto-filed by the platform (a tracked production error, a metric-threshold alert, and an assistant-proposed measurement plan respectively).
     */
    type: 'bug' | 'feature_request' | 'error' | 'alert' | 'measure_plan';
    /**
     * One-line item title (all types).
     */
    title: string;
    /**
     * Lifecycle status of a queue item (all types).
     */
    status: 'open' | 'triaged' | 'in_progress' | 'ready_for_qa' | 'resolved' | 'wont_fix';
    /**
     * Triage priority, or `null` if not yet set (all types).
     */
    priority: 'nice_to_have' | 'medium' | 'high' | 'critical' | null;
    /**
     * How the item was filed: `team` (a human admin/teammate — bug/feature) or `system` (auto-filed — error/alert/measure_plan).
     */
    source?: 'team' | 'system';
    /**
     * Stable key of the originating record for auto-filed tickets (error fingerprint, `<alert source>:<dedupeKey>`, or measurement-plan ref); `null` for human-filed bugs/features.
     */
    sourceRef?: string | null;
    /**
     * Reporter email (bug/feature), or `null`.
     */
    reporterEmail?: string | null;
    /**
     * URL the item relates to (bug/feature), or `null`.
     */
    pageUrl?: string | null;
    /**
     * Reporter user-agent (bug/feature), or `null`.
     */
    userAgent?: string | null;
    /**
     * Reproduction steps — populated for `bug`, empty string otherwise.
     */
    stepsToReproduce?: string;
    /**
     * What actually happened — `bug` only, empty string otherwise.
     */
    actualResult?: string;
    /**
     * What was expected — `bug` only, empty string otherwise.
     */
    expectedResult?: string;
    /**
     * Reporter viewport — `bug` only, else `null`.
     */
    viewport?: string | null;
    /**
     * Feature description — populated for `feature_request`, empty string otherwise.
     */
    description?: string;
    /**
     * Feature use case — `feature_request` only, empty string otherwise.
     */
    useCase?: string;
    /**
     * Type-specific capture context (e.g. the error/alert/measure_plan details for auto-filed tickets), or `null`.
     */
    context?: {
        [key: string]: unknown;
    } | null;
    /**
     * Connector linkage (e.g. the opened GitHub issue / linked PR), or `null`.
     */
    connectorData?: {
        [key: string]: unknown;
    } | null;
    /**
     * Per-item completion-notification target, or `null` (falls back to the project default).
     */
    notify?: NotificationTarget | null;
    /**
     * ISO-8601 creation timestamp.
     */
    createdAt: string;
    /**
     * ISO-8601 last-update timestamp.
     */
    updatedAt?: string;
    [key: string]: unknown;
};

/**
 * Lifecycle status of a queue item.
 */
export type OpsItemStatus = 'open' | 'triaged' | 'in_progress' | 'ready_for_qa' | 'resolved' | 'wont_fix';

/**
 * Triage priority, or `null` to clear it.
 */
export type OpsItemPriorityOrNull = 'nice_to_have' | 'medium' | 'high' | 'critical' | null;

/**
 * Where this item's completion notification lands, or `null`.
 */
export type OpsItemNotifyOrNull = NotificationTarget | null;

/**
 * Content + triage update for a `bug` item (`PATCH /api/admin/ops/{handle}` when the item is a bug).
 */
export type UpdateBugRequest = {
    /**
     * New bug title.
     */
    title?: string;
    /**
     * Updated reproduction steps.
     */
    stepsToReproduce?: string;
    /**
     * Updated actual result.
     */
    actualResult?: string;
    /**
     * Updated expected result.
     */
    expectedResult?: string;
    status?: OpsItemStatus;
    priority?: OpsItemPriorityOrNull;
    /**
     * Link (or, when `null`, unlink) a GitHub pull request to this bug.
     */
    githubPrNumber?: number | null;
    notify?: OpsItemNotifyOrNull;
};

/**
 * Content + triage update for a `feature_request` item (`PATCH /api/admin/ops/{handle}` when the item is a feature request).
 */
export type UpdateFeatureRequestRequest = {
    /**
     * New feature-request title.
     */
    title?: string;
    /**
     * Updated description.
     */
    description?: string;
    /**
     * Updated use case.
     */
    useCase?: string;
    status?: OpsItemStatus;
    priority?: OpsItemPriorityOrNull;
    /**
     * Link (or, when `null`, unlink) a GitHub pull request to this request.
     */
    githubPrNumber?: number | null;
    notify?: OpsItemNotifyOrNull;
};

/**
 * Triage-only update — the shape accepted for any item, and the ONLY shape for auto-filed `error`/`alert`/`measure_plan` tickets (their content is not user-editable).
 */
export type UpdateOpsItemStatusRequest = {
    status?: OpsItemStatus;
    priority?: OpsItemPriorityOrNull;
    notify?: OpsItemNotifyOrNull;
};

/**
 * Body for `PATCH /api/admin/ops/{handle}`. The body is type-less — the server validates it against the stored item's type: a `bug` accepts the bug content fields, a `feature_request` the feature fields, and `error`/`alert`/`measure_plan` only `status`/`priority`/`notify`. Pass at least one field.
 */
export type UpdateOpsItemRequest = UpdateBugRequest | UpdateFeatureRequestRequest | UpdateOpsItemStatusRequest;

/**
 * Response for the update / link-pr endpoints.
 */
export type UpdateOpsItemResponse = {
    /**
     * Item id that was updated.
     */
    id: string;
};

/**
 * Body for `POST /api/admin/ops/{handle}/link-pr`.
 */
export type LinkPrToOpsItemRequest = {
    /**
     * PR number to record on the item. `null` unlinks the PR.
     */
    prNumber: number | null;
    /**
     * Explicit PR URL. Required for error/alert tickets (no GitHub issue to derive the URL from).
     */
    prUrl?: string;
};

/**
 * Response for the update / link-pr endpoints.
 */
export type LinkPrToOpsItemResponse = {
    /**
     * Item id that was updated.
     */
    id: string;
};

/**
 * Body for `POST /api/admin/notifications`.
 */
export type NotifyOpsRequest = {
    /**
     * One-line headline of what's blocked.
     */
    title: string;
    /**
     * One sentence: why it can't be fixed in code.
     */
    summary: string;
    /**
     * Ordered steps the human should take to unblock.
     */
    steps?: Array<string>;
    /**
     * Dashboard-relative deep link to the related item.
     */
    href?: string;
    /**
     * Stable per-escalation key (e.g. `feedback:7`) so re-runs dedupe to one row.
     */
    dedupeKey?: string;
};

/**
 * Response for `POST /api/admin/notifications`.
 */
export type NotifyOpsResponse = {
    /**
     * The dedupe key the escalation was recorded under.
     */
    dedupeKey: string;
    /**
     * `true` if a new escalation was dispatched; `false` on an idempotent repeat.
     */
    dispatched: boolean;
};

/**
 * Response for `GET /api/admin/slack/channels`.
 */
export type ListSlackChannelsResponse = {
    /**
     * Whether a Slack connector is connected and authenticated.
     */
    connected: boolean;
    /**
     * The project's Slack channels (empty when no Slack is connected).
     */
    channels: Array<{
        /**
         * Slack channel id.
         */
        id: string;
        /**
         * Slack channel name (without the leading `#`).
         */
        name: string;
        /**
         * Whether the channel is private.
         */
        isPrivate?: boolean;
    }>;
};

export type ListAlertRulesResponse = Array<{
    /**
     * Stable opaque alert-rule id.
     */
    id: string;
    /**
     * Human label for the rule.
     */
    name: string;
    /**
     * Id of the metric the rule evaluates. Immutable after create.
     */
    metricId: string;
    /**
     * Display name of the bound metric, or `null` if the metric no longer exists.
     */
    metricName: string | null;
    /**
     * How the metric value is compared to the threshold (gt/gte/lt/lte).
     */
    comparator: 'gt' | 'gte' | 'lt' | 'lte';
    /**
     * Threshold the metric value is compared against.
     */
    threshold: number;
    /**
     * Lookback window (hours) the metric is aggregated over.
     */
    windowHours: number;
    /**
     * Severity of the raised alert.
     */
    severity: 'danger' | 'warn' | 'info';
    /**
     * Whether the rule is evaluated by the cron.
     */
    enabled: boolean;
    notify: NotificationTarget;
    /**
     * ISO-8601 timestamp of creation.
     */
    createdAt: string;
    /**
     * ISO-8601 timestamp of the last update.
     */
    updatedAt: string;
}>;

export type CreateAlertRuleRequest = {
    /**
     * Human label for the rule, shown on the alert and the rules list.
     */
    name: string;
    /**
     * Id of the metric to evaluate.
     */
    metricId: string;
    /**
     * How the metric value is compared to the threshold (gt/gte/lt/lte).
     */
    comparator: 'gt' | 'gte' | 'lt' | 'lte';
    /**
     * Threshold the metric value is compared against.
     */
    threshold: number;
    /**
     * Lookback window (hours) the metric is aggregated over. 1–720.
     */
    windowHours?: number;
    /**
     * Severity of the raised alert.
     */
    severity?: 'danger' | 'warn' | 'info';
    /**
     * Whether the rule is evaluated by the cron.
     */
    enabled?: boolean;
    notify?: NotificationTarget;
};

export type CreateAlertRuleResponse = {
    /**
     * Newly assigned alert-rule id.
     */
    id: string;
};

export type DeleteAlertRuleResponse = {
    ok: true;
};

export type UpdateAlertRuleRequest = {
    name?: string;
    comparator?: 'gt' | 'gte' | 'lt' | 'lte';
    threshold?: number;
    windowHours?: number;
    severity?: 'danger' | 'warn' | 'info';
    enabled?: boolean;
    notify?: NotificationTarget;
};

export type UpdateAlertRuleResponse = {
    /**
     * Newly assigned alert-rule id.
     */
    id: string;
};

/**
 * The project the caller's auth header resolves to. The shape is open — additional project fields may be present.
 */
export type GetCurrentProjectResponse = {
    /**
     * Stable opaque project id.
     */
    id: string;
    /**
     * Project name.
     */
    name: string;
    /**
     * Project domain, or `null` if unset.
     */
    domain: string | null;
    /**
     * Email of the account that owns the project.
     */
    ownerEmail: string;
    /**
     * Billing plan tier.
     */
    plan: 'free' | 'paid';
    /**
     * Project lifecycle status.
     */
    status: 'active' | 'inactive';
    /**
     * Stripe subscription status (`none`, `active`, `trialing`, `past_due`, …).
     */
    subscriptionStatus: string;
    /**
     * Billing cadence.
     */
    billingInterval: 'monthly' | 'annual';
    /**
     * ISO-8601 end of the current billing period, or `null`.
     */
    currentPeriodEnd: string | null;
    /**
     * ISO-8601 trial end, or `null` if not trialing.
     */
    trialEndsAt: string | null;
    /**
     * `1` if the subscription is set to cancel at period end, else `0`.
     */
    cancelAtPeriodEnd: number;
    /**
     * Whether the i18n/translations module is enabled.
     */
    moduleTranslations: boolean | number;
    /**
     * Whether the dynamic-configs module is enabled.
     */
    moduleConfigs: boolean | number;
    /**
     * Whether the feature-gates module is enabled.
     */
    moduleGates: boolean | number;
    /**
     * Whether the experiments module is enabled.
     */
    moduleExperiments: boolean | number;
    /**
     * Whether the feedback/ops module is enabled.
     */
    moduleFeedback: boolean | number;
    /**
     * ISO-8601 timestamp of project creation.
     */
    createdAt: string;
    /**
     * ISO-8601 timestamp of last update.
     */
    updatedAt: string;
    [key: string]: unknown;
};

/**
 * Body for `POST /api/admin/projects/upsert`. Only `domain` is required; the owner is resolved from the caller's session.
 */
export type UpsertProjectRequest = {
    /**
     * Hostname-like project identifier (e.g. `acme.com`). Use `*` to allow any origin. The project is keyed by `(owner_email, domain)`, so a second call with the same domain returns the existing project.
     */
    domain: string;
    /**
     * Human-readable project name. Defaults to the domain on first create.
     */
    name?: string;
};

/**
 * Result of `POST /api/admin/projects/upsert`.
 */
export type UpsertProjectResponse = {
    /**
     * Stable opaque project id.
     */
    id: string;
    /**
     * Project name (the supplied `name`, or the domain on first create).
     */
    name: string;
    /**
     * Project domain, or `null` if unset.
     */
    domain: string | null;
    /**
     * Email of the account that owns the project.
     */
    owner_email: string;
    /**
     * `true` if this call created the project, `false` if it returned an existing one.
     */
    created: boolean;
};

export type ListI18nProfilesResponse = Array<{
    /**
     * Stable opaque profile id.
     */
    id: string;
    /**
     * Profile handle, e.g. `en:prod` or `fr:prod`.
     */
    name: string;
    /**
     * `1` for the project's single default profile (always seeded as `en:prod`), else `0`.
     */
    isDefault?: number;
    /**
     * ISO-8601 timestamp of creation.
     */
    createdAt?: string;
    /**
     * ISO-8601 soft-delete timestamp, or `null` if live.
     */
    deletedAt?: string | null;
}>;

/**
 * Body for `POST /api/admin/i18n/profiles`. Only `name` is accepted.
 */
export type CreateI18nProfileRequest = {
    /**
     * Profile handle to create, e.g. `en:prod` or `fr:prod`. Lowercase alphanumeric start, then letters/digits/`_`/`:`/`.`/`-`; max 64 chars. The locale is encoded in the handle, so no separate locale fields are accepted.
     */
    name: string;
};

/**
 * Created profile id + name.
 */
export type CreateI18nProfileResponse = {
    /**
     * Newly assigned profile id.
     */
    id: string;
    /**
     * Profile handle that was created.
     */
    name: string;
};

/**
 * A page of i18n keys plus the total count. (The client also tolerates a bare array.)
 */
export type ListI18nKeysResponse = {
    /**
     * The page of matching keys.
     */
    keys: Array<{
        /**
         * Stable opaque key id.
         */
        id: string;
        /**
         * Dotted key path, e.g. `home.cta`.
         */
        key: string;
        /**
         * Current translated value for the key.
         */
        value: string;
        /**
         * Optional human note stored with the key.
         */
        description?: string | null;
        /**
         * `{{var}}` placeholder names in the value, or `null` when there are none.
         */
        variables?: Array<string> | null;
        /**
         * Owning profile id.
         */
        profileId?: string;
        /**
         * Owning chunk (authoring grouping) id.
         */
        chunkId?: string;
        /**
         * ISO-8601 timestamp of the last edit.
         */
        updatedAt?: string;
        /**
         * Actor email that last edited the key.
         */
        updatedBy?: string;
        [key: string]: unknown;
    }>;
    /**
     * Total matching keys across all pages (ignores `limit`/`offset`).
     */
    total: number;
};

/**
 * Body for `POST /api/admin/i18n/keys`. Insert-only: keys that already exist are never overwritten — use `PUT /keys/{id}` to change a value.
 */
export type PushI18nKeysRequest = {
    /**
     * Target profile id to add keys to.
     */
    profile_id: string;
    /**
     * Logical grouping the new keys are filed under. Defaults to `default`.
     */
    chunk?: string;
    /**
     * Keys to add. Insert-only — existing keys are reported back as `skipped`.
     */
    keys: Array<{
        /**
         * Dotted key path, e.g. `home.cta`.
         */
        key: string;
        /**
         * Translated value for the key.
         */
        value: string;
        /**
         * Optional human note stored with the key.
         */
        description?: string;
        /**
         * Optional explicit list of `{{var}}` placeholder names in the value. Auto-derived from the value when omitted.
         */
        variables?: Array<string>;
    }>;
};

/**
 * Result of an insert-only key push.
 */
export type PushI18nKeysResponse = {
    /**
     * Key names that were newly inserted.
     */
    added: Array<string>;
    /**
     * Key names that already existed and were left untouched.
     */
    skipped: Array<string>;
    /**
     * Number of keys inserted (== `added.length`).
     */
    pushed_count: number;
    /**
     * Number of keys skipped (== `skipped.length`).
     */
    skipped_count: number;
    /**
     * The chunk the keys were filed under.
     */
    chunk?: string;
};

/**
 * Body for `PUT /api/admin/i18n/keys/{id}`. Updates one existing key's value.
 */
export type UpdateI18nKeyRequest = {
    /**
     * New value for the key (the only overwrite path).
     */
    value: string;
    /**
     * Optional human note to store with the key.
     */
    description?: string;
    /**
     * Explicit `{{var}}` placeholder names in the value. Omit to auto-derive them from the value.
     */
    variables?: Array<string>;
};

/**
 * Updated key id.
 */
export type UpdateI18nKeyResponse = {
    /**
     * Id of the key that was updated.
     */
    id: string;
};

export type ListI18nDraftsResponse = Array<{
    /**
     * Stable opaque draft id.
     */
    id: string;
    /**
     * Draft name, e.g. the target locale being staged.
     */
    name?: string;
    /**
     * Profile the draft targets.
     */
    profileId?: string;
    /**
     * Profile the draft was seeded from, or `null`.
     */
    sourceProfileId?: string | null;
    /**
     * Lifecycle state of the draft.
     */
    status?: 'open' | 'merged' | 'abandoned';
    /**
     * Actor email that created the draft.
     */
    createdBy?: string;
    /**
     * ISO-8601 timestamp of creation.
     */
    createdAt?: string;
    /**
     * ISO-8601 merge/publish timestamp, or `null`.
     */
    publishedAt?: string | null;
    [key: string]: unknown;
}>;

/**
 * Body for `POST /api/admin/i18n/drafts`. Stages a new translation draft against a target profile, optionally seeding from a source profile.
 */
export type CreateI18nDraftRequest = {
    /**
     * Draft name, e.g. the target locale being staged.
     */
    name: string;
    /**
     * Profile the draft targets.
     */
    profile_id: string;
    /**
     * Optional profile to seed the draft's keys from.
     */
    source_profile_id?: string;
};

/**
 * One staged translation draft.
 */
export type I18nDraft = {
    /**
     * Stable opaque draft id.
     */
    id: string;
    /**
     * Draft name, e.g. the target locale being staged.
     */
    name?: string;
    /**
     * Profile the draft targets.
     */
    profileId?: string;
    /**
     * Profile the draft was seeded from, or `null`.
     */
    sourceProfileId?: string | null;
    /**
     * Lifecycle state of the draft.
     */
    status?: 'open' | 'merged' | 'abandoned';
    /**
     * Actor email that created the draft.
     */
    createdBy?: string;
    /**
     * ISO-8601 timestamp of creation.
     */
    createdAt?: string;
    /**
     * ISO-8601 merge/publish timestamp, or `null`.
     */
    publishedAt?: string | null;
    [key: string]: unknown;
};

/**
 * Body for `PATCH /api/admin/i18n/drafts/{draftId}`. Transitions the draft's lifecycle state.
 */
export type UpdateI18nDraftRequest = {
    /**
     * New lifecycle state for the draft.
     */
    status?: 'open' | 'merged' | 'abandoned';
};

/**
 * Body for `POST /api/admin/i18n/profiles/{profileId}/publish`. The `chunk` is an audit label only.
 */
export type PublishI18nProfileRequest = {
    /**
     * Optional chunk label to stamp on the audit log. Publishing is profile-wide regardless — the whole profile is snapshotted into one KV blob.
     */
    chunk?: string;
};

/**
 * Result of a profile-wide publish.
 */
export type PublishI18nProfileResponse = {
    /**
     * Always `true` on success.
     */
    ok: true;
    /**
     * Profile that was published.
     */
    profile_id: string;
    /**
     * Audit chunk label, or `null` when none was given.
     */
    chunk: string | null;
    /**
     * ISO-8601 timestamp of the publish.
     */
    published_at: string;
    /**
     * New KV snapshot version that was shipped.
     */
    version: string;
    /**
     * Number of keys in the published snapshot.
     */
    key_count: number;
    /**
     * Whether the snapshot's contents actually changed since the last publish.
     */
    changed: boolean;
    /**
     * CDN purge outcome: `purged` ok, `skipped` (no creds), or `failed` (edge still stale).
     */
    purged: 'purged' | 'skipped' | 'failed';
    /**
     * Whether a KV read-back confirmed the new version persisted.
     */
    kv_verified: boolean;
    /**
     * Human-readable caveat when the publish landed but is not fully live.
     */
    warning?: string;
};

/**
 * Body for `POST /api/admin/i18n/set`. Upserts one key's value and publishes the profile live.
 */
export type SetI18nLabelRequest = {
    /**
     * Dotted key path to set, e.g. `home.cta`.
     */
    key: string;
    /**
     * New value for the key. Inserted when the key is new, overwritten when it exists.
     */
    value: string;
    /**
     * Profile name to target, e.g. `en:prod`. Omit to target the project's default-marked profile.
     */
    profile?: string;
    /**
     * Optional human note to store with the key.
     */
    description?: string;
};

/**
 * Result of a one-shot set-and-publish.
 */
export type SetI18nLabelResponse = {
    /**
     * Always `true` on success.
     */
    ok: true;
    /**
     * Name of the profile that was published.
     */
    profile: string;
    /**
     * Id of the profile that was published.
     */
    profile_id: string;
    /**
     * The key that was set.
     */
    key: string;
    /**
     * The value that was stored.
     */
    value: string;
    /**
     * ISO-8601 timestamp of the publish.
     */
    published_at: string;
    /**
     * New KV snapshot version that was shipped.
     */
    version: string;
    /**
     * Number of keys in the published snapshot.
     */
    key_count: number;
    /**
     * Whether the snapshot's contents actually changed since the last publish.
     */
    changed: boolean;
    /**
     * CDN purge outcome: `purged` ok, `skipped` (no creds), or `failed` (edge still stale).
     */
    purged: 'purged' | 'skipped' | 'failed';
    /**
     * Whether a KV read-back confirmed the new version persisted.
     */
    kv_verified: boolean;
    /**
     * Human-readable caveat when the publish landed but is not fully live.
     */
    warning?: string;
};

/**
 * A tracked production error — one row per distinct issue, keyed by `fingerprint`. Rows are never created by hand: an ingestion path (worker log drain / the `see()` SDK reporter) folds each occurrence into the matching row, bumping `count` and `lastSeenAt`. The admin surface only lists them, reads one, and flips `status`.
 *
 * Field names are camelCase (the D1 row projected through Drizzle). Many columns are nullable because the reporting source may not supply them.
 */
export type ErrorRecord = {
    /**
     * Stable opaque error id.
     */
    id: string;
    /**
     * Project this issue belongs to.
     */
    projectId: string;
    /**
     * Stable dedupe key (hash of `errorType` + normalized `message` + consequence). Unique per project — every occurrence with the same fingerprint folds into this one row.
     */
    fingerprint: string;
    /**
     * Fingerprint of the issue this one descends from — set when `see()` reported the same error (re-throw) or its `{ cause }` (wrap) at an inner boundary first. Points at another error's `fingerprint` in the same project (soft reference, no FK). `null` for a root issue.
     */
    causedByFingerprint?: string | null;
    /**
     * Error message text.
     */
    message: string;
    /**
     * Error class/name, e.g. `TypeError`. `null` when the source didn't supply one.
     */
    errorType?: string | null;
    /**
     * Stack trace of the latest occurrence, or `null` if none was captured.
     */
    stack?: string | null;
    /**
     * Where it surfaced — Worker name (`shipeasy`, `shipeasy-worker`) or `sdk-client` / `sdk-server`. `null` if unknown.
     */
    source?: string | null;
    /**
     * Latest occurrence's raw URL (with ids intact), or `null`.
     */
    url?: string | null;
    /**
     * Distinct, id-normalized route templates this issue has surfaced on, as a JSON-encoded string array (e.g. `["https://app/dashboard/#/gates"]`). UUIDs / numeric ids / opaque tokens are collapsed to `#` so the same route under different ids counts once. `null` if none recorded.
     */
    seenUrls?: string | null;
    /**
     * Consequence subject — `<errorType> causes the <subject> to <outcome>`. `null` if no consequence was reported.
     */
    subject?: string | null;
    /**
     * Consequence outcome — see `subject`. `null` if no consequence was reported.
     */
    outcome?: string | null;
    /**
     * Which SDK side reported it — `client` or `server`. `null` if unknown.
     */
    side?: string | null;
    /**
     * Published env the reporting SDK ran against (e.g. `dev`, `staging`, `prod`). `null` if unknown.
     */
    env?: string | null;
    /**
     * `see()` error kind. `null` when the source didn't classify the occurrence.
     */
    kind?: 'caught' | 'uncaught' | 'unhandled_rejection' | 'network' | 'violation' | null;
    /**
     * Latest occurrence's sanitized extras, JSON-encoded. `null` if none.
     */
    lastExtrasJson?: string | null;
    /**
     * `@shipeasy/sdk` version of the latest occurrence, or `null`.
     */
    sdkVersion?: string | null;
    /**
     * Number of folded occurrences for this fingerprint.
     */
    count: number;
    /**
     * Triage state. `open` is the default; a `resolved` error reopens automatically (ingestion-side) if it recurs; `ignored` is sticky until flipped back here.
     */
    status: 'open' | 'resolved' | 'ignored';
    /**
     * ISO-8601 timestamp of the first folded occurrence.
     */
    firstSeenAt: string;
    /**
     * ISO-8601 timestamp of the most recent folded occurrence. Rows are ordered by this, descending.
     */
    lastSeenAt: string;
    /**
     * ISO-8601 timestamp the row was created.
     */
    createdAt: string;
    /**
     * ISO-8601 timestamp of the last mutation (e.g. a status flip).
     */
    updatedAt: string;
};

/**
 * A bare JSON array of tracked errors, ordered by `lastSeenAt` descending. There is no pagination envelope — `limit` caps the page size.
 */
export type ListErrorsResponse = Array<ErrorRecord>;

/**
 * Body for `PATCH /api/admin/errors/{id}`. The only mutable field is `status` (open ⇄ resolved ⇄ ignored).
 */
export type UpdateErrorStatusRequest = {
    /**
     * New triage state. `resolved` reopens automatically on recurrence; `ignored` is sticky.
     */
    status: 'open' | 'resolved' | 'ignored';
};

/**
 * The feedback ticket that tracks this error. Idempotent — if an open `error` ticket already tracks this fingerprint (hand- or auto-filed), the existing one is returned instead of creating a duplicate.
 */
export type FileErrorTicketResponse = {
    /**
     * Feedback ticket id.
     */
    id: string;
    /**
     * Human-facing per-project ticket number.
     */
    number: number;
};

/**
 * Time window for the occurrence series. Bounds are epoch **seconds** (Analytics Engine stores the occurrence timestamp in seconds). `to` must be strictly greater than `from`.
 */
export type ErrorSeriesRequest = {
    /**
     * Window start, epoch seconds (inclusive).
     */
    from: number;
    /**
     * Window end, epoch seconds (exclusive). Must be greater than `from`.
     */
    to: number;
    /**
     * Bucket width in seconds (60s–86400s/1d). Defaults to `3600` (hourly). Each returned point is floor-aligned to this width.
     */
    bucket?: number;
};

/**
 * Bucketed occurrence counts for one tracked error (by its fingerprint), plus the Analytics Engine SQL that produced them.
 */
export type ErrorSeriesResponse = {
    /**
     * The Analytics Engine SQL executed to produce `rows` (echoed for transparency / debugging).
     */
    sql: string;
    /**
     * Bucketed occurrence series, ordered by `t` ascending.
     */
    rows: Array<{
        /**
         * Bucket start, epoch seconds, floor-aligned to `bucket`.
         */
        t: number;
        /**
         * Occurrence count in the bucket (sample-interval weighted).
         */
        v: number;
        [key: string]: unknown;
    }>;
};

/**
 * Connector destination. OAuth/app providers (`google_sheets`, `github`, `slack`) dispatch the event payload to an external destination. Trigger providers (`claude_trigger`, `cursor_trigger`, `copilot_trigger`, `jules_trigger`) instead kick a coding-agent run that burns down the operational queue.
 */
export type ConnectorProvider = 'google_sheets' | 'github' | 'slack' | 'claude_trigger' | 'cursor_trigger' | 'copilot_trigger' | 'jules_trigger';

/**
 * Lifecycle event a connector subscribes to. `bug.created` fires when a new bug report lands in the feedback inbox; `feature_request.created` fires for a new feature request. A connector dispatches (posts to Slack / appends a Sheets row / files a GitHub Issue) — or, for trigger connectors, auto-fires a coding-agent run — once per subscribed event.
 */
export type ConnectorEvent = 'bug.created' | 'feature_request.created';

/**
 * A connector row. The encrypted credentials cipher backing the connector is intentionally never serialised.
 */
export type ConnectorRecord = {
    /**
     * Stable opaque connector id (a UUID).
     */
    id: string;
    /**
     * Id of the project the connector belongs to.
     */
    projectId: string;
    provider: ConnectorProvider;
    /**
     * Human-readable connector label shown in the dashboard.
     */
    name: string;
    /**
     * Whether the connector is active. OAuth providers are created `false` and flip to `true` only after the OAuth/config flow completes; trigger providers default to `true` on create. A disabled connector never dispatches and never auto-fires.
     */
    enabled: boolean;
    /**
     * Events this connector is subscribed to. Empty array = no auto-dispatch / no auto-fire (the connector can still be fired/tested manually).
     */
    events: Array<ConnectorEvent>;
    /**
     * Provider-specific, non-secret configuration (e.g. a Google Sheets `spreadsheetId`/`sheetTitle`, a Slack channel ref, or a trigger's repo coordinates + routine id). Secrets are never stored here — they live in an encrypted credentials cipher that is never returned by the API.
     */
    config: {
        [key: string]: unknown;
    };
    /**
     * Display label for the connected account / target (e.g. the OAuth account email, or a trigger's idempotency key such as its repo url or routine id). `null` until the connector is authenticated/configured.
     */
    accountLabel: string | null;
    /**
     * Error message from the most recent failed dispatch/fire attempt, or `null` if the last attempt succeeded (or none has run).
     */
    lastError: string | null;
    /**
     * ISO-8601 timestamp of the most recent dispatch/fire attempt, or `null` if none has run.
     */
    lastAttemptAt: string | null;
    /**
     * ISO-8601 timestamp of the most recent successful dispatch/fire, or `null` if none has succeeded. Preserved across later failures.
     */
    lastSuccessAt: string | null;
    /**
     * ISO-8601 timestamp of creation.
     */
    createdAt: string;
    /**
     * ISO-8601 timestamp of last mutation.
     */
    updatedAt: string;
};

/**
 * Bare array of every connector in the project (no pagination envelope).
 */
export type ListConnectorsResponse = Array<ConnectorRecord>;

/**
 * Create an OAuth/app connector stub. The connector is created `enabled: false` with empty `config` and no credentials; the provider's OAuth flow then attaches credentials and enables it.
 */
export type CreateOAuthConnectorRequest = {
    /**
     * OAuth/app connector destination.
     */
    provider: 'google_sheets' | 'github' | 'slack';
    /**
     * Human-readable connector label.
     */
    name: string;
    /**
     * Events to subscribe to. At least one is required.
     */
    events: Array<ConnectorEvent>;
};

/**
 * Non-secret config for a Claude trigger.
 */
export type ClaudeTriggerConfig = {
    /**
     * The Claude Code routine id this connector fires (the id recorded at setup). Idempotency key for the connector.
     */
    routineId: string;
    /**
     * Optional default prompt sent on a manual fire / when no event text applies.
     */
    fireText?: string;
};

/**
 * Create a Claude Code routine trigger in one shot. Idempotent by `config.routineId` — re-creating with the same routine id updates the existing row.
 */
export type CreateClaudeTriggerRequest = {
    /**
     * Discriminator. Claude Code scheduled-routine trigger.
     */
    provider: 'claude_trigger';
    /**
     * Human-readable connector label.
     */
    name?: string;
    /**
     * Events that auto-fire the routine. Defaults to empty so the trigger does not auto-fire paid runs until events are subscribed.
     */
    events?: Array<ConnectorEvent>;
    config: ClaudeTriggerConfig;
    /**
     * The routine's fire bearer token (secret). **Optional** — a tokenless trigger is recorded but not fireable until a token is added later. Encrypted into the credentials cipher; never persisted in `config` or returned.
     */
    token?: string;
    /**
     * Whether the trigger is active on create.
     */
    enabled?: boolean;
};

/**
 * Non-secret config for a Cursor trigger.
 */
export type CursorTriggerConfig = {
    /**
     * Repo the cloud agent runs against, e.g. `https://github.com/owner/repo`. Idempotency key for the connector.
     */
    repoUrl: string;
    /**
     * Optional git ref the run starts from.
     */
    startingRef?: string;
    /**
     * Shipeasy project id the run targets.
     */
    projectId: string;
};

/**
 * Create a Cursor cloud-agent trigger. Both keys are required up front (never tokenless). Idempotent by `config.repoUrl`.
 */
export type CreateCursorTriggerRequest = {
    /**
     * Discriminator. Cursor cloud-agent trigger.
     */
    provider: 'cursor_trigger';
    /**
     * Human-readable connector label.
     */
    name?: string;
    /**
     * Events that auto-fire a cold cloud-agent run. Defaults to empty.
     */
    events?: Array<ConnectorEvent>;
    config: CursorTriggerConfig;
    /**
     * Cursor API key that launches the run (secret). Encrypted into the credentials cipher; never returned.
     */
    apiKey: string;
    /**
     * Restricted Shipeasy ops key, injected into the run as `SHIPEASY_CLI_TOKEN` via the launch envVars (secret). Encrypted; never returned.
     */
    opsKey: string;
    /**
     * Whether the trigger is active on create.
     */
    enabled?: boolean;
};

/**
 * Non-secret config for a Copilot trigger.
 */
export type CopilotTriggerConfig = {
    /**
     * GitHub repo owner. Together with `repo` forms the connector's idempotency key (`owner/repo`).
     */
    owner: string;
    /**
     * GitHub repo name.
     */
    repo: string;
    /**
     * Optional base ref the agent branches from.
     */
    baseRef?: string;
    /**
     * Shipeasy project id the run targets.
     */
    projectId: string;
};

/**
 * Create a GitHub Copilot coding-agent trigger. Idempotent by `config.owner/config.repo`.
 */
export type CreateCopilotTriggerRequest = {
    /**
     * Discriminator. GitHub Copilot coding-agent trigger.
     */
    provider: 'copilot_trigger';
    /**
     * Human-readable connector label.
     */
    name?: string;
    /**
     * Events that auto-fire a Copilot agent task. Defaults to empty.
     */
    events?: Array<ConnectorEvent>;
    config: CopilotTriggerConfig;
    /**
     * Copilot-licensed user PAT (secret). The ops key lives in the repo's GitHub "Agents" secret store and is never sent through Shipeasy. Encrypted; never returned.
     */
    token: string;
    /**
     * Whether the trigger is active on create.
     */
    enabled?: boolean;
};

/**
 * Non-secret config for a Jules trigger.
 */
export type JulesTriggerConfig = {
    /**
     * Jules source for the connected repo, e.g. `sources/github/owner/repo`. Idempotency key for the connector.
     */
    source: string;
    /**
     * Optional branch the session starts from.
     */
    startingBranch?: string;
    /**
     * Shipeasy project id the run targets.
     */
    projectId: string;
};

/**
 * Create a Google Jules trigger. Both keys are required up front. Idempotent by `config.source`.
 */
export type CreateJulesTriggerRequest = {
    /**
     * Discriminator. Google Jules trigger.
     */
    provider: 'jules_trigger';
    /**
     * Human-readable connector label.
     */
    name?: string;
    /**
     * Events that auto-fire a Jules session. Defaults to empty.
     */
    events?: Array<ConnectorEvent>;
    config: JulesTriggerConfig;
    /**
     * Jules API key that launches the session (secret). Encrypted into the credentials cipher; never returned.
     */
    apiKey: string;
    /**
     * Restricted Shipeasy ops key, embedded in the prompt (Jules exposes no env channel) (secret). Encrypted; never returned.
     */
    opsKey: string;
    /**
     * Whether the trigger is active on create.
     */
    enabled?: boolean;
};

/**
 * Body for `POST /api/admin/connectors`, discriminated on `provider`. The OAuth providers (`google_sheets`/`github`/`slack`) create a disabled stub that is finished via the provider's OAuth flow. The trigger providers (`claude_trigger`/`cursor_trigger`/`copilot_trigger`/`jules_trigger`) arrive config + credentials together and are fireable immediately.
 */
export type CreateConnectorRequest = ({
    provider: 'google_sheets' | 'github' | 'slack';
} & CreateOAuthConnectorRequest) | ({
    provider: 'claude_trigger';
} & CreateClaudeTriggerRequest) | ({
    provider: 'cursor_trigger';
} & CreateCursorTriggerRequest) | ({
    provider: 'copilot_trigger';
} & CreateCopilotTriggerRequest) | ({
    provider: 'jules_trigger';
} & CreateJulesTriggerRequest);

export type CreateConnectorResponse = {
    /**
     * Newly assigned connector id.
     */
    id: string;
};

export type DeleteConnectorResponse = {
    ok: true;
};

/**
 * Body for `PATCH /api/admin/connectors/{id}`. Partial — only supplied fields change. Array/object fields (`events`, `config`) replace, not merge.
 */
export type UpdateConnectorRequest = {
    /**
     * New connector label.
     */
    name?: string;
    /**
     * Toggle the connector on/off.
     */
    enabled?: boolean;
    /**
     * Replaces the subscribed-events list wholesale. An empty array unsubscribes the connector from every event (e.g. turning off a trigger's auto-fire).
     */
    events?: Array<ConnectorEvent>;
    /**
     * Replaces the non-secret config wholesale. Secrets are never set through this endpoint.
     */
    config?: {
        [key: string]: unknown;
    };
};

/**
 * Update returns only the id — re-fetch via `GET /api/admin/connectors/{id}` for the full row.
 */
export type UpdateConnectorResponse = {
    /**
     * Connector id that was updated.
     */
    id: string;
};

/**
 * Body for `POST /api/admin/connectors/{id}/fire`. Entirely optional — an empty body kicks the run with the provider default.
 */
export type FireConnectorRequest = {
    /**
     * Optional prompt override for the run. When omitted, each provider falls back to its own default (Claude → the configured `fireText`; the others → their built cold-start prompt).
     */
    text?: string;
};

/**
 * Result of firing a trigger connector. `{ ok: true }` on success; `{ ok: false, error }` when the dispatch fails (the request still returns HTTP 200).
 */
export type FireConnectorResponse = {
    /**
     * `true` when the trigger run was successfully kicked off; `false` when the dispatch itself failed (still HTTP 200, with `error` set).
     */
    ok: boolean;
    /**
     * Failure reason. Present only when `ok` is `false` — the fire reached the handler but the underlying dispatch to the provider failed.
     */
    error?: string;
};

/**
 * Result of testing a connector. `{ ok: true, issueUrl }` on success; `{ ok: false, issueUrl: null, error }` when the dispatch fails (the request still returns HTTP 200).
 */
export type TestConnectorResponse = {
    /**
     * `true` when the test dispatch reached the destination; `false` when the dispatch itself failed (still HTTP 200, with `error` set).
     */
    ok: boolean;
    /**
     * URL of the artifact the test produced (e.g. the GitHub Issue created by a `github` connector), or `null` when the provider produces no linkable artifact (or the dispatch failed).
     */
    issueUrl: string | null;
    /**
     * Failure reason. Present only when `ok` is `false` — the test reached the handler but the underlying dispatch to the provider failed.
     */
    error?: string;
};

/**
 * A single API key as returned by the list endpoint. Response fields are snake_case. The raw token is never returned here — only its `last4` tail. `last4` and the typed `scopes` may be `null` for keys minted before those columns existed.
 */
export type KeyRecord = {
    /**
     * Stable opaque key id (UUID). Use it to revoke the key.
     */
    id: string;
    /**
     * Key kind. `server` (back-end, full SDK auth), `client` (public, browser-embeddable), `admin` (CLI/devtools auth token), `ops` (restricted unattended-trigger credential).
     */
    type: 'server' | 'client' | 'admin' | 'ops';
    /**
     * Environment the key is bound to. The worker derives the read env from the key, so this is the isolation boundary. `admin`/`ops` keys are env-agnostic and pinned to `prod`.
     */
    env: 'dev' | 'staging' | 'prod';
    /**
     * ISO-8601 timestamp the key was minted.
     */
    created_at: string;
    /**
     * ISO-8601 timestamp the key was revoked, or `null` while the key is active.
     */
    revoked_at: string | null;
    /**
     * ISO-8601 expiry, or `null` for a key that never expires. `admin` keys are fixed at 90 days; `ops` keys use a short sliding window.
     */
    expires_at: string | null;
    /**
     * Email of the human or actor that minted the key, or `null` when not attributable (e.g. CLI device-flow).
     */
    created_by_email: string | null;
    /**
     * Human label for the key. `null` for unnamed dashboard mints; programmatic mints auto-name themselves.
     */
    name: string | null;
    /**
     * Permission strings recorded on the key for audit/display, or `null` when none were set.
     */
    scopes: Array<string> | null;
    /**
     * Last 4 characters of the raw token, so the dashboard mask is matchable against the held key. `null` for keys minted before this column.
     */
    last4: string | null;
};

/**
 * One page of API keys plus a keyset `next_cursor` (ordered `created_at desc, id desc`).
 */
export type ListKeysResponse = {
    data: Array<KeyRecord>;
    /**
     * Opaque cursor for the next page, or `null` on the last page. Pass it back as the `cursor` query parameter.
     */
    next_cursor: string | null;
};

/**
 * Body for `POST /api/admin/keys`. Only `type` is required.
 *
 * `env` is **required** for `server` and `client` keys (the key is bound to one environment — that binding is the read-env isolation boundary). For `admin` and `ops` keys `env` is ignored and the key is pinned to `prod`.
 */
export type CreateKeyRequest = {
    /**
     * Key kind to mint. `server` (back-end), `client` (public, browser), `admin` (CLI/devtools token), `ops` (restricted unattended-trigger credential). `admin`/`ops` keys don't count toward the plan key limit.
     */
    type: 'server' | 'client' | 'admin' | 'ops';
    /**
     * Optional human label. Programmatic (API) mints that omit it get an auto-generated descriptive name; dashboard mints may leave it blank.
     */
    name?: string;
    /**
     * Optional permission strings recorded on the key for audit/display.
     */
    scopes?: Array<'experiments:read' | 'gates:evaluate' | 'events:write' | 'configs:write' | 'experiments:write'>;
    /**
     * Days until the key expires (1–3650), or `null`/omitted for a key that never expires. Ignored for `admin` keys (fixed 90-day expiry) and `ops` keys (short sliding window).
     */
    expiresInDays?: number | null;
    /**
     * Environment to bind the key to. Always required. For `server`/`client` keys it is the isolation boundary the worker reads from; `admin`/`ops` keys are env-agnostic and pinned to `prod` by the handler regardless of the value sent.
     */
    env: 'dev' | 'staging' | 'prod';
};

/**
 * The minted key. The plaintext `key` is returned **once**, here — it is stored hashed and can never be retrieved again.
 */
export type CreateKeyResponse = {
    /**
     * Stable opaque key id (UUID). Use it to revoke the key.
     */
    id: string;
    /**
     * The kind of key that was minted.
     */
    type: 'server' | 'client' | 'admin' | 'ops';
    /**
     * Environment the key is bound to (`prod` for `admin`/`ops`).
     */
    env: 'dev' | 'staging' | 'prod';
    /**
     * The plaintext API token (e.g. `sdk_server_…`). Returned once — store it now; it cannot be recovered.
     */
    key: string;
    /**
     * ISO-8601 expiry, or `null` if the key never expires.
     */
    expires_at: string | null;
};

/**
 * Confirms the key is revoked. Idempotent — revoking an already-revoked key returns the same shape.
 */
export type RevokeKeyResponse = {
    /**
     * Id of the key that was revoked.
     */
    id: string;
    revoked: true;
};

/**
 * A single universal-search hit. Field names are camelCase; `title` is `null` when the resource has no human title/display name (members fall back to a role label).
 */
export type SearchHit = {
    /**
     * Resource family — drives the result group + icon in the palette. (`member` hits are surfaced first.)
     */
    type: 'member' | 'gate' | 'experiment' | 'config' | 'killswitch' | 'metric';
    /**
     * Stable identifier used to deep-link into the row's detail pane. For `member` hits this is the teammate's email; for everything else it is the resource id.
     */
    id: string;
    /**
     * The matched handle — the resource's immutable `name`, or the member's display name (falling back to email).
     */
    name: string;
    /**
     * Human title/display name when present, else `null`. For `member` hits this carries the email (or the `Owner`/`Member` role when no profile name is set).
     */
    title: string | null;
    /**
     * Project-relative href that opens the row (e.g. `/gates?open=<id>`).
     */
    href: string;
};

/**
 * Flat, ranked-by-type list of search hits the command palette groups for display. Capped per resource family so one noisy type can't crowd out the rest.
 */
export type SearchResponse = {
    hits: Array<SearchHit>;
};

/**
 * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
 */
export type ProjectId = string;

/**
 * Page size (1–500). Defaults to 100.
 */
export type PaginationLimit = number;

/**
 * Opaque cursor returned in the previous page's `next_cursor`. Omit for the first page.
 */
export type PaginationCursor = string;

export type ListGatesData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: {
        /**
         * Page size (1–500). Defaults to 100.
         */
        limit?: number;
        /**
         * Opaque cursor returned in the previous page's `next_cursor`. Omit for the first page.
         */
        cursor?: string;
    };
    url: '/api/admin/gates';
};

export type ListGatesErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type ListGatesError = ListGatesErrors[keyof ListGatesErrors];

export type ListGatesResponses = {
    /**
     * List feature gates
     */
    200: ListGatesResponse;
};

export type ListGatesResponse2 = ListGatesResponses[keyof ListGatesResponses];

export type CreateGateData = {
    body: CreateGateRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/api/admin/gates';
};

export type CreateGateErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type CreateGateError = CreateGateErrors[keyof CreateGateErrors];

export type CreateGateResponses = {
    /**
     * Create a feature gate
     */
    201: CreateGateResponse;
};

export type CreateGateResponse2 = CreateGateResponses[keyof CreateGateResponses];

export type DeleteGateData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque gate id (`gat_…`) or the gate's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/gates/{id}';
};

export type DeleteGateErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type DeleteGateError = DeleteGateErrors[keyof DeleteGateErrors];

export type DeleteGateResponses = {
    /**
     * Delete a feature gate
     */
    200: DeleteGateResponse;
};

export type DeleteGateResponse2 = DeleteGateResponses[keyof DeleteGateResponses];

export type UpdateGateData = {
    body: UpdateGateRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque gate id (`gat_…`) or the gate's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/gates/{id}';
};

export type UpdateGateErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type UpdateGateError = UpdateGateErrors[keyof UpdateGateErrors];

export type UpdateGateResponses = {
    /**
     * Update a feature gate
     */
    200: UpdateGateResponse;
};

export type UpdateGateResponse2 = UpdateGateResponses[keyof UpdateGateResponses];

export type EnableGateData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque gate id (`gat_…`) or the gate's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/gates/{id}/enable';
};

export type EnableGateErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type EnableGateError = EnableGateErrors[keyof EnableGateErrors];

export type EnableGateResponses = {
    /**
     * Enable a gate
     */
    201: EnableGateResponse;
};

export type EnableGateResponse2 = EnableGateResponses[keyof EnableGateResponses];

export type DisableGateData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque gate id (`gat_…`) or the gate's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/gates/{id}/disable';
};

export type DisableGateErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type DisableGateError = DisableGateErrors[keyof DisableGateErrors];

export type DisableGateResponses = {
    /**
     * Disable a gate
     */
    201: DisableGateResponse;
};

export type DisableGateResponse2 = DisableGateResponses[keyof DisableGateResponses];

export type ListExperimentsData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: {
        /**
         * Page size (1–500). Defaults to 100.
         */
        limit?: number;
        /**
         * Opaque cursor returned in the previous page's `next_cursor`. Omit for the first page.
         */
        cursor?: string;
    };
    url: '/api/admin/experiments';
};

export type ListExperimentsErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type ListExperimentsError = ListExperimentsErrors[keyof ListExperimentsErrors];

export type ListExperimentsResponses = {
    /**
     * List experiments
     */
    200: ListExperimentsResponse;
};

export type ListExperimentsResponse2 = ListExperimentsResponses[keyof ListExperimentsResponses];

export type CreateExperimentData = {
    body: CreateExperimentRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/api/admin/experiments';
};

export type CreateExperimentErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type CreateExperimentError = CreateExperimentErrors[keyof CreateExperimentErrors];

export type CreateExperimentResponses = {
    /**
     * Create an experiment
     */
    201: CreateExperimentResponse;
};

export type CreateExperimentResponse2 = CreateExperimentResponses[keyof CreateExperimentResponses];

export type DeleteExperimentData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque experiment id (`exp_…`) or the experiment's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/experiments/{id}';
};

export type DeleteExperimentErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type DeleteExperimentError = DeleteExperimentErrors[keyof DeleteExperimentErrors];

export type DeleteExperimentResponses = {
    /**
     * Delete an experiment
     */
    200: DeleteExperimentResponse;
};

export type DeleteExperimentResponse2 = DeleteExperimentResponses[keyof DeleteExperimentResponses];

export type GetExperimentData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque experiment id (`exp_…`) or the experiment's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/experiments/{id}';
};

export type GetExperimentErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type GetExperimentError = GetExperimentErrors[keyof GetExperimentErrors];

export type GetExperimentResponses = {
    /**
     * Get one experiment
     */
    200: GetExperimentResponse;
};

export type GetExperimentResponse2 = GetExperimentResponses[keyof GetExperimentResponses];

export type UpdateExperimentData = {
    body: UpdateExperimentRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque experiment id (`exp_…`) or the experiment's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/experiments/{id}';
};

export type UpdateExperimentErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type UpdateExperimentError = UpdateExperimentErrors[keyof UpdateExperimentErrors];

export type UpdateExperimentResponses = {
    /**
     * Update an experiment
     */
    200: UpdateExperimentResponse;
};

export type UpdateExperimentResponse2 = UpdateExperimentResponses[keyof UpdateExperimentResponses];

export type SetExperimentStatusData = {
    body: SetExperimentStatusRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque experiment id (`exp_…`) or the experiment's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/experiments/{id}/status';
};

export type SetExperimentStatusErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type SetExperimentStatusError = SetExperimentStatusErrors[keyof SetExperimentStatusErrors];

export type SetExperimentStatusResponses = {
    /**
     * Transition experiment status
     */
    201: SetExperimentStatusResponse;
};

export type SetExperimentStatusResponse2 = SetExperimentStatusResponses[keyof SetExperimentStatusResponses];

export type SetExperimentMetricsData = {
    body: SetExperimentMetricsRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque experiment id (`exp_…`) or the experiment's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/experiments/{id}/metrics';
};

export type SetExperimentMetricsErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type SetExperimentMetricsError = SetExperimentMetricsErrors[keyof SetExperimentMetricsErrors];

export type SetExperimentMetricsResponses = {
    /**
     * Attach metrics
     */
    201: SetExperimentMetricsResponse;
};

export type SetExperimentMetricsResponse2 = SetExperimentMetricsResponses[keyof SetExperimentMetricsResponses];

export type GetExperimentResultsData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque experiment id (`exp_…`) or the experiment's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/experiments/{id}/results';
};

export type GetExperimentResultsErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type GetExperimentResultsError = GetExperimentResultsErrors[keyof GetExperimentResultsErrors];

export type GetExperimentResultsResponses = {
    /**
     * Get analysis results
     */
    200: GetExperimentResultsResponse;
};

export type GetExperimentResultsResponse2 = GetExperimentResultsResponses[keyof GetExperimentResultsResponses];

export type GetExperimentTimeseriesData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque experiment id (`exp_…`) or the experiment's `name`.
         */
        id: string;
    };
    query?: {
        /**
         * Optional metric name to filter the series.
         */
        metric?: string;
    };
    url: '/api/admin/experiments/{id}/timeseries';
};

export type GetExperimentTimeseriesErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type GetExperimentTimeseriesError = GetExperimentTimeseriesErrors[keyof GetExperimentTimeseriesErrors];

export type GetExperimentTimeseriesResponses = {
    /**
     * Get analysis timeseries
     */
    200: GetExperimentTimeseriesResponse;
};

export type GetExperimentTimeseriesResponse2 = GetExperimentTimeseriesResponses[keyof GetExperimentTimeseriesResponses];

export type ReanalyzeExperimentData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque experiment id (`exp_…`) or the experiment's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/experiments/{id}/reanalyze';
};

export type ReanalyzeExperimentErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type ReanalyzeExperimentError = ReanalyzeExperimentErrors[keyof ReanalyzeExperimentErrors];

export type ReanalyzeExperimentResponses = {
    /**
     * Re-queue analysis
     */
    201: ReanalyzeExperimentResponse;
};

export type ReanalyzeExperimentResponse2 = ReanalyzeExperimentResponses[keyof ReanalyzeExperimentResponses];

export type ListConfigsData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: {
        /**
         * Page size (1–500). Defaults to 100.
         */
        limit?: number;
        /**
         * Opaque cursor returned in the previous page's `next_cursor`. Omit for the first page.
         */
        cursor?: string;
    };
    url: '/api/admin/configs';
};

export type ListConfigsErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type ListConfigsError = ListConfigsErrors[keyof ListConfigsErrors];

export type ListConfigsResponses = {
    /**
     * List dynamic configs
     */
    200: ListConfigsResponse;
};

export type ListConfigsResponse2 = ListConfigsResponses[keyof ListConfigsResponses];

export type CreateConfigData = {
    body: CreateConfigRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/api/admin/configs';
};

export type CreateConfigErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type CreateConfigError = CreateConfigErrors[keyof CreateConfigErrors];

export type CreateConfigResponses = {
    /**
     * Create a dynamic config
     */
    201: CreateConfigResponse;
};

export type CreateConfigResponse2 = CreateConfigResponses[keyof CreateConfigResponses];

export type DeleteConfigData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque config id (`cfg_…`) or the config's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/configs/{id}';
};

export type DeleteConfigErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type DeleteConfigError = DeleteConfigErrors[keyof DeleteConfigErrors];

export type DeleteConfigResponses = {
    /**
     * Delete a dynamic config
     */
    200: DeleteConfigResponse;
};

export type DeleteConfigResponse2 = DeleteConfigResponses[keyof DeleteConfigResponses];

export type GetConfigData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque config id (`cfg_…`) or the config's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/configs/{id}';
};

export type GetConfigErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type GetConfigError = GetConfigErrors[keyof GetConfigErrors];

export type GetConfigResponses = {
    /**
     * Get one config
     */
    200: GetConfigResponse;
};

export type GetConfigResponse2 = GetConfigResponses[keyof GetConfigResponses];

export type UpdateConfigData = {
    body: UpdateConfigRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque config id (`cfg_…`) or the config's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/configs/{id}';
};

export type UpdateConfigErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type UpdateConfigError = UpdateConfigErrors[keyof UpdateConfigErrors];

export type UpdateConfigResponses = {
    /**
     * Update a dynamic config
     */
    200: UpdateConfigResponse;
};

export type UpdateConfigResponse2 = UpdateConfigResponses[keyof UpdateConfigResponses];

export type DiscardConfigDraftData = {
    body: DiscardConfigDraftRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque config id (`cfg_…`) or the config's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/configs/{id}/drafts';
};

export type DiscardConfigDraftErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type DiscardConfigDraftError = DiscardConfigDraftErrors[keyof DiscardConfigDraftErrors];

export type DiscardConfigDraftResponses = {
    /**
     * Discard a draft
     */
    200: DiscardConfigDraftResponse;
};

export type DiscardConfigDraftResponse2 = DiscardConfigDraftResponses[keyof DiscardConfigDraftResponses];

export type SaveConfigDraftData = {
    body: SaveConfigDraftRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque config id (`cfg_…`) or the config's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/configs/{id}/drafts';
};

export type SaveConfigDraftErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type SaveConfigDraftError = SaveConfigDraftErrors[keyof SaveConfigDraftErrors];

export type SaveConfigDraftResponses = {
    /**
     * Save a draft value
     */
    200: SaveConfigDraftResponse;
};

export type SaveConfigDraftResponse2 = SaveConfigDraftResponses[keyof SaveConfigDraftResponses];

export type PublishConfigDraftData = {
    body: PublishConfigDraftRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque config id (`cfg_…`) or the config's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/configs/{id}/publish';
};

export type PublishConfigDraftErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type PublishConfigDraftError = PublishConfigDraftErrors[keyof PublishConfigDraftErrors];

export type PublishConfigDraftResponses = {
    /**
     * Publish a draft
     */
    201: PublishConfigDraftResponse;
};

export type PublishConfigDraftResponse2 = PublishConfigDraftResponses[keyof PublishConfigDraftResponses];

export type ListConfigActivityData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque config id (`cfg_…`) or the config's `name`.
         */
        id: string;
    };
    query?: {
        /**
         * Max rows to return (1–100). Defaults to 20.
         */
        limit?: number;
    };
    url: '/api/admin/configs/{id}/activity';
};

export type ListConfigActivityErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type ListConfigActivityError = ListConfigActivityErrors[keyof ListConfigActivityErrors];

export type ListConfigActivityResponses = {
    /**
     * List config activity
     */
    200: ListConfigActivityResponse;
};

export type ListConfigActivityResponse2 = ListConfigActivityResponses[keyof ListConfigActivityResponses];

export type UpdateConfigSchemaData = {
    body: UpdateConfigSchemaRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque config id (`cfg_…`) or the config's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/configs/{id}/schema';
};

export type UpdateConfigSchemaErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type UpdateConfigSchemaError = UpdateConfigSchemaErrors[keyof UpdateConfigSchemaErrors];

export type UpdateConfigSchemaResponses = {
    /**
     * Update a config schema
     */
    200: UpdateConfigSchemaResponse;
};

export type UpdateConfigSchemaResponse2 = UpdateConfigSchemaResponses[keyof UpdateConfigSchemaResponses];

export type ListKillswitchesData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: {
        /**
         * Page size (1–500). Defaults to 100.
         */
        limit?: number;
        /**
         * Opaque cursor returned in the previous page's `next_cursor`. Omit for the first page.
         */
        cursor?: string;
    };
    url: '/api/admin/killswitches';
};

export type ListKillswitchesErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type ListKillswitchesError = ListKillswitchesErrors[keyof ListKillswitchesErrors];

export type ListKillswitchesResponses = {
    /**
     * List killswitches
     */
    200: ListKillswitchesResponse;
};

export type ListKillswitchesResponse2 = ListKillswitchesResponses[keyof ListKillswitchesResponses];

export type CreateKillswitchData = {
    body: CreateKillswitchRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/api/admin/killswitches';
};

export type CreateKillswitchErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type CreateKillswitchError = CreateKillswitchErrors[keyof CreateKillswitchErrors];

export type CreateKillswitchResponses = {
    /**
     * Create a killswitch
     */
    201: CreateKillswitchResponse;
};

export type CreateKillswitchResponse2 = CreateKillswitchResponses[keyof CreateKillswitchResponses];

export type DeleteKillswitchData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque killswitch id (`ksw_…`) or the killswitch's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/killswitches/{id}';
};

export type DeleteKillswitchErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type DeleteKillswitchError = DeleteKillswitchErrors[keyof DeleteKillswitchErrors];

export type DeleteKillswitchResponses = {
    /**
     * Delete a killswitch
     */
    200: DeleteKillswitchResponse;
};

export type DeleteKillswitchResponse2 = DeleteKillswitchResponses[keyof DeleteKillswitchResponses];

export type GetKillswitchData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque killswitch id (`ksw_…`) or the killswitch's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/killswitches/{id}';
};

export type GetKillswitchErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type GetKillswitchError = GetKillswitchErrors[keyof GetKillswitchErrors];

export type GetKillswitchResponses = {
    /**
     * Get one killswitch
     */
    200: GetKillswitchResponse;
};

export type GetKillswitchResponse2 = GetKillswitchResponses[keyof GetKillswitchResponses];

export type UpdateKillswitchData = {
    body: UpdateKillswitchRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque killswitch id (`ksw_…`) or the killswitch's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/killswitches/{id}';
};

export type UpdateKillswitchErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type UpdateKillswitchError = UpdateKillswitchErrors[keyof UpdateKillswitchErrors];

export type UpdateKillswitchResponses = {
    /**
     * Update a killswitch
     */
    200: UpdateKillswitchResponse;
};

export type UpdateKillswitchResponse2 = UpdateKillswitchResponses[keyof UpdateKillswitchResponses];

export type UnsetKillswitchSwitchData = {
    body: UnsetKillswitchSwitchRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque killswitch id (`ksw_…`) or the killswitch's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/killswitches/{id}/switch';
};

export type UnsetKillswitchSwitchErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type UnsetKillswitchSwitchError = UnsetKillswitchSwitchErrors[keyof UnsetKillswitchSwitchErrors];

export type UnsetKillswitchSwitchResponses = {
    /**
     * Remove one switch entry
     */
    200: UnsetKillswitchSwitchResponse;
};

export type UnsetKillswitchSwitchResponse2 = UnsetKillswitchSwitchResponses[keyof UnsetKillswitchSwitchResponses];

export type SetKillswitchSwitchData = {
    body: SetKillswitchSwitchRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque killswitch id (`ksw_…`) or the killswitch's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/killswitches/{id}/switch';
};

export type SetKillswitchSwitchErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type SetKillswitchSwitchError = SetKillswitchSwitchErrors[keyof SetKillswitchSwitchErrors];

export type SetKillswitchSwitchResponses = {
    /**
     * Set one switch entry
     */
    200: SetKillswitchSwitchResponse;
};

export type SetKillswitchSwitchResponse2 = SetKillswitchSwitchResponses[keyof SetKillswitchSwitchResponses];

export type SetKillswitchValueData = {
    body: SetKillswitchValueRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque killswitch id (`ksw_…`) or the killswitch's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/killswitches/{id}/value';
};

export type SetKillswitchValueErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type SetKillswitchValueError = SetKillswitchValueErrors[keyof SetKillswitchValueErrors];

export type SetKillswitchValueResponses = {
    /**
     * Set the flat value on one env
     */
    200: SetKillswitchValueResponse;
};

export type SetKillswitchValueResponse2 = SetKillswitchValueResponses[keyof SetKillswitchValueResponses];

export type ListUniversesData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: {
        /**
         * Page size (1–500). Defaults to 100.
         */
        limit?: number;
        /**
         * Opaque cursor returned in the previous page's `next_cursor`. Omit for the first page.
         */
        cursor?: string;
    };
    url: '/api/admin/universes';
};

export type ListUniversesErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type ListUniversesError = ListUniversesErrors[keyof ListUniversesErrors];

export type ListUniversesResponses = {
    /**
     * List universes
     */
    200: ListUniversesResponse;
};

export type ListUniversesResponse2 = ListUniversesResponses[keyof ListUniversesResponses];

export type CreateUniverseData = {
    body: CreateUniverseRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/api/admin/universes';
};

export type CreateUniverseErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type CreateUniverseError = CreateUniverseErrors[keyof CreateUniverseErrors];

export type CreateUniverseResponses = {
    /**
     * Create a universe
     */
    201: CreateUniverseResponse;
};

export type CreateUniverseResponse2 = CreateUniverseResponses[keyof CreateUniverseResponses];

export type DeleteUniverseData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque universe id (`uni_…`) or the universe's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/universes/{id}';
};

export type DeleteUniverseErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type DeleteUniverseError = DeleteUniverseErrors[keyof DeleteUniverseErrors];

export type DeleteUniverseResponses = {
    /**
     * Delete a universe
     */
    200: DeleteUniverseResponse;
};

export type DeleteUniverseResponse2 = DeleteUniverseResponses[keyof DeleteUniverseResponses];

export type UpdateUniverseData = {
    body: UpdateUniverseRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque universe id (`uni_…`) or the universe's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/universes/{id}';
};

export type UpdateUniverseErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type UpdateUniverseError = UpdateUniverseErrors[keyof UpdateUniverseErrors];

export type UpdateUniverseResponses = {
    /**
     * Update a universe
     */
    200: UpdateUniverseResponse;
};

export type UpdateUniverseResponse2 = UpdateUniverseResponses[keyof UpdateUniverseResponses];

export type ListAttributesData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/api/admin/attributes';
};

export type ListAttributesErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type ListAttributesError = ListAttributesErrors[keyof ListAttributesErrors];

export type ListAttributesResponses = {
    /**
     * List targeting attributes
     */
    200: ListAttributesResponse;
};

export type ListAttributesResponse2 = ListAttributesResponses[keyof ListAttributesResponses];

export type CreateAttributeData = {
    body: CreateAttributeRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/api/admin/attributes';
};

export type CreateAttributeErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type CreateAttributeError = CreateAttributeErrors[keyof CreateAttributeErrors];

export type CreateAttributeResponses = {
    /**
     * Declare a targeting attribute
     */
    201: CreateAttributeResponse;
};

export type CreateAttributeResponse2 = CreateAttributeResponses[keyof CreateAttributeResponses];

export type DeleteAttributeData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * The attribute id.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/attributes/{id}';
};

export type DeleteAttributeErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type DeleteAttributeError = DeleteAttributeErrors[keyof DeleteAttributeErrors];

export type DeleteAttributeResponses = {
    /**
     * Archive a targeting attribute
     */
    200: DeleteAttributeResponse;
};

export type DeleteAttributeResponse2 = DeleteAttributeResponses[keyof DeleteAttributeResponses];

export type GetAttributeData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * The attribute id.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/attributes/{id}';
};

export type GetAttributeErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type GetAttributeError = GetAttributeErrors[keyof GetAttributeErrors];

export type GetAttributeResponses = {
    /**
     * Get a targeting attribute
     */
    200: GetAttributeResponse;
};

export type GetAttributeResponse2 = GetAttributeResponses[keyof GetAttributeResponses];

export type UpdateAttributeData = {
    body: UpdateAttributeRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * The attribute id.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/attributes/{id}';
};

export type UpdateAttributeErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type UpdateAttributeError = UpdateAttributeErrors[keyof UpdateAttributeErrors];

export type UpdateAttributeResponses = {
    /**
     * Update a targeting attribute
     */
    200: UpdateAttributeResponse;
};

export type UpdateAttributeResponse2 = UpdateAttributeResponses[keyof UpdateAttributeResponses];

export type ListMetricsData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/api/admin/metrics';
};

export type ListMetricsErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type ListMetricsError = ListMetricsErrors[keyof ListMetricsErrors];

export type ListMetricsResponses = {
    /**
     * List metrics
     */
    200: ListMetricsResponse;
};

export type ListMetricsResponse2 = ListMetricsResponses[keyof ListMetricsResponses];

export type CreateMetricData = {
    body: CreateMetricRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/api/admin/metrics';
};

export type CreateMetricErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type CreateMetricError = CreateMetricErrors[keyof CreateMetricErrors];

export type CreateMetricResponses = {
    /**
     * Create a metric
     */
    201: CreateMetricResponse;
};

export type CreateMetricResponse2 = CreateMetricResponses[keyof CreateMetricResponses];

export type DeleteMetricData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque metric id (`met_…`) or the metric's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/metrics/{id}';
};

export type DeleteMetricErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type DeleteMetricError = DeleteMetricErrors[keyof DeleteMetricErrors];

export type DeleteMetricResponses = {
    /**
     * Archive a metric
     */
    200: DeleteMetricResponse;
};

export type DeleteMetricResponse2 = DeleteMetricResponses[keyof DeleteMetricResponses];

export type GetMetricData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque metric id (`met_…`) or the metric's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/metrics/{id}';
};

export type GetMetricErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type GetMetricError = GetMetricErrors[keyof GetMetricErrors];

export type GetMetricResponses = {
    /**
     * Get a metric
     */
    200: GetMetricResponse;
};

export type GetMetricResponse2 = GetMetricResponses[keyof GetMetricResponses];

export type UpdateMetricData = {
    body: UpdateMetricRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque metric id (`met_…`) or the metric's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/metrics/{id}';
};

export type UpdateMetricErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type UpdateMetricError = UpdateMetricErrors[keyof UpdateMetricErrors];

export type UpdateMetricResponses = {
    /**
     * Update a metric
     */
    200: GetMetricResponse;
};

export type UpdateMetricResponse = UpdateMetricResponses[keyof UpdateMetricResponses];

export type ListEventsData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/api/admin/events';
};

export type ListEventsErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type ListEventsError = ListEventsErrors[keyof ListEventsErrors];

export type ListEventsResponses = {
    /**
     * List events
     */
    200: ListEventsResponse;
};

export type ListEventsResponse2 = ListEventsResponses[keyof ListEventsResponses];

export type CreateEventData = {
    body: CreateEventRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/api/admin/events';
};

export type CreateEventErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type CreateEventError = CreateEventErrors[keyof CreateEventErrors];

export type CreateEventResponses = {
    /**
     * Register an event
     */
    201: CreateEventResponse;
};

export type CreateEventResponse2 = CreateEventResponses[keyof CreateEventResponses];

export type DeleteEventData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque event id (`evt_…`) or the event's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/events/{id}';
};

export type DeleteEventErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type DeleteEventError = DeleteEventErrors[keyof DeleteEventErrors];

export type DeleteEventResponses = {
    /**
     * Archive an event
     */
    200: DeleteEventResponse;
};

export type DeleteEventResponse2 = DeleteEventResponses[keyof DeleteEventResponses];

export type GetEventData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque event id (`evt_…`) or the event's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/events/{id}';
};

export type GetEventErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type GetEventError = GetEventErrors[keyof GetEventErrors];

export type GetEventResponses = {
    /**
     * Get an event
     */
    200: GetEventResponse;
};

export type GetEventResponse2 = GetEventResponses[keyof GetEventResponses];

export type UpdateEventData = {
    body: UpdateEventRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque event id (`evt_…`) or the event's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/events/{id}';
};

export type UpdateEventErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type UpdateEventError = UpdateEventErrors[keyof UpdateEventErrors];

export type UpdateEventResponses = {
    /**
     * Update an event
     */
    200: UpdateEventResponse;
};

export type UpdateEventResponse2 = UpdateEventResponses[keyof UpdateEventResponses];

export type ApproveEventData = {
    body: ApproveEventRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque event id (`evt_…`) or the event's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/events/{id}/approve';
};

export type ApproveEventErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type ApproveEventError = ApproveEventErrors[keyof ApproveEventErrors];

export type ApproveEventResponses = {
    /**
     * Approve a pending event
     */
    200: ApproveEventResponse;
};

export type ApproveEventResponse2 = ApproveEventResponses[keyof ApproveEventResponses];

export type ListOpsItemsData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: {
        /**
         * Filter by item type (`bug`/`feature_request`/`error`/`alert`), or `all`.
         */
        type?: 'bug' | 'feature_request' | 'error' | 'alert' | 'all';
        /**
         * Filter by lifecycle status, or `all`.
         */
        status?: 'open' | 'triaged' | 'in_progress' | 'ready_for_qa' | 'resolved' | 'wont_fix' | 'all';
        /**
         * Max items to return (1–500).
         */
        limit?: number;
    };
    url: '/api/admin/ops';
};

export type ListOpsItemsErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type ListOpsItemsError = ListOpsItemsErrors[keyof ListOpsItemsErrors];

export type ListOpsItemsResponses = {
    /**
     * List the operational queue
     */
    200: ListOpsItemsResponse;
};

export type ListOpsItemsResponse2 = ListOpsItemsResponses[keyof ListOpsItemsResponses];

export type CreateOpsItemData = {
    body: CreateOpsItemRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/api/admin/ops';
};

export type CreateOpsItemErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type CreateOpsItemError = CreateOpsItemErrors[keyof CreateOpsItemErrors];

export type CreateOpsItemResponses = {
    /**
     * File a queue item
     */
    201: CreateOpsItemResponse;
};

export type CreateOpsItemResponse2 = CreateOpsItemResponses[keyof CreateOpsItemResponses];

export type GetOpsItemData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Per-project item number (e.g. `7`) or the full ops item id.
         */
        handle: string;
    };
    query?: never;
    url: '/api/admin/ops/{handle}';
};

export type GetOpsItemErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type GetOpsItemError = GetOpsItemErrors[keyof GetOpsItemErrors];

export type GetOpsItemResponses = {
    /**
     * Get one queue item
     */
    200: GetOpsItemResponse;
};

export type GetOpsItemResponse2 = GetOpsItemResponses[keyof GetOpsItemResponses];

export type UpdateOpsItemData = {
    body: UpdateOpsItemRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Per-project item number (e.g. `7`) or the full ops item id.
         */
        handle: string;
    };
    query?: never;
    url: '/api/admin/ops/{handle}';
};

export type UpdateOpsItemErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type UpdateOpsItemError = UpdateOpsItemErrors[keyof UpdateOpsItemErrors];

export type UpdateOpsItemResponses = {
    /**
     * Update a queue item
     */
    200: UpdateOpsItemResponse;
};

export type UpdateOpsItemResponse2 = UpdateOpsItemResponses[keyof UpdateOpsItemResponses];

export type LinkPrToOpsItemData = {
    body: LinkPrToOpsItemRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Per-project item number (e.g. `7`) or the full ops item id.
         */
        handle: string;
    };
    query?: never;
    url: '/api/admin/ops/{handle}/link-pr';
};

export type LinkPrToOpsItemErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type LinkPrToOpsItemError = LinkPrToOpsItemErrors[keyof LinkPrToOpsItemErrors];

export type LinkPrToOpsItemResponses = {
    /**
     * Link a fixing PR
     */
    200: LinkPrToOpsItemResponse;
};

export type LinkPrToOpsItemResponse2 = LinkPrToOpsItemResponses[keyof LinkPrToOpsItemResponses];

export type NotifyOpsData = {
    body: NotifyOpsRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/api/admin/notifications';
};

export type NotifyOpsErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type NotifyOpsError = NotifyOpsErrors[keyof NotifyOpsErrors];

export type NotifyOpsResponses = {
    /**
     * Raise an attention notification
     */
    201: NotifyOpsResponse;
};

export type NotifyOpsResponse2 = NotifyOpsResponses[keyof NotifyOpsResponses];

export type ListSlackChannelsData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/api/admin/slack/channels';
};

export type ListSlackChannelsErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type ListSlackChannelsError = ListSlackChannelsErrors[keyof ListSlackChannelsErrors];

export type ListSlackChannelsResponses = {
    /**
     * List Slack channels
     */
    200: ListSlackChannelsResponse;
};

export type ListSlackChannelsResponse2 = ListSlackChannelsResponses[keyof ListSlackChannelsResponses];

export type ListAlertRulesData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/api/admin/alert-rules';
};

export type ListAlertRulesErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type ListAlertRulesError = ListAlertRulesErrors[keyof ListAlertRulesErrors];

export type ListAlertRulesResponses = {
    /**
     * List alert rules
     */
    200: ListAlertRulesResponse;
};

export type ListAlertRulesResponse2 = ListAlertRulesResponses[keyof ListAlertRulesResponses];

export type CreateAlertRuleData = {
    body: CreateAlertRuleRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/api/admin/alert-rules';
};

export type CreateAlertRuleErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type CreateAlertRuleError = CreateAlertRuleErrors[keyof CreateAlertRuleErrors];

export type CreateAlertRuleResponses = {
    /**
     * Create an alert rule
     */
    201: CreateAlertRuleResponse;
};

export type CreateAlertRuleResponse2 = CreateAlertRuleResponses[keyof CreateAlertRuleResponses];

export type DeleteAlertRuleData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque alert-rule id (`ar_…`) or the rule's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/alert-rules/{id}';
};

export type DeleteAlertRuleErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type DeleteAlertRuleError = DeleteAlertRuleErrors[keyof DeleteAlertRuleErrors];

export type DeleteAlertRuleResponses = {
    /**
     * Delete an alert rule
     */
    200: DeleteAlertRuleResponse;
};

export type DeleteAlertRuleResponse2 = DeleteAlertRuleResponses[keyof DeleteAlertRuleResponses];

export type UpdateAlertRuleData = {
    body: UpdateAlertRuleRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque alert-rule id (`ar_…`) or the rule's `name`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/alert-rules/{id}';
};

export type UpdateAlertRuleErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type UpdateAlertRuleError = UpdateAlertRuleErrors[keyof UpdateAlertRuleErrors];

export type UpdateAlertRuleResponses = {
    /**
     * Update an alert rule
     */
    200: UpdateAlertRuleResponse;
};

export type UpdateAlertRuleResponse2 = UpdateAlertRuleResponses[keyof UpdateAlertRuleResponses];

export type GetCurrentProjectData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/api/admin/projects/current';
};

export type GetCurrentProjectErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type GetCurrentProjectError = GetCurrentProjectErrors[keyof GetCurrentProjectErrors];

export type GetCurrentProjectResponses = {
    /**
     * Show the current project
     */
    200: GetCurrentProjectResponse;
};

export type GetCurrentProjectResponse2 = GetCurrentProjectResponses[keyof GetCurrentProjectResponses];

export type UpsertProjectData = {
    body: UpsertProjectRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/api/admin/projects/upsert';
};

export type UpsertProjectErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type UpsertProjectError = UpsertProjectErrors[keyof UpsertProjectErrors];

export type UpsertProjectResponses = {
    /**
     * Find-or-create a project by domain
     */
    200: UpsertProjectResponse;
};

export type UpsertProjectResponse2 = UpsertProjectResponses[keyof UpsertProjectResponses];

export type ListI18nProfilesData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/api/admin/i18n/profiles';
};

export type ListI18nProfilesErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type ListI18nProfilesError = ListI18nProfilesErrors[keyof ListI18nProfilesErrors];

export type ListI18nProfilesResponses = {
    /**
     * List i18n profiles
     */
    200: ListI18nProfilesResponse;
};

export type ListI18nProfilesResponse2 = ListI18nProfilesResponses[keyof ListI18nProfilesResponses];

export type CreateI18nProfileData = {
    body: CreateI18nProfileRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/api/admin/i18n/profiles';
};

export type CreateI18nProfileErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type CreateI18nProfileError = CreateI18nProfileErrors[keyof CreateI18nProfileErrors];

export type CreateI18nProfileResponses = {
    /**
     * Create an i18n profile
     */
    201: CreateI18nProfileResponse;
};

export type CreateI18nProfileResponse2 = CreateI18nProfileResponses[keyof CreateI18nProfileResponses];

export type ListI18nKeysData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: {
        /**
         * Profile id to list keys for.
         */
        profile_id?: string;
        /**
         * Only keys whose name starts with this.
         */
        prefix?: string;
        /**
         * Free-text search — matches keys whose name OR value contains this substring (case-insensitive). Use it to find the key behind a piece of on-screen copy.
         */
        q?: string;
        /**
         * Max keys to return (1–500).
         */
        limit?: number;
    };
    url: '/api/admin/i18n/keys';
};

export type ListI18nKeysErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type ListI18nKeysError = ListI18nKeysErrors[keyof ListI18nKeysErrors];

export type ListI18nKeysResponses = {
    /**
     * List i18n keys
     */
    200: ListI18nKeysResponse;
};

export type ListI18nKeysResponse2 = ListI18nKeysResponses[keyof ListI18nKeysResponses];

export type PushI18nKeysData = {
    body: PushI18nKeysRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/api/admin/i18n/keys';
};

export type PushI18nKeysErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type PushI18nKeysError = PushI18nKeysErrors[keyof PushI18nKeysErrors];

export type PushI18nKeysResponses = {
    /**
     * Push new i18n keys (insert-only)
     */
    201: PushI18nKeysResponse;
};

export type PushI18nKeysResponse2 = PushI18nKeysResponses[keyof PushI18nKeysResponses];

export type UpdateI18nKeyData = {
    body: UpdateI18nKeyRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * The key's id.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/i18n/keys/{id}';
};

export type UpdateI18nKeyErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type UpdateI18nKeyError = UpdateI18nKeyErrors[keyof UpdateI18nKeyErrors];

export type UpdateI18nKeyResponses = {
    /**
     * Update one i18n key
     */
    200: UpdateI18nKeyResponse;
};

export type UpdateI18nKeyResponse2 = UpdateI18nKeyResponses[keyof UpdateI18nKeyResponses];

export type ListI18nDraftsData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/api/admin/i18n/drafts';
};

export type ListI18nDraftsErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type ListI18nDraftsError = ListI18nDraftsErrors[keyof ListI18nDraftsErrors];

export type ListI18nDraftsResponses = {
    /**
     * List translation drafts
     */
    200: ListI18nDraftsResponse;
};

export type ListI18nDraftsResponse2 = ListI18nDraftsResponses[keyof ListI18nDraftsResponses];

export type CreateI18nDraftData = {
    body: CreateI18nDraftRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/api/admin/i18n/drafts';
};

export type CreateI18nDraftErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type CreateI18nDraftError = CreateI18nDraftErrors[keyof CreateI18nDraftErrors];

export type CreateI18nDraftResponses = {
    /**
     * Create a translation draft
     */
    200: I18nDraft;
};

export type CreateI18nDraftResponse = CreateI18nDraftResponses[keyof CreateI18nDraftResponses];

export type UpdateI18nDraftData = {
    body: UpdateI18nDraftRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * The draft id to update.
         */
        draftId: string;
    };
    query?: never;
    url: '/api/admin/i18n/drafts/{draftId}';
};

export type UpdateI18nDraftErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type UpdateI18nDraftError = UpdateI18nDraftErrors[keyof UpdateI18nDraftErrors];

export type UpdateI18nDraftResponses = {
    /**
     * Update a translation draft
     */
    200: I18nDraft;
};

export type UpdateI18nDraftResponse = UpdateI18nDraftResponses[keyof UpdateI18nDraftResponses];

export type PublishI18nProfileData = {
    body: PublishI18nProfileRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * The profile id to publish.
         */
        profileId: string;
    };
    query?: never;
    url: '/api/admin/i18n/profiles/{profileId}/publish';
};

export type PublishI18nProfileErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type PublishI18nProfileError = PublishI18nProfileErrors[keyof PublishI18nProfileErrors];

export type PublishI18nProfileResponses = {
    /**
     * Publish a profile chunk
     */
    201: PublishI18nProfileResponse;
};

export type PublishI18nProfileResponse2 = PublishI18nProfileResponses[keyof PublishI18nProfileResponses];

export type SetI18nLabelData = {
    body: SetI18nLabelRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/api/admin/i18n/set';
};

export type SetI18nLabelErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type SetI18nLabelError = SetI18nLabelErrors[keyof SetI18nLabelErrors];

export type SetI18nLabelResponses = {
    /**
     * Set a key's value and publish it live
     */
    200: SetI18nLabelResponse;
};

export type SetI18nLabelResponse2 = SetI18nLabelResponses[keyof SetI18nLabelResponses];

export type ListErrorsData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: {
        /**
         * Filter by triage state. `all` (the default) returns every status.
         */
        status?: 'open' | 'resolved' | 'ignored' | 'all';
        /**
         * Case-insensitive substring match against `message`, `errorType`, and `subject`.
         */
        q?: string;
        /**
         * Maximum number of rows to return (1–500). Defaults to 200.
         */
        limit?: number;
    };
    url: '/api/admin/errors';
};

export type ListErrorsErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type ListErrorsError = ListErrorsErrors[keyof ListErrorsErrors];

export type ListErrorsResponses = {
    /**
     * List tracked errors
     */
    200: ListErrorsResponse;
};

export type ListErrorsResponse2 = ListErrorsResponses[keyof ListErrorsResponses];

export type GetErrorData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque error id (`err_…`).
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/errors/{id}';
};

export type GetErrorErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type GetErrorError = GetErrorErrors[keyof GetErrorErrors];

export type GetErrorResponses = {
    /**
     * Get a tracked error
     */
    200: ErrorRecord;
};

export type GetErrorResponse = GetErrorResponses[keyof GetErrorResponses];

export type UpdateErrorStatusData = {
    body: UpdateErrorStatusRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque error id (`err_…`).
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/errors/{id}';
};

export type UpdateErrorStatusErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type UpdateErrorStatusError = UpdateErrorStatusErrors[keyof UpdateErrorStatusErrors];

export type UpdateErrorStatusResponses = {
    /**
     * Update a tracked error's status
     */
    200: ErrorRecord;
};

export type UpdateErrorStatusResponse = UpdateErrorStatusResponses[keyof UpdateErrorStatusResponses];

export type FileErrorTicketData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque error id (`err_…`).
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/errors/{id}/file';
};

export type FileErrorTicketErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type FileErrorTicketError = FileErrorTicketErrors[keyof FileErrorTicketErrors];

export type FileErrorTicketResponses = {
    /**
     * File a feedback ticket for an error
     */
    201: FileErrorTicketResponse;
};

export type FileErrorTicketResponse2 = FileErrorTicketResponses[keyof FileErrorTicketResponses];

export type GetErrorSeriesData = {
    body: ErrorSeriesRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque error id (`err_…`).
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/errors/{id}/series';
};

export type GetErrorSeriesErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type GetErrorSeriesError = GetErrorSeriesErrors[keyof GetErrorSeriesErrors];

export type GetErrorSeriesResponses = {
    /**
     * Get an error's occurrence series
     */
    200: ErrorSeriesResponse;
};

export type GetErrorSeriesResponse = GetErrorSeriesResponses[keyof GetErrorSeriesResponses];

export type ListConnectorsData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/api/admin/connectors';
};

export type ListConnectorsErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type ListConnectorsError = ListConnectorsErrors[keyof ListConnectorsErrors];

export type ListConnectorsResponses = {
    /**
     * List connectors
     */
    200: ListConnectorsResponse;
};

export type ListConnectorsResponse2 = ListConnectorsResponses[keyof ListConnectorsResponses];

export type CreateConnectorData = {
    body: CreateConnectorRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/api/admin/connectors';
};

export type CreateConnectorErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type CreateConnectorError = CreateConnectorErrors[keyof CreateConnectorErrors];

export type CreateConnectorResponses = {
    /**
     * Create a connector
     */
    201: CreateConnectorResponse;
};

export type CreateConnectorResponse2 = CreateConnectorResponses[keyof CreateConnectorResponses];

export type DeleteConnectorData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque connector id.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/connectors/{id}';
};

export type DeleteConnectorErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type DeleteConnectorError = DeleteConnectorErrors[keyof DeleteConnectorErrors];

export type DeleteConnectorResponses = {
    /**
     * Delete a connector
     */
    200: DeleteConnectorResponse;
};

export type DeleteConnectorResponse2 = DeleteConnectorResponses[keyof DeleteConnectorResponses];

export type GetConnectorData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque connector id.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/connectors/{id}';
};

export type GetConnectorErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type GetConnectorError = GetConnectorErrors[keyof GetConnectorErrors];

export type GetConnectorResponses = {
    /**
     * Get a connector
     */
    200: ConnectorRecord;
};

export type GetConnectorResponse = GetConnectorResponses[keyof GetConnectorResponses];

export type UpdateConnectorData = {
    body: UpdateConnectorRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque connector id.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/connectors/{id}';
};

export type UpdateConnectorErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type UpdateConnectorError = UpdateConnectorErrors[keyof UpdateConnectorErrors];

export type UpdateConnectorResponses = {
    /**
     * Update a connector
     */
    200: UpdateConnectorResponse;
};

export type UpdateConnectorResponse2 = UpdateConnectorResponses[keyof UpdateConnectorResponses];

export type FireConnectorData = {
    body?: FireConnectorRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque connector id.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/connectors/{id}/fire';
};

export type FireConnectorErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type FireConnectorError = FireConnectorErrors[keyof FireConnectorErrors];

export type FireConnectorResponses = {
    /**
     * Fire a trigger connector
     */
    200: FireConnectorResponse;
};

export type FireConnectorResponse2 = FireConnectorResponses[keyof FireConnectorResponses];

export type TestConnectorData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque connector id.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/connectors/{id}/test';
};

export type TestConnectorErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type TestConnectorError = TestConnectorErrors[keyof TestConnectorErrors];

export type TestConnectorResponses = {
    /**
     * Test a connector
     */
    200: TestConnectorResponse;
};

export type TestConnectorResponse2 = TestConnectorResponses[keyof TestConnectorResponses];

export type ListKeysData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: {
        /**
         * Page size (1–500). Defaults to 100.
         */
        limit?: number;
        /**
         * Opaque cursor returned in the previous page's `next_cursor`. Omit for the first page.
         */
        cursor?: string;
    };
    url: '/api/admin/keys';
};

export type ListKeysErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type ListKeysError = ListKeysErrors[keyof ListKeysErrors];

export type ListKeysResponses = {
    /**
     * List API keys
     */
    200: ListKeysResponse;
};

export type ListKeysResponse2 = ListKeysResponses[keyof ListKeysResponses];

export type CreateKeyData = {
    body: CreateKeyRequest;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: never;
    url: '/api/admin/keys';
};

export type CreateKeyErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type CreateKeyError = CreateKeyErrors[keyof CreateKeyErrors];

export type CreateKeyResponses = {
    /**
     * Create an API key
     */
    201: CreateKeyResponse;
};

export type CreateKeyResponse2 = CreateKeyResponses[keyof CreateKeyResponses];

export type RevokeKeyData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path: {
        /**
         * Stable opaque key id (UUID) returned by `create` / `list`.
         */
        id: string;
    };
    query?: never;
    url: '/api/admin/keys/{id}/revoke';
};

export type RevokeKeyErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type RevokeKeyError = RevokeKeyErrors[keyof RevokeKeyErrors];

export type RevokeKeyResponses = {
    /**
     * Revoke an API key
     */
    200: RevokeKeyResponse;
};

export type RevokeKeyResponse2 = RevokeKeyResponses[keyof RevokeKeyResponses];

export type SearchResourcesData = {
    body?: never;
    headers?: {
        /**
         * Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it).
         */
        'X-Project-Id'?: string;
    };
    path?: never;
    query?: {
        /**
         * Search query. Matched as a case-insensitive substring against each resource's `name` and title/display name. Trimmed; empty returns no hits.
         */
        q?: string;
    };
    url: '/api/admin/search';
};

export type SearchResourcesErrors = {
    /**
     * The request was malformed (bad JSON or missing project scope).
     */
    400: Error;
    /**
     * Missing or invalid admin SDK key.
     */
    401: Error;
    /**
     * The key is valid but not allowed to perform this action.
     */
    403: Error;
    /**
     * The resource does not exist or is not visible to the caller.
     */
    404: Error;
    /**
     * The mutation conflicts with current state.
     */
    409: Error;
    /**
     * The request body failed validation.
     */
    422: Error;
};

export type SearchResourcesError = SearchResourcesErrors[keyof SearchResourcesErrors];

export type SearchResourcesResponses = {
    /**
     * Search project resources
     */
    200: SearchResponse;
};

export type SearchResourcesResponse = SearchResourcesResponses[keyof SearchResourcesResponses];
