{
  "name": "@shipeasy/skills-contract",
  "private": true,
  "version": "0.0.0",
  "description": "Contract test: every CLI/MCP command referenced by a shipped skill must exist (command path + flags + tool params) on the real CLI command tree and MCP tool catalog.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@shipeasy/cli": "workspace:*",
    "@shipeasy/mcp": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.19.39",
    "typescript": "^5.7.4",
    "vitest": "^2.1.9"
  },
  "engines": {
    "node": ">=20"
  }
}
